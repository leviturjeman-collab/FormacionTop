import type { PathLesson } from './tool-path'
import { method } from './study-method'

const es = [
 ['Del encargo a la evidencia','Codex puede leer y modificar archivos del proyecto y utilizar herramientas del entorno disponible. Tú defines el comportamiento que necesitas y revisas la evidencia. En este recorrido construirás una función pequeña que decide si una cantidad de huéspedes es válida. El ejercicio no necesita una cuenta de reservas, una base de datos ni datos de clientes. Así puedes concentrarte en la relación entre requisito, código y prueba.','Distingue plan, cambio y verificación. Un plan explica qué se hará; un cambio modifica archivos; una prueba ejecutada aporta una observación. «He añadido una validación» no demuestra que la validación funcione. En cada unidad conservarás algo que puedas inspeccionar: un encargo, una función, una comparación o una salida de prueba.','Ficha resuelta\nProblema: se admite una reserva con 0 huéspedes.\nEntrada: un valor que representa huéspedes.\nRegla: aceptar únicamente números enteros de 1 a 8.\nSalida: true si cumple; false en cualquier otro caso.\nFuera de alcance: crear reservas, cobrar o enviar mensajes.','El valor booleano separa la regla del resto de la aplicación. Todavía no decide qué mensaje mostrará un formulario ni cómo guardará una reserva. Esa delimitación hace posible comprobar el núcleo sin depender de servicios externos.','Escribe una ficha equivalente para validar una cantidad de entradas de 1 a 6. No cambies todavía el código: identifica qué requisito sería distinto.'],
 ['Carpeta, archivos y entorno de ejecución','Una carpeta abierta determina qué proyecto estás trabajando. Antes de editar, comprueba su ruta y sus archivos. Abrir una carpeta superior con varios proyectos puede llevar a modificar el lugar equivocado. Utiliza booking-practice y conserva una copia inicial. Pide una inspección del entorno antes de solicitar instalaciones o cambios.','En esta práctica usaremos archivos .mjs, módulos de JavaScript que Node puede ejecutar. Node es el entorno de ejecución; Codex es el asistente que trabaja con él; la terminal muestra los comandos y su salida. Si Node no está disponible, la práctica de ejecución queda pendiente hasta preparar ese requisito. No interpretes una explicación de código como una ejecución.','Encargo de preparación\n«Muestra el directorio actual y los archivos de esta carpeta. Comprueba si Node está disponible con node --version. Explica qué falta para ejecutar archivos .mjs aquí. No modifiques archivos todavía».\n\nResultado a registrar: ruta observada, versión disponible y forma de recuperar la copia inicial.','Una versión mostrada por node --version confirma ese ejecutable en ese entorno. No demuestra aún que el programa exista ni que sus pruebas funcionen. La comprobación tiene un alcance concreto y conviene conservarlo.','Abre una copia de la carpeta con otro nombre y repite la inspección. Explica qué observación te permite distinguir ambos espacios.'],
 ['Una función, unas pruebas y unas instrucciones','Separar responsabilidades permite localizar cambios. booking.mjs contiene la regla; booking.test.mjs contiene ejemplos que la comprueban; README.md explica el propósito y cómo ejecutar. El archivo de pruebas importa la función, por lo que observa el mismo código que se entregará. Copiar la función dentro de las pruebas crearía dos versiones y podría ocultar un defecto en la original.','Un archivo .mjs puede exportar un valor para que otro lo importe. En este ejercicio exportamos validateGuests. El nombre debe coincidir en la exportación y en la importación; la ruta relativa indica dónde encontrar el archivo. Si aparece un error de módulo no encontrado, revisa nombre, extensión y ruta antes de cambiar la lógica de validación.','booking-practice/\n  booking.mjs       ← exporta validateGuests\n  booking.test.mjs  ← importa y comprueba la función\n  README.md         ← explica requisitos y comandos\n\nImportación prevista:\nimport { validateGuests } from "./booking.mjs";','Las pruebas consumen la misma función que utilizaría una aplicación. El README permite a otra persona reproducir el ensayo sin conocer la conversación. No hace falta añadir un framework para comprobar una regla tan pequeña.','Dibuja una flecha desde el archivo de pruebas al archivo de función. Anota qué archivo editarías para cambiar el límite y cuáles revisarías después.'],
 ['Contrato de entrada y salida','Un contrato describe valores aceptados y resultados. Aquí aceptamos números enteros desde 1 hasta 8, ambos incluidos. El texto "2" se rechaza: convertir texto es otra decisión que correspondería a la capa que lee el formulario. Mantener esa separación evita que diferentes entradas se acepten por una conversión accidental.','Los límites incluidos se expresan con >= y <=. El caso 1 comprueba el comienzo permitido y el caso 8 el final permitido. Los casos 0 y 9 comprueban lo que queda inmediatamente fuera. Añade un decimal y un tipo incorrecto porque comprobar solo el intervalo no prueba la exigencia de entero numérico.','Contrato de validateGuests(value)\n1 → true\n8 → true\n0 → false\n9 → false\n1.5 → false\n"2" → false\n\nEncargo: «Implementa exactamente este contrato y exporta la función desde booking.mjs. No añadas dependencias ni conviertas texto de forma implícita».','Los seis casos comprueban decisiones diferentes. La entrada "2" podría representar una cantidad en un formulario, pero el contrato de esta función no la acepta. El código debe seguir la regla acordada, no reinterpretarla para hacer que el ejemplo parezca funcionar.','Define el contrato de la variante de entradas de 1 a 6. Incluye el primer valor aceptado, el último y los dos valores inmediatamente exteriores.'],
 ['Leer una solución línea por línea','Number.isInteger(value) comprueba que el valor es un número entero. Para un texto como "2" devuelve false. La comparación value >= 1 exige el mínimo y value <= 8 exige el máximo. El operador && requiere que se cumplan las tres condiciones: si alguna falla, el resultado es false.','Para value = 2, las tres condiciones son verdaderas y la función devuelve true. Para value = 9, el tipo y el mínimo se cumplen pero el máximo falla. Para value = 1.5, falla la condición de entero. Esta lectura con valores concretos te permite explicar la solución en vez de tratarla como un bloque que simplemente parece correcto.','// booking.mjs\nexport function validateGuests(value) {\n  return Number.isInteger(value)\n    && value >= 1\n    && value <= 8;\n}\n\n// booking.test.mjs\nimport assert from "node:assert/strict";\nimport { validateGuests } from "./booking.mjs";\nassert.equal(validateGuests(1), true);\nassert.equal(validateGuests(8), true);\nassert.equal(validateGuests(0), false);\nassert.equal(validateGuests("2"), false);\nconsole.log("4 comprobaciones correctas");\n\n// Ejecuta en la terminal:\n// node booking.test.mjs','assert.equal compara el resultado real con el esperado y detiene la ejecución si no coinciden. El mensaje final solo se alcanza después de superar las cuatro comprobaciones. Si aparece un error antes, conserva la salida: no equivale a cuatro pruebas superadas.','Añade una comprobación para 9 y otra para 1.5. Actualiza el mensaje final para que describa las seis comprobaciones realmente ejecutadas.'],
 ['Revisar alcance y capacidad de detectar fallos','Una revisión observa los cambios respecto a una versión anterior. En este ejercicio esperas una función, sus pruebas y documentación. Pregunta qué requisito justifica cada archivo modificado. Si aparecen servicios externos o cambios en carpetas ajenas al ejercicio, revisa su necesidad antes de incluirlos en la entrega.','También debes revisar la calidad de la comprobación. Un console.log("correcto") se ejecutaría aunque la función estuviera mal. Una aserción compara el resultado real. Para comprobar que detecta un defecto, modifica temporalmente una copia de la función y observa una prueba fallida; después restaura la versión correcta.','Ensayo de revisión en una copia\n1. Conserva la función correcta.\n2. Sustituye temporalmente su cuerpo por return true.\n3. Ejecuta node booking.test.mjs.\n4. La comprobación de 0 debe fallar.\n5. Restaura la función y repite.','La mutación temporal demuestra que la prueba no se limita a imprimir una etiqueta de éxito. Debe fallar porque se ha alterado el comportamiento observado. Restaura la copia antes de guardarla como entrega.','Revisa las seis comprobaciones y relaciona cada una con una condición del contrato. Señala qué defecto no detectaría una prueba que solo usara el valor 2.'],
 ['Del síntoma a la comparación equivocada','El síntoma es aceptar cero huéspedes. El caso mínimo que lo reproduce es validateGuests(0). Una causa posible es haber escrito value >= 0 en lugar de value >= 1. Debes comprobarla leyendo la condición y ejecutando el caso. No cambies simultáneamente el tipo de dato y los límites: perderías claridad sobre la causa.','Una corrección documentada conserva el mismo caso antes y después. El resultado inicial true contradice la expectativa false. Tras cambiar únicamente el límite inferior, el resultado debe ser false. Ejecuta además el archivo completo para comprobar que 1 y 8 siguen aceptándose. Corregir cero rechazando todo el intervalo tampoco sería una solución válida.','Defecto de ensayo: value >= 0\nEntrada: 0\nEsperado: false\nObservado antes: true\nCorrección: value >= 1\nObservado esperado después: false\n\nEncargo: «Reproduce este caso con la prueba existente. Corrige el límite inferior y ejecuta todas las comprobaciones. Muestra el resultado del comando».','La prueba de regresión conserva el ejemplo que reveló el defecto. Si vuelve a cambiar el límite por error, el mismo caso debe detectarlo. El informe distingue observación de expectativa: rellena el resultado real después de ejecutar, no antes.','En una copia, cambia el límite superior a 9. Encuentra el caso mínimo que lo detecta y documenta la corrección con la misma estructura.'],
 ['Pruebas de límites, tipos y ausencia','Organiza los casos por la condición que comprueban. Los enteros 1 y 8 deben aceptarse; 0 y 9 están fuera del intervalo; -1 es negativo; 1.5 no es entero. El texto "2", null y undefined no son números enteros válidos. Esta clasificación ayuda a evitar muchas muestras repetidas que verifican lo mismo y dejan otra condición sin probar.','Al integrar la función en un formulario, el navegador puede proporcionar una cadena de texto. La capa de lectura debe decidir cómo convertirla y cómo tratar un campo vacío. La función no muestra mensajes ni bloquea una escritura por sí sola: la interfaz tiene que usar su resultado. Por eso separarás las pruebas de función de las pruebas de interacción.','// Amplía booking.test.mjs\nconst cases = [\n  [1, true], [8, true], [0, false], [9, false],\n  [-1, false], [1.5, false], ["2", false],\n  [null, false], [undefined, false]\n];\nfor (const [input, expected] of cases) {\n  assert.equal(validateGuests(input), expected,\n    `Entrada: ${String(input)}`);\n}\nconsole.log(`${cases.length} casos correctos`);','La tabla permite ver cobertura y expectativas sin duplicar la lógica de la función. El mensaje incluye la entrada para localizar el fallo. Estas nueve comprobaciones no prueban una base de datos ni la accesibilidad de un formulario: ese trabajo requiere otras observaciones.','Añade NaN e Infinity y explica por qué deberían rechazarse. Si tienes formulario, escribe por separado tres comprobaciones: vacío, válido y fuera del límite.'],
 ['Una versión que representa un estado comprobado','Guardar una versión permite recuperar un conjunto coherente de archivos. Antes del punto de control, revisa que función, pruebas e instrucciones correspondan al mismo contrato. En Git, un commit incluye los cambios seleccionados; los archivos nuevos no quedan incluidos solo por existir en la carpeta. Comprueba su presencia en el estado que conservas.','Practica la recuperación en una copia o rama de ensayo para no perder el trabajo actual. El objetivo es demostrar que puedes recuperar una versión identificable y ejecutar sus pruebas. Anota la referencia utilizada y el resultado del comando. Una copia incompleta puede abrirse y aun así carecer del archivo necesario para ejecutar.','Registro de versión\nDescripción: validar huéspedes enteros de 1 a 8.\nIncluye: booking.mjs, booking.test.mjs y README.md.\nComprobación: node booking.test.mjs.\nReferencia: anota el identificador real del punto de control.\nRecuperación: repetir el comando en una copia de esa versión.','La descripción indica comportamiento, no solo «cambios». La referencia debe proceder del sistema de versiones o de la copia real. No inventes un identificador ni afirmes haber recuperado una versión por haberla guardado.','Recupera la copia en otro espacio de ensayo y sigue las instrucciones sin consultar la conversación. Registra cualquier paso que falte en el README.'],
 ['Entregar para que otra persona pueda continuar','La entrega reúne código, pruebas e instrucciones verificadas. La persona destinataria necesita saber qué hace la función, qué entorno requiere y cómo reconocer una ejecución correcta. Añade límites: esta práctica valida un valor, pero no implementa el formulario completo ni crea reservas. Evitar esa ambigüedad permite utilizar la función correctamente.','Haz una demostración breve con una entrada válida y otra inválida, después ejecuta todas las pruebas. Explica qué se ha comprobado y qué integración falta. Si más adelante publicas una aplicación, habrá que verificar la URL y el recorrido real del usuario. Un commit y una carpeta ejecutable no son por sí solos una publicación.','README de entrega\nPropósito: validar huéspedes enteros de 1 a 8.\nRequisito: Node disponible; anotar versión comprobada.\nEjecutar: node booking.test.mjs.\nCorrecto: todas las aserciones pasan y aparece el recuento final.\nEntrada inválida: la función devuelve false.\nLímites: sin formulario, almacenamiento ni envío de reservas.\nVersión: referencia real de la entrega.','Un entorno limpio debe poder seguir estas instrucciones. Si la persona necesita una explicación verbal para ejecutar, incorpórala al documento. La evidencia final describe comandos realmente ejecutados y limita la conclusión al comportamiento que has observado.','Integra la función en una pequeña interfaz de práctica o adapta el contrato a entradas de 1 a 6. Presenta el cambio y repite las pruebas pertinentes sin reutilizar expectativas que ya no correspondan.'],
]
const english = [
 ['From a request to evidence','Codex works with project files and available tools. You define behavior and inspect evidence. This exercise builds one guest-count validator without booking services, customer data or external accounts.','Separate a plan, file changes and verification. A statement that validation was added is not evidence that it ran. Keep something inspectable from each unit.','Brief\nProblem: zero guests are accepted.\nInput: a guest-count value.\nRule: accept integer numbers 1 through 8.\nOutput: true for valid input, false otherwise.\nOut of scope: reservations, payments and messages.','The boolean result isolates the rule from forms and storage. This makes the core independently testable.','Write an equivalent brief for ticket counts from 1 to 6 without editing code yet.'],
 ['Folder, files and runtime','Verify the current directory and its files before editing. Use a dedicated booking-practice folder and preserve an initial copy.','JavaScript .mjs files run in Node. Codex is the assistant, Node the runtime and the terminal shows execution output. An explanation is not a run.','Preparation request\n“Show the current directory and files. Run node --version to check the runtime. Explain missing prerequisites for executing .mjs here. Do not edit yet.”','The version output establishes runtime availability, not program correctness. Record the actual directory and version.','Repeat the inspection in a differently named copy and explain how you distinguish them.'],
 ['Separate function, tests and instructions','booking.mjs contains the rule; booking.test.mjs checks it; README.md explains usage. Import the real function rather than copying its implementation into tests.','Exports and imports must agree on name and relative path. A module-not-found error concerns file resolution before it concerns validator logic.','booking-practice/\n  booking.mjs\n  booking.test.mjs\n  README.md\n\nimport { validateGuests } from "./booking.mjs";','Tests exercise the same function an application would import. No additional framework is necessary for this small rule.','Draw the test-to-function dependency and name the files to review when the limit changes.'],
 ['Specify input and output contracts','Accept only integer numbers from 1 through 8 inclusive. Reject the string "2"; form parsing is a separate responsibility.','Use 1 and 8 for included boundaries, 0 and 9 for adjacent rejected values, and a decimal and string for type rules.','validateGuests(value)\n1 → true\n8 → true\n0 → false\n9 → false\n1.5 → false\n"2" → false','Each case targets a different decision. The implementation must follow the agreed contract rather than silently broaden it.','Define the corresponding boundary cases for ticket counts 1 through 6.'],
 ['Read the implementation line by line','Number.isInteger checks numeric integer input. The comparisons enforce both boundaries and && requires all three conditions.','For 2 every condition is true; for 9 the upper bound fails; for 1.5 the integer test fails. Explain expressions using concrete inputs.','// booking.mjs\nexport function validateGuests(value) {\n  return Number.isInteger(value)\n    && value >= 1\n    && value <= 8;\n}\n\n// booking.test.mjs\nimport assert from "node:assert/strict";\nimport { validateGuests } from "./booking.mjs";\nassert.equal(validateGuests(1), true);\nassert.equal(validateGuests(8), true);\nassert.equal(validateGuests(0), false);\nassert.equal(validateGuests("2"), false);\nconsole.log("4 checks passed");\n\n// Run: node booking.test.mjs','assert.equal stops execution on mismatch. The final message is reached only after all four checks pass.','Add checks for 9 and 1.5 and update the final message to describe six actual checks.'],
 ['Review scope and test sensitivity','Relate each changed file to the requirement. A tiny validator should have a clear explanation for any extra service or unrelated edit.','A success log alone cannot detect defects. Temporarily alter a disposable copy and observe a failing assertion, then restore it.','Mutation exercise\n1. Keep the correct function.\n2. Temporarily replace its body with return true.\n3. Run node booking.test.mjs.\n4. The zero-guests assertion must fail.\n5. Restore and rerun.','The mutation establishes that the test observes behavior instead of merely printing success.','Map the six checks to the contract and explain what a test using only 2 would miss.'],
 ['Find the incorrect comparison','The symptom is accepting zero. The smallest reproducer is validateGuests(0). Inspect whether the lower bound accidentally uses >= 0.','Keep the same case before and after repair and rerun all checks. Rejecting the entire interval would not be a valid fix.','Fault: value >= 0\nInput: 0\nExpected: false\nBefore: true\nCorrection: value >= 1\nExpected after: false','A regression test retains the case that exposed the defect. Record actual observations after running.','Introduce an upper-bound fault in a copy and document its smallest failing case and repair.'],
 ['Cover boundaries, types and absence','Group cases by the rule they check: valid endpoints, outside interval, negative, decimal, string and missing values.','A form may return text. Parsing and visible error behavior need separate integration checks; the validator does not show messages or prevent writes by itself.','const cases = [\n  [1, true], [8, true], [0, false], [9, false],\n  [-1, false], [1.5, false], ["2", false],\n  [null, false], [undefined, false]\n];\nfor (const [input, expected] of cases) {\n  assert.equal(validateGuests(input), expected,\n    `Input: ${String(input)}`);\n}\nconsole.log(`${cases.length} cases passed`);','The case table documents independent expectations and identifies failing inputs. It does not test form accessibility or storage.','Add NaN and Infinity, explain their rejection and define separate form checks if applicable.'],
 ['Keep a checked version','A checkpoint should contain matching code, tests and instructions. Git includes selected changes; new files need explicit inclusion.','Test recovery in a separate copy or branch. Record the actual reference and repeat the documented command.','Version record\nPurpose: integer guests 1–8.\nFiles: booking.mjs, booking.test.mjs, README.md.\nCheck: node booking.test.mjs.\nReference: actual checkpoint identifier.\nRecovery: same command in a recovered copy.','Saving a version does not prove recovery; reopen and run it without inventing a reference.','Recover the copy and follow the README without the conversation. Record any missing step.'],
 ['Hand over a repeatable result','Provide code, tests and verified instructions. Explain purpose, runtime and success output, plus limits: no full form or booking storage.','Demonstrate valid and invalid input, run all tests and separate verified behavior from future integration. A commit is not a deployment.','Delivery README\nPurpose: integer guests 1–8.\nRequires: Node; record tested version.\nRun: node booking.test.mjs.\nSuccess: all assertions pass and final count appears.\nInvalid input: false.\nLimits: no form, storage or booking messages.\nVersion: actual delivery reference.','A clean environment should follow the instructions without verbal assistance. Report only commands actually executed.','Integrate into a practice interface or adapt to ticket counts 1–6 and revise the corresponding tests.'],
]
export function expandCodex(lesson:PathLesson,index:number,en:boolean):PathLesson {
 const row=(en?english:es)[index],m=method(index,en)
 return {...lesson,explanation:[row[1],row[2]],study:{objective:lesson.purpose,concepts:[{title:row[0],paragraphs:[row[1],row[2]]},{title:m.title,paragraphs:m.paragraphs}],solution:row[3],reasoning:row[4],exercise:row[5],criteria:[lesson.check,m.evidence,en?'Explain the result using the observed input and command output.':'Explica el resultado usando la entrada y la salida del comando observado.'],minutes:index===1?30:45}}
}
