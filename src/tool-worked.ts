/** Worked examples authored for the academy. No external execution is implied. */
const solutions:Record<string,[string,string]> = {
 cursor:['Normaliza tanto la consulta como el nombre antes de comparar: clientes.filter(c => c.nombre.toLocaleLowerCase().includes(consulta.toLocaleLowerCase())). Con [{nombre: "Ana"}], las consultas «Ana» y «ana» conservan ese registro; «Luis» devuelve una lista vacía. Esta regla solo ignora mayúsculas: no promete eliminar acentos.','Normalize both query and name before comparison: customers.filter(c => c.name.toLocaleLowerCase().includes(query.toLocaleLowerCase())). For [{name: "Ana"}], Ana and ana retain the record; Luis returns an empty list. This rule does not remove accents.'],
 'claude-code':['Si precio1 y precio2 llegan como texto, «10» + «20» concatena y produce «1020». Convierte entradas no vacías con Number y rechaza valores no finitos antes de sumar. Number("10") + Number("20") produce 30. Number("abc") es NaN: debe activar un error, no llegar al total. Decide por separado cómo tratar una cadena vacía, porque Number("") produce 0.','Text values "10" + "20" concatenate into "1020". Convert nonempty inputs with Number and reject nonfinite values before addition. Number("10") + Number("20") is 30; Number("abc") is NaN and must be rejected. Decide empty-string behavior explicitly because Number("") is 0.'],
 github:['El README de la práctica contiene: propósito del repositorio, requisitos, pasos de instalación realmente comprobados, comando de ejecución y resultado esperado. El cambio se presenta en una rama y un pull request cuya descripción indica qué instrucciones se añadieron y cómo se comprobaron. La revisión compara las instrucciones con los archivos; no aprueba solo por tener formato Markdown.','The practice README contains purpose, prerequisites, verified installation steps, run command and expected result. Submit the change on a branch through a pull request describing the instructions and their validation. Review against actual files, not Markdown formatting alone.'],
 python:['La fila taza produce 2 × 5 = 10 y la fila libro produce 1 × 12 = 12. El total es 22. Con csv.DictReader, lee cantidad y precio por sus encabezados; convierte y valida antes de multiplicar. Conserva el número de fila para informar del error. No omitas silenciosamente una fila inválida: el total parecería correcto pero estaría incompleto.','Mugs contribute 2 × 5 = 10 and the book 1 × 12 = 12, totaling 22. Use csv.DictReader headers, convert and validate before multiplication, and retain row numbers for errors. Silently skipping invalid rows would conceal an incomplete total.'],
 node:['Los argumentos de terminal llegan como texto: process.argv.slice(2). Exige exactamente dos valores y comprueba que no estén vacíos. Convierte con Number, valida con Number.isFinite y suma. La llamada con 10 y 20 muestra 30. Sin argumentos, muestra «Uso: node suma.mjs numero1 numero2» y termina con un estado de error.','Terminal arguments are strings: process.argv.slice(2). Require exactly two nonempty values, convert with Number, validate with Number.isFinite and add. Inputs 10 and 20 print 30. Without arguments, show “Usage: node sum.mjs number1 number2” and exit with an error status.'],
 typescript:['type Reserva = { id: string; huespedes: number } define la estructura prevista. Asignar {id: "R1", huespedes: "2"} produce un error de tipos al comprobar el código. Los datos recibidos desde un formulario siguen necesitando validación durante la ejecución: el tipo no convierte ni revisa por sí solo un JSON externo.','type Booking = { id: string; guests: number } defines the expected structure. Assigning {id: "R1", guests: "2"} fails type checking. External form or JSON data still needs runtime validation; the type does not convert or inspect it automatically.'],
 react:['Representa cada tarea con id, título y completada. Al cambiar una, crea un nuevo arreglo: tareas.map(t => t.id === id ? {...t, completada: !t.completada} : t). Conecta un input de tipo checkbox a su etiqueta. La segunda tarea cambia sin modificar las otras dos y puede activarse mediante teclado.','Give each task an id, title and completed flag. Update with tasks.map(t => t.id === id ? {...t, completed: !t.completed} : t). Associate each checkbox with a label. Toggling the second task leaves the other two unchanged and works by keyboard.'],
 vscode:['En index.html, localiza class="reserva" y busca .reserva en styles.css. Cambia solo la propiedad de color prevista, guarda y recarga la página. Si no cambia, comprueba primero que index.html enlaza ese archivo CSS y que el selector coincide; no añadas otra regla sin encontrar la discrepancia.','Locate class="booking" in index.html and .booking in styles.css. Change the intended color property, save and reload. If unchanged, verify the stylesheet link and matching selector before adding another rule.'],
 colab:['En una celda define valores = [10, 20, 30]. En la siguiente calcula media = sum(valores) / len(valores) y muestra media. El resultado es 20.0. Reinicia y ejecuta ambas celdas en orden. Si ejecutas la segunda sin la primera, valores no existe: ese fallo revela una dependencia, no un error de la fórmula.','Define values = [10, 20, 30] in one cell, then mean = sum(values) / len(values) and display mean in the next. Expected output is 20.0. Restart and run in order. Running the second cell first exposes its dependency on values.'],
 langchain:['La muestra tiene tres documentos con metadato sede: Norte abre a las 9, Sur a las 10 y Centro a las 8. La pregunta «¿Cuándo abre Sur?» debe recuperar el fragmento de Sur y responder «A las 10», con su referencia. Si no existe la sede solicitada, devuelve información insuficiente en vez de usar el horario de otra.','Three documents have branch metadata: North opens at 9, South at 10, Central at 8. “When does South open?” should retrieve South and answer “At 10” with its reference. Missing branches must not inherit another branch’s schedule.'],
 sheets:['En A1 escribe servicio y en B1 importe. Las filas 2–4 contienen limpieza/40, limpieza/60 y reparación/25. =SUM(B2:B4) devuelve 125. Para limpieza, =SUMIF(A2:A4,"limpieza",B2:B4) devuelve 100. Si la hoja utiliza nombres de funciones o separadores localizados, emplea el equivalente que muestre su ayuda: la operación sigue siendo suma total y suma condicionada.','With service in column A and amount in B, rows cleaning/40, cleaning/60 and repair/25 total 125 using =SUM(B2:B4). =SUMIF(A2:A4,"cleaning",B2:B4) returns 100. Use localized function names or separators if required by the spreadsheet locale.'],
 airtable:['Crea tres registros con identificadores S1, S2 y S3. Sus estados son pendiente, pendiente y resuelta. Una vista con la condición estado = pendiente muestra S1 y S2. S3 sigue en la tabla original: el filtro cambia la selección visible y no elimina datos. Cambiar S2 a resuelta deja una sola coincidencia.','Create S1, S2 and S3 with pending, pending and resolved states. A status = pending view shows S1 and S2 while S3 remains in the source table. Changing S2 to resolved leaves one match.'],
 postgres:['Con reservas(id, importe) y filas (R1,20), (R2,30), SELECT SUM(importe) AS total FROM reservas devuelve 50. Define id como clave primaria para que R1 no pueda insertarse dos veces. La suma no evita duplicados por sí sola: la restricción protege la identidad del registro.','With bookings(id, amount) rows (R1,20), (R2,30), SELECT SUM(amount) AS total FROM bookings returns 50. Make id a primary key to reject duplicate R1. The aggregate does not itself prevent duplicates.'],
 supabase:['El diseño tiene notas(id, owner_id, texto). La regla de lectura solo permite filas cuyo owner_id coincide con la identidad autenticada. Con las identidades de prueba A y B, A obtiene su nota y B obtiene la suya. La prueba debe realizarse con sus sesiones de usuario: una clave privilegiada no demuestra que el aislamiento funcione.','Use notes(id, owner_id, text) with a read policy limited to the authenticated owner. Test users A and B each retrieve only their note. Verify through their user sessions; a privileged key cannot demonstrate user isolation.'],
 notion:['La tabla de tareas contiene título, responsable, fecha y estado. Crea Preparar/Ana/martes/pendiente, Revisar/Luis/jueves/pendiente y Archivar/Ana/viernes/completada. Una vista de pendientes muestra las dos primeras. Abre cada ficha para comprobar que el filtro no ha ocultado información necesaria sobre responsable o fecha.','Create title, owner, date and status fields with Prepare/Ana/Tuesday/pending, Review/Luis/Thursday/pending and Archive/Ana/Friday/completed. The pending view shows the first two; opening each record retains owner and date details.'],
 obsidian:['La nota Proyecto reservas contiene el enlace [[Decisiones técnicas]]. La nota de decisiones explica «La primera versión usa datos ficticios y no envía mensajes». Al activar el enlace, abre esa nota. Una búsqueda de «datos ficticios» permite recuperar la decisión sin recordar el título exacto.','Booking project links to [[Technical decisions]]. That note records “The first version uses fictional data and sends no messages.” The link opens it, and searching “fictional data” retrieves the decision.'],
 n8n:['Resolución del flujo: inicio de prueba → validar id, huéspedes y estado → buscar RES-001 en el destino → crear solo si no existe. Con {id:"RES-001", huespedes:2, estado:"nuevo"}, la primera ejecución guarda una reserva de prueba. La repetición toma la rama existente y no crea otra. La consulta previa sirve para el ensayo; un destino concurrente necesita además una restricción única o una operación atómica.','Practice flow: test trigger → validate id, guests and status → look up RES-001 → create only if absent. The first sample creates a test booking; replay takes the existing-record branch. Concurrent production writes additionally need uniqueness or an atomic operation.'],
 make:['El escenario de ensayo recibe {id:"SOL-001", servicio:"limpieza"}. Mapea id al identificador del destino y servicio a su campo de texto. Antes de guardar, comprueba que ambos estén presentes. Una segunda ejecución con SOL-001 debe seguir la regla de actualización u omisión que hayas definido, conservando un solo resultado final.','The test scenario receives {id:"REQ-001", service:"cleaning"}. Map both fields explicitly and require them before writing. Replaying REQ-001 follows the defined update or skip rule, preserving one final record.'],
 zapier:['La muestra {nombre:"Ana", estado:"pendiente"} se convierte en un registro de destino con esos dos campos. Elige los valores del evento al configurar el mapeo; escribir «Ana» como texto fijo haría que todas las solicitudes futuras conservaran ese nombre. Añade un identificador estable si vas a reconocer repeticiones.','Map event values {name:"Ana", status:"pending"} to destination fields. Hardcoding Ana would give future records the same name. Add a stable identifier when replay detection is required.'],
 pipedream:['La transformación toma precio 10 y cantidad 3. Valida que ambos sean números finitos y que cantidad sea positiva. Calcula total = precio * cantidad y devuelve {total:30}. Con cantidad -1, devuelve o lanza el error previsto antes de ejecutar una escritura posterior.','Validate finite numeric price 10 and positive quantity 3, calculate total = price * quantity and return {total:30}. Quantity -1 must take the defined error path before downstream writes.'],
 docker:['La resolución requiere identificar el comando que ya arranca la aplicación, el puerto interno y el puerto publicado del contenedor. Construye la imagen del proyecto, inicia un contenedor y abre el puerto publicado. Detén y crea otro contenedor desde la misma imagen para comprobar reproducibilidad. Documenta por separado cualquier dato que deba persistir fuera del contenedor.','Identify the working app command, internal port and published container port. Build the project image, start a container and open the published port. Recreate from the image to check reproducibility; document persistent data separately.'],
 vercel:['La entrega de ensayo es una vista previa del proyecto. Comprueba la página inicial, el formulario con un correo válido y la recarga directa de la ruta de confirmación. Registra la URL y el identificador de versión. Una construcción correcta no sustituye estas pruebas; el resultado depende también de la configuración de rutas y servicios del proyecto.','Use a project preview. Check the homepage, valid email submission and direct reload of the confirmation route. Record URL and version. A successful build does not replace route and service checks.'],
 gmail:['Borrador: «Hola. Hemos recibido tu solicitud para cambiar la reserva R1 del lunes al martes. Revisaremos la disponibilidad y te confirmaremos el cambio». Se conserva la referencia y la fecha solicitada sin afirmar que la reserva ya se ha modificado. No enviar durante la práctica.','Draft: “Hello. We received your request to move booking R1 from Monday to Tuesday. We will check availability and confirm the change.” It preserves the request without claiming the booking was already changed. Keep unsent.'],
 slack:['Muestra de incidencias: pago fallido — Ana — revisar referencia; acceso bloqueado — Luis — verificar permisos; documento ausente — sin responsable — asignar. El borrador mantiene tres filas y no atribuye la tercera a alguien por suposición. Se prepara para revisión, sin publicarlo en un canal.','Draft three incidents: payment failure—Ana—check reference; blocked access—Luis—verify permissions; missing document—unassigned—assign owner. Preserve all three without inventing the third owner. Do not post during practice.'],
 telegram:['Borrador de aviso: «Prueba · RES-001 no se ha guardado. Motivo: falta el campo correo. Siguiente paso: completar el registro y volver a validar». El aviso identifica el registro y una acción útil. No incluye credenciales ni se dirige a un chat desconocido.','Draft: “Test · RES-001 was not saved. Reason: missing email. Next: complete the record and validate again.” Identify the record and a useful action without credentials or an unknown destination.'],
 whatsapp:['Borrador: «Nuestro horario habitual es de 9:00 a 17:00. Si consultas por un festivo, necesitamos confirmar el horario de ese día». La segunda frase mantiene la duda explícita porque la muestra no proporciona un calendario de festivos.','Draft: “Our regular hours are 9:00–17:00. For a holiday, we need to confirm that day’s hours.” Holiday opening remains unconfirmed because the source provides no holiday calendar.'],
 'wispr-flow':['Texto de referencia: «Reserva de Ana Pérez, dos huéspedes, martes a las 17:30». Después de dictar, contrasta cuatro campos: Ana Pérez; 2; martes; 17:30. Corrige una confusión como «17:13» antes de usar el texto. No conviertas martes en una fecha de calendario sin conocer la semana.','Reference transcript: “Booking for Ana Perez, two guests, Tuesday at 17:30.” Check name, quantity, day and time separately. Correct “17:13” before use and do not infer a calendar date without the week.'],
 notebooklm:['Documento A: «La tienda abre a las 9». Documento B: «La tienda cierra a las 17». Resolución: «El horario descrito es de 9 a 17», con referencia A para apertura y B para cierre. Ante «¿abre en festivos?», la respuesta debe señalar que los documentos no lo indican.','Document A states opening at 9; B states closing at 17. Answer 9–17 with A supporting opening and B closing. Holiday opening is unspecified in these documents.'],
 perplexity:['La resolución es una matriz de investigación, no una política jurídica terminada: tema; afirmación encontrada; fuente oficial; fecha de consulta; ámbito; duda pendiente. Distingue describir una práctica de afirmar una obligación. Si falta el país o tipo de producto, esas condiciones quedan pendientes antes de redactar conclusiones aplicables.','Produce a research matrix, not a finished legal policy: topic, claim, official source, access date, scope and open question. Distinguish a practice from an obligation and leave jurisdiction or product scope unresolved when missing.'],
 huggingface:['Muestra de comparación: «Excelente atención» → positiva; «El pedido llegó roto» → negativa; «Recibí el paquete» → neutra. Define esas etiquetas de referencia antes de ejecutar dos modelos. Registra modelo, versión, licencia, requisitos y salida por texto. Tres muestras enseñan el método; no justifican una cifra general de precisión.','Reference sample: “Excellent service” positive; “The order arrived broken” negative; “I received the package” neutral. Record model, version, license, requirements and each output. Three samples demonstrate method, not general accuracy.'],
 replicate:['El registro del ensayo contiene modelo y versión seleccionados, entrada pública utilizada, parámetros, identificador de ejecución y ubicación de la salida. Abre el resultado y compáralo con el encargo. Antes de repetir, comprueba el consumo que indique la cuenta; no supongas que dos modelos miden el uso de la misma forma.','Record selected model/version, public sample, parameters, run identifier and output location. Open and inspect the result. Check account usage before repeating rather than assuming identical billing units across models.'],
 elevenlabs:['Guion de referencia: «Bienvenido al curso. Hoy crearás tu primer proyecto». La propuesta usa una pausa entre ambas frases y una voz autorizada. Reproduce el audio completo y verifica que «primer proyecto» no queda cortado. Una voz agradable no compensa omitir palabras del guion.','Reference script: “Welcome to the course. Today you will create your first project.” Use an authorized voice and a pause between sentences. Check full playback and an uncut ending against the script.'],
 gamma:['Propuesta de tres diapositivas: 1) Problema: las notas de reunión no se convierten en tareas claras; 2) Propuesta: una tabla con tarea, responsable y fecha; 3) Siguiente paso: probar el método con una nota ficticia. Cada página cumple una función diferente y evita repetir el mismo párrafo con otro título.','Three-slide plan: 1) Problem: meeting notes lack clear tasks; 2) Proposal: a task/owner/date table; 3) Next step: test with one fictional note. Each slide serves a distinct role.'],
 tailwind:['La tarjeta usa un contenedor flexible con ancho máximo y relleno, un título que pueda ocupar varias líneas y un botón que no dependa de un ancho fijo de escritorio. A 320 píxeles, los elementos se apilan sin cortar el título; a 1440, el contenido mantiene un ancho de lectura razonable. Comprueba ambos tamaños con el mismo texto largo.','Use a flexible card with max width, padding, wrapping title and a button without fixed desktop width. At 320 pixels content stacks without clipping; at 1440 reading width stays reasonable. Check the same long text at both sizes.'],
}
for(const id of ['openai','anthropic','claude','gemini','copilot','ollama']) solutions[id]=['Tabla resuelta: tarea «Preparar presupuesto», responsable Ana, fecha martes; tarea «Revisar presupuesto», responsable Luis, fecha jueves. Importe: «No indicado». Hay dos tareas porque la fuente contiene dos acciones asignadas. La tabla no añade fechas de calendario ni convierte una propuesta en un acuerdo confirmado.','Resolved table: Prepare quote—Ana—Tuesday; Review quote—Luis—Thursday. Amount: unspecified. Two assigned actions yield two rows; do not invent calendar dates or confirmed agreements.']
for(const id of ['base44','bolt','replit','lovable','v0','framer']) solutions[id]=['Propuesta de resolución: título «Aprende a organizar tu trabajo», explicación breve del curso, campo «Correo electrónico» y botón «Solicitar información». Un correo vacío o inválido muestra un mensaje junto al campo; uno válido muestra confirmación. En esta práctica, identifica expresamente si se trata de una demostración sin almacenamiento. El texto de confirmación no debe afirmar un envío que no existe.','Solution plan: “Learn to organize your work” heading, course description, labeled email field and “Request information” button. Invalid input shows a field error; valid input shows confirmation. Explicitly identify a demo without storage and do not claim an email was sent when none was.']
for(const id of ['canva','figma','midjourney','nano-banana']) solutions[id]=['Propuesta de composición: lienzo cuadrado, imagen principal en la mitad inferior y zona superior despejada para el título. El contenido importante queda separado de los bordes. Añade el título en una capa de texto cuando el editor lo permita y comprueba su legibilidad al tamaño de uso. Esta descripción es el modelo de resolución que debes producir y verificar; no representa una imagen ya generada.','Composition plan: square canvas, main image in the lower half, clear upper title area and safe margins. Add editable title text where supported and check at intended display size. This is a plan to produce and verify, not an already generated image.']
for(const id of ['higgsfield','seedance-2-5','heygen','descript','runway']) solutions[id]=['Propuesta de secuencia: escena inicial con escritorio desordenado; transición comprensible; escena final con el mismo escritorio ordenado. Conserva una perspectiva que permita comparar. Si añades audio, debe acompañar el cambio sin terminar antes de la última escena ni prolongarse sobre un corte accidental. El archivo final se comprueba completo fuera del editor.','Sequence plan: messy desk, understandable transition, then the same tidy desk from a comparable perspective. Any audio must align with the sequence and end cleanly. Verify the entire exported file outside the editor.']
export function workedSolution(id:string,en:boolean) { const s=solutions[id];if(!s)throw new Error(`Missing worked solution: ${id}`);return s[en?1:0] }
