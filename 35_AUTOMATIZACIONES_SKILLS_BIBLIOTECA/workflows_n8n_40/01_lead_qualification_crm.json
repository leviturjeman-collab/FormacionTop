{
  "name": "01 Lead Qualification CRM",
  "nodes": [
    {
      "parameters": {
        "path": "01-lead-qualification-crm",
        "httpMethod": "POST",
        "responseMode": "responseNode"
      },
      "id": "trigger",
      "name": "Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [
        0,
        0
      ]
    },
    {
      "parameters": {
        "jsCode": "// Campos que este flujo necesita para poder trabajar.\n// Cambia esta lista por los campos que de verdad te hagan falta a ti.\nconst REQUERIDOS = [\"email\"];\n\n// Un webhook no entrega los datos sueltos: entrega\n// { headers, params, query, body } y lo tuyo va dentro de body.\n// Si el flujo arranca por hora en vez de por webhook, no hay body y\n// los datos vienen tal cual. Esta linea cubre los dos casos.\nconst entrada = $json.body ?? $json;\n\n// Falta un campo si no viene, si viene nulo o si viene en blanco.\n// Comparar solo con '' deja pasar los que no vienen: ese era el fallo.\nconst faltan = REQUERIDOS.filter((campo) => {\n  const valor = entrada[campo];\n  return valor === undefined || valor === null || String(valor).trim() === '';\n});\n\nreturn [{\n  json: {\n    ...entrada,\n    workflow: \"01 Lead Qualification CRM\",\n    category: \"ventas\",\n    faltan,\n    status: faltan.length > 0 ? 'needs_review' : 'processed',\n    checked_at: new Date().toISOString(),\n  },\n}];"
      },
      "id": "process",
      "name": "Validate process and enrich",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        280,
        0
      ]
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{$json}}"
      },
      "id": "respond",
      "name": "Respond",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [
        560,
        0
      ]
    }
  ],
  "connections": {
    "Webhook": {
      "main": [
        [
          {
            "node": "Validate process and enrich",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Validate process and enrich": {
      "main": [
        [
          {
            "node": "Respond",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1"
  },
  "meta": {
    "academy": "AI Professional Academy",
    "category": "ventas",
    "case_roto": "campo obligatorio ausente, credencial falsa o accion sin aprobacion"
  },
  "pinData": {
    "Webhook": [
      {
        "headers": {
          "content-type": "application/json"
        },
        "params": {},
        "query": {},
        "body": {
          "lead_id": "LEAD-2026-0148",
          "nombre": "Marta Solís",
          "email": "marta.solis@ejemplo.es",
          "empresa": "Construcciones Solís SL",
          "telefono": "+34 600 111 222",
          "interes": "formación para 12 personas",
          "origen": "web"
        }
      }
    ]
  }
}
