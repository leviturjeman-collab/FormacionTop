export const deploymentWorkflow = `name: Checked production deployment
on:
  push:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: production
  cancel-in-progress: false
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm
      - run: npm ci
      - run: npm test
      - run: npm run build
  deploy:
    needs: verify
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    environment: production
    outputs:
      url: \${{ steps.publish.outputs.url }}
    env:
      VERCEL_ORG_ID: \${{ secrets.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID: \${{ secrets.VERCEL_PROJECT_ID }}
      VERCEL_TOKEN: \${{ secrets.VERCEL_TOKEN }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm
      - run: npm ci
      - run: npx --yes vercel@59.11.7 pull --yes --environment=production --token="$VERCEL_TOKEN"
      - run: npx --yes vercel@59.11.7 build --prod --token="$VERCEL_TOKEN"
      - id: publish
        shell: bash
        run: |
          url=$(npx --yes vercel@59.11.7 deploy --prebuilt --prod --token="$VERCEL_TOKEN")
          echo "url=$url" >> "$GITHUB_OUTPUT"
  notify:
    needs: [verify, deploy]
    if: always()
    runs-on: ubuntu-latest
    env:
      SLACK_WEBHOOK_URL: \${{ secrets.SLACK_WEBHOOK_URL }}
      VERIFY_RESULT: \${{ needs.verify.result }}
      DEPLOY_RESULT: \${{ needs.deploy.result }}
      DEPLOY_URL: \${{ needs.deploy.outputs.url }}
      RUN_URL: \${{ github.server_url }}/\${{ github.repository }}/actions/runs/\${{ github.run_id }}
      COMMIT_SHA: \${{ github.sha }}
    steps:
      - name: Send result to the test channel
        shell: bash
        run: |
          test -n "$SLACK_WEBHOOK_URL" || { echo 'Missing Slack webhook'; exit 1; }
          python3 - <<'PY' > message.json
          import json, os
          print(json.dumps({'text': 'Commit: '+os.environ['COMMIT_SHA']+'\\nChecks: '+os.environ['VERIFY_RESULT']+'\\nDeployment: '+os.environ['DEPLOY_RESULT']+'\\nURL: '+os.environ.get('DEPLOY_URL','')+'\\nRun: '+os.environ['RUN_URL']}))
          PY
          curl --fail-with-body --silent --show-error --max-time 20 -H 'Content-Type: application/json' --data-binary @message.json "$SLACK_WEBHOOK_URL"
`
const es=[
 ['Comprender qué vas a construir','Un repositorio es la carpeta compartida de código y su historial. Un commit identifica una versión; push la sube a GitHub. Una rama es una línea de trabajo y main es la rama elegida en este ejemplo. GitHub Actions ejecutará tres trabajos: verificar, desplegar y avisar. El despliegue convierte una versión comprobada en una web accesible. El aviso informa del resultado, pero no sustituye las pruebas de la web. Antes de copiar archivos, dibuja esas tres cajas y una flecha desde verificar hasta desplegar: esa dependencia será needs: verify.','Puedes explicar por qué un fallo de las pruebas debe impedir publicar.'],
 ['Preparar un proyecto de ensayo','Utiliza un repositorio propio de ensayo con package.json, package-lock.json y scripts test y build que ya funcionen. Abre un terminal en su carpeta y ejecuta npm ci, npm test y npm run build, por separado. npm ci instala exactamente el archivo de bloqueo; test ejecuta las pruebas definidas por tu proyecto; build prepara la aplicación. Si falta alguno de esos scripts, añade una comprobación real adecuada al proyecto antes de continuar. No lo sustituyas por echo todo correcto: eso solo imprime una frase.','Los tres comandos terminan sin error y sabes qué comportamiento comprueba al menos una prueba.'],
 ['Preparar Vercel y evitar un segundo camino de publicación','Crea un proyecto de ensayo en Vercel y confirma su directorio raíz y configuración de compilación. Identifica el proyecto y la organización mediante la vinculación local de Vercel; los identificadores aparecen en .vercel/project.json. Esta carpeta no se sube al repositorio. Si ya existe una integración Git que despliega automáticamente cada push, desconéctala en este proyecto de ensayo antes de depender de Actions. De lo contrario, Vercel podría publicar por su cuenta aunque falle la prueba de Actions.','Solo el trabajo deploy de este ejercicio inicia la publicación del proyecto de ensayo.'],
 ['Guardar las credenciales en su lugar','En GitHub abre Settings, Environments y crea production. Guarda allí VERCEL_ORG_ID, VERCEL_PROJECT_ID y VERCEL_TOKEN como secretos del entorno. El token se crea en tu cuenta Vercel y debe permitir acceso al proyecto elegido. Crea un webhook entrante de Slack para un canal de ensayo y guarda SLACK_WEBHOOK_URL en Settings, Secrets and variables, Actions, como secreto del repositorio: notify no utiliza el entorno production. Los nombres deben coincidir exactamente con el YAML. No pegues los valores en prompts, capturas ni archivos versionados.','Los cuatro nombres existen en el ámbito indicado y el repositorio no contiene sus valores.'],
 ['Crear y leer el archivo de automatización','Crea .github/workflows/deploy.yml en la raíz del repositorio y copia el archivo completo de materiales. Respeta los espacios: YAML usa la indentación para expresar la estructura. verify se ejecuta primero. deploy depende de verify y selecciona production; descarga la configuración, construye la salida de Vercel y publica esa salida. notify espera a ambos y usa always() para informar también de un fallo. Las variables del evento pasan por env; el JSON del aviso se serializa, evitando romperlo con comillas o saltos de línea.','GitHub reconoce el archivo en Actions y muestra tres trabajos conectados.'],
 ['Realizar el primer ensayo y conservar evidencia','Guarda un cambio inocuo en main del repositorio de ensayo y súbelo. Abre Actions, selecciona la ejecución correspondiente al commit y observa cada trabajo. Al terminar, abre la URL del despliegue y comprueba una función visible. Busca en Slack el mismo commit y enlace a la ejecución. Un aviso fallido puede coexistir con un despliegue correcto: lee los estados por separado. No vuelvas a desplegar solo para corregir el webhook del aviso.','verify y deploy están correctos, la web responde y el aviso identifica esa misma versión.'],
 ['Demostrar el bloqueo y adaptar a tu proyecto','En el repositorio de ensayo cambia una expectativa para que una prueba falle de verdad. Sube ese commit: verify debe fallar, deploy debe quedar omitido y notify debe comunicar ambos estados. Comprueba que la web sigue mostrando la versión anterior. Restaura la prueba y repite. Para tu proyecto cambia la rama, versión de Node, comandos, raíz y credenciales según sus requisitos; conserva needs. Registra quién revisa fallos y cómo recuperar una versión anterior. No hagas el ensayo de fallo en una rama de producción utilizada por clientes.','Conservas una ejecución correcta y otra fallida; la segunda no genera una nueva publicación.'],
]
const en=[
 ['Understand the system you are building','A repository is the shared code folder and its history. A commit identifies a version; push uploads it to GitHub. A branch is a line of work, and main is the branch chosen in this example. GitHub Actions will run three jobs: verify, deploy and notify. Deployment makes a checked version available as a website. The notification reports the outcome but does not replace checking the website. Draw the three boxes before copying files. The arrow from verify to deploy becomes the needs: verify dependency.','You can explain why a failing test must prevent publication.'],
 ['Prepare a practice project','Use your own disposable repository with package.json, package-lock.json and working test and build scripts. Open a terminal in its folder and run npm ci, npm test and npm run build separately. npm ci installs the locked dependencies; test runs the checks defined by your project; build prepares the application. If a script is missing, add a meaningful check appropriate to the project before proceeding. Do not replace it with an echo command that merely prints a success message.','All three commands finish successfully and you can explain one behavior checked by a test.'],
 ['Prepare Vercel and eliminate a second deployment path','Create a practice project in Vercel and check its root directory and build configuration. Link the local folder to identify the project and organization; their identifiers appear in .vercel/project.json. Do not commit this folder. If an existing Git integration automatically deploys every push, disconnect it for this practice project before relying on Actions. Otherwise Vercel could publish independently even when the Actions test fails, defeating the dependency you are trying to learn.','Only the deploy job in this exercise starts publication of the practice project.'],
 ['Store credentials in the correct scope','In GitHub open Settings, Environments and create production. Store VERCEL_ORG_ID, VERCEL_PROJECT_ID and VERCEL_TOKEN there as environment secrets. Create the token in your Vercel account with access to the selected project. Create a Slack incoming webhook for a test channel. Store SLACK_WEBHOOK_URL under Settings, Secrets and variables, Actions as a repository secret: notify does not use the production environment. Names must match the YAML exactly. Never paste secret values into prompts, screenshots or committed files.','The four names exist in the required scopes and their values are absent from the repository.'],
 ['Create and read the workflow file','Create .github/workflows/deploy.yml at the repository root and copy the complete material below. Preserve spaces: YAML indentation expresses structure. verify runs first. deploy depends on verify and selects production; it retrieves configuration, builds Vercel output and publishes that output. notify waits for both jobs and uses always() to report failure as well as success. Event values are passed through environment variables; the notification JSON is serialized so quotes and line breaks cannot corrupt it.','GitHub recognizes the file under Actions and displays three connected jobs.'],
 ['Run the first test and preserve evidence','Commit a harmless change to main in the practice repository and push it. Open Actions, select the run for that commit and inspect each job. After completion open the deployment URL and check a visible function. Find the same commit and run link in Slack. A failed notification can coexist with a successful deployment: read the statuses separately. Do not redeploy merely to fix the notification webhook. Record the run, deployment and observed behavior together.','verify and deploy succeed, the website responds and the notification identifies that version.'],
 ['Prove the gate and adapt it to your project','In the practice repository change an expectation so a real test fails. Push that commit: verify must fail, deploy must be skipped and notify must report both states. Confirm the website still shows the previous version. Restore the test and repeat. For your own project adapt the branch, Node version, commands, root and credentials to its requirements while preserving needs. Document who investigates failures and how to recover a known version. Never conduct this failure exercise on a production branch serving customers.','You retain one successful and one failed run; the failed run does not create another deployment.'],
]
export function githubDeploymentManual(english=false){
 const t=(a,b)=>english?b:a,steps=english?en:es
 return {language:english?'en':'es',title:t('Publicar con pruebas previas y un aviso verificable','Deploy with a test gate and a verifiable notification'),outcome:t('Construir y comprobar un circuito GitHub Actions → Vercel → Slack en un proyecto de ensayo.','Build and verify a GitHub Actions → Vercel → Slack pipeline in a practice project.'),context:[t('Ejemplo resuelto: una web de solicitudes de Aula Norte. Un cambio válido se publica; una prueba fallida conserva la versión anterior. Después adaptarás la misma dependencia a tu proyecto.','Worked example: an Aula Norte request website. A valid change is published; a failing test preserves the previous version. You will then adapt the same dependency to your own project.')],prerequisites:[{name:t('Antes de comenzar','Before starting'),instruction:t('Completa las unidades de carpeta, terminal y GitHub del Programa si todavía no sabes abrir un repositorio. Necesitas permiso para editar el repositorio de ensayo, un proyecto Vercel y un canal Slack de ensayo.','Complete the folder, terminal and GitHub Programme units if you cannot yet open a repository. You need permission to edit the practice repository, a Vercel project and a Slack test channel.'),check:t('Puedes localizar package.json y distinguir la carpeta de proyecto del navegador.','You can locate package.json and distinguish the project folder from the browser.')}],inputs:[{field:'branch',example:'main',rule:t('Nombre exacto de la rama del ejercicio.','Exact practice branch name.')},{field:'commit',example:'github.sha',rule:t('Identifica la versión comprobada y publicada.','Identifies the version checked and deployed.')},{field:'secrets',example:'VERCEL_TOKEN / SLACK_WEBHOOK_URL',rule:t('Se configuran en GitHub; nunca forman parte del YAML literal.','Configured in GitHub; never written as literal values in YAML.')}],steps:steps.map(([title,instruction,expected])=>({title,instruction,expected,why:t('La comprobación al final de este paso evita continuar sobre una configuración que todavía no has verificado.','The check at the end prevents building on configuration you have not yet verified.')})),prompts:[{title:t('Adaptar el archivo a tu repositorio','Adapt the file to your repository'),where:t('En tu asistente de programación, con package.json y el workflow como contexto.','In your coding assistant, with package.json and the workflow as context.'),text:t('Revisa este proyecto y adapta el workflow de despliegue: [ARCHIVOS SIN SECRETOS]. Mi rama de publicación es [RAMA], mi versión de Node es [VERSION] y mi directorio raíz es [DIRECTORIO]. Explica qué comprueban los scripts existentes antes de cambiarlos. Conserva la dependencia entre verificar y desplegar. Identifica si hay otra integración que publique sin esperar a esas pruebas. Entrega el YAML completo, enumera los nombres de secretos y su ámbito sin pedir valores, y explica una prueba positiva y una negativa. Distingue deploy fallido, deploy omitido y aviso fallido. No afirmes haber publicado nada si solo has editado el archivo.','Review this project and adapt its deployment workflow: [FILES WITHOUT SECRETS]. My release branch is [BRANCH], Node version is [VERSION] and root directory is [DIRECTORY]. Explain what the existing scripts check before changing them. Preserve the dependency between verification and deployment. Identify another integration that might publish without waiting for these tests. Provide the complete YAML, list secret names and scopes without requesting values, and explain a positive and negative test. Distinguish a failed deployment, a skipped deployment and a failed notification. Do not claim to have published anything if you have only edited the file.'),replace:t('Sustituye rama, versión, directorio y archivos; conserva fuera las credenciales.','Replace branch, version, directory and files; keep credentials out.'),expected:t('Un archivo coherente con tus scripts y dos pruebas cuyo resultado puedas observar.','A file matching your scripts and two tests with observable outcomes.'),followUp:t('Señala el primer paso que falló en este registro anonimizado [REGISTRO] y propón una corrección concreta conservando el bloqueo del despliegue.','Identify the first failing step in this redacted log [LOG] and propose a concrete fix while preserving the deployment gate.')}],files:[{name:'.github/workflows/deploy.yml',purpose:t('Archivo completo para el proyecto de ensayo. Léelo antes de copiarlo.','Complete practice workflow. Read it before copying.'),content:deploymentWorkflow}],tests:[{name:t('Cambio válido','Valid change'),input:t('Cambio de texto que mantiene las pruebas correctas.','A text change that preserves passing tests.'),expected:'verify: success; deploy: success; notify: success',inspect:'GitHub Actions / Vercel / Slack'},{name:t('Prueba fallida','Failing test'),input:t('Expectativa incorrecta en una prueba del repositorio de ensayo.','An incorrect expectation in a practice-repository test.'),expected:'verify: failure; deploy: skipped; notify: success',inspect:t('La URL pública mantiene la versión anterior.','The public URL retains the previous version.')}],troubleshooting:[{symptom:t('Se publica aunque la prueba falla','Publication happens despite a failing test'),cause:t('Falta needs o existe otro camino de despliegue automático.','Missing needs or a separate automatic deployment path.'),fix:t('Revisa la ejecución que creó esa publicación y desactiva el camino paralelo en el proyecto de ensayo.','Identify the run that created the deployment and disable the parallel path in the practice project.')},{symptom:t('El aviso falla, pero la web cambió','Notification fails but the website changed'),cause:t('notify tiene una credencial o un destino incorrectos; no significa que deploy haya fallado.','notify has an incorrect credential or destination; this does not mean deploy failed.'),fix:t('Comprueba el secreto del repositorio y el canal; revisa el trabajo de aviso sin repetir el despliegue.','Check the repository secret and channel; investigate the notification job without repeating deployment.')}],production:[t('Adapta una sola diferencia cada vez y repite las dos pruebas en el proyecto de ensayo.','Adapt one difference at a time and repeat both tests in the practice project.'),t('Conserva la versión anterior y documenta la recuperación antes de utilizar un dominio de clientes.','Keep the previous version and document recovery before using a customer-facing domain.')],sources:[{title:'Vercel: GitHub Actions',url:'https://vercel.com/kb/guide/how-can-i-use-github-actions-with-vercel'},{title:'GitHub Actions: workflow syntax',url:'https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-syntax'}]}
}
