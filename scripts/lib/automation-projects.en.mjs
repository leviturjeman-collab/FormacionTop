const rows=[
 ['classify','Classify requests and preserve the decision','Map id, text and email from the Webhook body. Apply the visible rules, retain the original text and route unmatched categories to review.','Use unmatched text: category must be otros and review true.'],
 ['priority-alert','Notify only for an urgent incident','Use IF with Boolean notify equal to true. Connect only that branch to Slack. Select a test channel by id, map the calculated text and retain the returned message id.','Set priority to baja: notify is false and text is null.'],
 ['daily-summary','Prepare a daily summary with references','Set the workflow timezone before the schedule. Query a start-inclusive, end-exclusive interval. Aggregate rows into items and explicitly supply items:[] when no rows exist.','Use items:[]: total and pending are 0 and text is Sin pendientes.'],
 ['deduplicate','Recognize repeated records with stable identity','The lab removes repeated ids within one batch. Use the persistent recipe/event_id key for independent events. A Set within Code is not shared across executions.','Add another record with id R2: accepted remains 2 and duplicates increases.'],
 ['approval','Check approval against the exact version','Store item id, version, reviewer and decision. Compare the approved version with current content immediately before the external action. A changed version needs another approval.','Change the current version while retaining approvedVersion: the action must be blocked.'],
 ['retry','Plan bounded retries for transient failures','Route eligible retry decisions into Wait and then the request step. Retain attempt count and cap the loop. Inspect Retry-After when supported. Reconcile uncertain external writes before retrying.','Use a permanent 400 response: retry must be false.'],
 ['document-record','Turn known document text into a record','Obtain text from the document connector before extraction. This lab is not OCR. Preserve the document id and original text; missing or unrecognized labels require review.','Remove a labeled value and inspect the missing-field result instead of inventing it.'],
 ['follow-up','Calculate a follow-up deadline','Map an unambiguous source timestamp. Store dueAt with the source id. Add a scheduled query or wait mechanism to act when due: calculating a date does not itself schedule a reminder.','Change the source timestamp and verify dueAt shifts by the same amount.'],
 ['sync','Update without overwriting a newer version','Read the destination id and version. Apply a conditional update using the expected version and inspect affected rows. A separate read and unconditional write do not prevent concurrent overwrites.','Change the destination version and verify that the update reports a conflict.'],
 ['audit','Record duration and outcome','Record start and finish around the actual operation. Store id, observed status and duration without unnecessary source text or credentials. Document retention and how to find the original run.','Use a finish time before the start and review the invalid duration.'],
 ['validation','Reject invalid input before writing','Map the contract before calculation. Route accepted and rejected results separately. Return field errors for rejection and keep the destination write only on the accepted branch.','Use an invalid email or quantity and confirm that the input is rejected.'],
 ['usage','Report usage and missing observations','Collect entries for a documented period. Keep the currency convention explicit and sum integer minor units where the contract uses cents. Count missing observations instead of treating them as measured zero.','Remove a cost value and verify that the missing count increases.'],
 ['draft','Prepare a response draft for review','Use source facts and preserve recipient and thread identifiers. Create a draft, not a sent message. Reopen it and compare fields. Do not invent availability or confirm a booking.','Use a request with missing information and check that the draft requests clarification.'],
 ['attachments','Track attachments before archiving','Retrieve binary content with its stable attachment id. Sanitize the filename, upload to a test folder and store the returned file id. Inspect the file itself, not only a successful API status.','Use path separators in a filename and inspect the sanitized result.'],
 ['invoice','Check invoice totals and exceptions','Preserve document id and currency. Compare calculated integer line amounts with the declared total. Route discrepancies to review before exporting; this exercise does not authorize accounting approval.','Change the declared total and confirm the discrepancy is flagged.'],
 ['crm','Update only intended CRM fields','Find the destination contact by a stable id. Whitelist mutable fields and omit empty values according to the contract. Inspect the patch before applying it and retain both source and destination ids.','Supply an empty field and ensure it does not erase an existing value.'],
 ['ticket','Create a ticket with an explicit priority','Normalize incident fields, calculate priority and create a ticket in a test queue. Store its returned id with the source event. Missing information requires review rather than an invented severity.','Change the severity-driving input and inspect the priority transition.'],
 ['escalate','Escalate an overdue case once','Query open cases with timestamps. Compare the threshold consistently and persist an escalation marker. Reconcile an uncertain notification before repeating it.','Set the escalation marker to true and ensure another alert is not proposed.'],
 ['publish','Publish only the approved version','Load current content and approval. Compare version and status before publishing, then retain the publication id. Query the destination after an uncertain response rather than resubmitting blindly.','Change the version after approval and verify publication is blocked.'],
 ['variants','Compare variants against a brief','Evaluate candidates against explicit constraints and retain rejection reasons. Keep selection separate from publishing. Unsupported claims should be rejected rather than rewarded for persuasive wording.','Add a variant with an unsupported claim and ensure it is not selected.'],
 ['monitor','Open an incident after repeated failures','Run scheduled requests with timeouts and persistent consecutive-failure state. Apply the threshold before opening an incident, and record recovery separately from an ongoing outage.','Provide a successful check after an incident and inspect the recovery decision.'],
 ['normalize','Normalize rows without hiding errors','Read all rows, normalize permitted fields and retain source ids. Separate accepted records from row errors. Compare counts before writing and retain the original source for diagnosis.','Add an invalid or duplicate row and inspect accepted and error counts.'],
 ['onboarding','Choose onboarding by a defined role','Map the role to a known template. Unknown roles require review. Draft messages or tasks before provisioning access; actual access changes need authorization in the destination service.','Use role unknown: template is null and review is true.'],
 ['meeting','Prepare a meeting from linked tasks','Query the meeting interval with timezone information. Use a persisted project_id to retrieve its open tasks. Store the brief with the event and retain task ids, avoiding loose client-name matching.','Mark all tasks done: openTasks is 0 and brief is Sin tareas pendientes.'],
 ['day-close','Close the day with owners and blockers','Read all pages of the team task source. Preserve owners and states, calculate pending and blocked counts, and store the period and source with the report.','Use tasks:[]: pending and blocked are 0 and lines is empty.'],
]
export const englishScenarios=new Map(rows.map(([id,name,detail,variation])=>[id,{name,detail,variation}]))
export function englishAutomationManual(s,code,schema,query){
 const e=englishScenarios.get(s.id);if(!e)throw new Error('Missing English scenario '+s.id)
 const sample=JSON.stringify(s.sample,null,2),expected=JSON.stringify(s.expected,null,2)
 return {language:'en',title:e.name,outcome:e.detail,context:['Start with the worked example, then build a connected copy. The downloadable workflow has a manual trigger, fixed sample, transformation and assertion. It performs no external account action. Keep the original unchanged so later edits can be compared with it.','Technical field names and category values stay identical across languages. Inspect the input and expected result before replacing them with your own project data.'],prerequisites:[{name:'Practice n8n instance',instruction:'Create a workflow in an instance you can access and record its version. Import the JSON through the workflow menu and inspect the four nodes before running.',check:'The sample, transformation and assertion are visible; the lab needs no external credentials.'},{name:'Connected implementation',instruction:'Prepare test credentials for the chosen source and destination. For persistence, run the supplied schema in a practice PostgreSQL database and configure a connection limited to that schema.',check:'A minimal test operation reaches the intended resource.'}],inputs:Object.entries(s.sample).map(([field,value])=>({field,example:JSON.stringify(value),rule:s.required.includes(field)?'Required by the contract; preserve the displayed type.':'Preserve meaning and type when adapting the sample.'})),steps:[
 {title:'Import and inspect the worked example',instruction:'Download and import the workflow. Open every Code node and inspect its parameters. Run manually and open the final assertion output. Its comparison must evaluate actual values; a fixed success message would not test the transformation.',expected:'passed is true and the asserted fields match: '+expected,why:'A repeatable sample separates logic from external setup.'},
 {title:'Read the transformation with its input',instruction:'Select Run Once for All Items. The function reads the first contract object, which may itself contain arrays. Follow each required field into its decision. Do not assume multiple input items are processed independently by this example.',configuration:code,expected,why:'Understanding the item structure prevents silently processing the wrong scope.'},
 {title:'Test a relevant variation',instruction:e.variation+' Keep the original lab. Write the changed expectation before running the copy; do not merely replace the assertion with whatever output happens to appear.',expected:e.variation,why:'A second case tests the rule rather than just the presence of output.'},
 {title:'Configure the real source and operation',instruction:e.detail,expected:'The step before calculation produces the same field contract as the sample from a test source.',why:'Different triggers deliver different names, nesting and types.'},
 {title:'Normalize and retain the event',instruction:'Add Edit Fields before calculation and name it Entrada normalizada. This technical name must stay unchanged because the query references it. For Webhook map fields from body, such as {{$json.body.id}}. Map every required field and inspect its resolved type. Remove the fixed assertion from the connected copy; it is only valid for its known sample.',configuration:'id = {{ $json.body.id }}\nMap the remaining sample keys from their corresponding body fields.',expected:'Different events produce distinct normalized objects with the correct types.',why:'Preserving the source relates a later service response to its original event.'},
 {title:'Persist identity and distinguish conflicts',instruction:'Run the schema, then add Postgres Execute Query after calculation. Copy the query and parameter expression below. Route created, duplicate and conflict separately. A conflict must not trigger the final action. Reusing an id with changed content is not an identical retry.',configuration:query+`\n\nQuery Parameters:\n{{ ["${s.id}", $("Entrada normalizada").first().json.id, JSON.stringify($("Entrada normalizada").first().json), JSON.stringify($json)] }}`,expected:'First input: created. Same input: duplicate. Changed content under the same id: conflict. One stored row remains.',why:'A database constraint protects concurrent writes; adding a key field alone does not.'},
 {title:'Confirm the destination and respond',instruction:e.detail+' Retain the actual destination id and status. For Webhook select Respond using Respond to Webhook Node and define success, validation-error and conflict responses. Use Test URL while listening, and Production URL after publishing the checked flow. Track pending, sent or unknown when an external effect needs reconciliation.',expected:'The response describes an observed outcome and the destination object can be found.',why:'A local transaction does not include an external API; an uncertain send needs inspection before retrying.'}],prompts:[{title:'Adapt the lab to your project',where:'In a text or coding assistant; apply the resulting settings in n8n.',text:`Help me adapt this exercise: ${e.name}.\nSOURCE\n${sample}\nREFERENCE\n${expected}\nOPERATION\n${e.detail}\nMy source is [SOURCE], destination is [DESTINATION] and version is [VERSION]. Preserve the contract and explain how actual source fields map into it. Provide exact node names, settings, expressions and expected intermediate outputs. Define unfamiliar terms. Separate local calculations from connected actions and never invent credentials or claim an unobserved execution. Include the normal case, this variation: ${e.variation}, an invalid input and an identical retry. Explain changed content with the same id. If an external write times out, show how to inspect the destination before repeating it. Return a configuration and test table that a beginner can use without this conversation.`,replace:'Replace SOURCE, DESTINATION and VERSION. Keep secrets out and start with the fictional sample.',expected:'Concrete field mapping, settings and separate expected and observed results.',followUp:'Identify the first mismatching node, its input, exact setting and expected output. Propose the smallest correction.'}],files:[{name:s.id+'.n8n.json',purpose:'Four-node local logic lab; technical node names are shared across languages.',url:'/project-assets/automations/'+s.id+'.n8n.json'},{name:'sample.json',purpose:'Original input for repeatable comparison.',content:sample},{name:'schema.sql',purpose:'Practice schema and atomic identity decision. It does not guarantee exactly-once external delivery.',content:schema}],tests:[{name:'Reference',input:sample,expected,inspect:'Final assertion and actual result.'},{name:'Variation',input:e.variation,expected:e.variation,inspect:'Copied lab with a stated new expectation.'},{name:'Repeated and changed input',input:'Send identical content twice, then changed content under the same id.',expected:'created, duplicate, conflict; one stored row.',inspect:'Database response and row count.'},{name:'Uncertain external effect',input:'Investigate a lost response after a possible practice write.',expected:'Inspect the destination before deciding to retry.',inspect:'Source id, destination id and reconciliation record.'}],troubleshooting:[{symptom:'Output differs from the reference',cause:'A field, type or decision differs from the contract.',fix:'Compare the first differing input and output; repeat the original after correction.'},{symptom:'The same event creates another record',cause:'Identity is not persisted or duplicate state is ignored.',fix:'Inspect the unique key and state routing; a local Set is not a cross-run guarantee.'},{symptom:'A destination action is unconfirmed',cause:'Credentials, permissions, mapping or a lost response need inspection.',fix:'Find the actual destination object before repeating an effect.'}],production:[e.detail,'Replace sample data gradually and retain regression tests.','Document credential names, resource identifiers, version and recovery without secret values.','Measure small runs before increasing volume and assign an owner for unresolved results.'],sources:[{title:'n8n Webhook',url:'https://docs.n8n.io/integrations/builtin/core-nodes/n8n-nodes-base.webhook/'},{title:'n8n Postgres',url:'https://docs.n8n.io/integrations/builtin/app-nodes/n8n-nodes-base.postgres/'}]}
}
