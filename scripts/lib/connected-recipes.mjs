// Each case defines a real trigger, an explicit mapping and an observable destination.
const cases={}
const add=(tool,rows)=>rows.forEach((row,index)=>cases[tool+':'+index]=row)
// title EN, normalized sample, trigger ES/EN, processing ES/EN, destination ES/EN, evidence ES/EN
add('sheets',[
 ['New sheet row → create a record and notify',{id:'SOL-001',name:'Ana',email:'ana@example.com',status:'pending'},'Google Sheets Trigger: selecciona el documento y pestaña de ensayo, evento de fila añadida y frecuencia disponible. La primera fila contiene id, name, email y status.','Google Sheets Trigger: select the practice document and sheet, row-added event and available polling interval. Row one contains id, name, email and status.','Edit Fields conserva esos cuatro campos. IF exige id y email no vacíos. Busca el id en la pestaña destino antes de crear; esta búsqueda no es una garantía atómica si hay ejecuciones concurrentes. Para ese caso utiliza una base con clave única.','Edit Fields retains those four fields. IF requires nonempty id and email. Look up the id before creating; this is not an atomic guarantee under concurrent runs. Use a database unique key when concurrency matters.','Google Sheets Append Row en una pestaña fichas: id, name, email y status vienen de la entrada. Después guarda en la fila origen processed_at y el identificador de destino; el aviso utiliza ese enlace.','Append Row in a fichas sheet: map id, name, email and status from input. Then record processed_at and destination identity on the source row; use that reference in the notification.','Una fila válida produce una ficha con SOL-001; una entrada sin correo queda rechazada y registrada para revisión.','A valid row creates one SOL-001 record; a missing email is rejected and recorded for review.'],
 ['Daily sheet summary at 09:00',{id:'DAY-001',items:[{id:'T1',status:'pending'},{id:'T2',status:'done'}]},'Schedule Trigger a las 09:00 con la zona horaria del equipo. Google Sheets Get Rows lee todas las filas de la pestaña de ensayo.','Schedule Trigger at 09:00 in the team timezone. Google Sheets Get Rows reads all rows from the practice sheet.','Filter conserva status exactamente pending, después Aggregate reúne los registros en una lista. Compara el número de registros con una consulta manual de la hoja.','Filter keeps status exactly pending, then Aggregate collects the records into one list. Compare the count with a manual inspection of the sheet.','Genera un único texto con cantidad e ids y envíalo al canal de ensayo. Define una rama explícita para cero pendientes: registrar cero sin enviar, según la regla de este ejercicio.','Build one message containing the count and ids and send it to the test channel. Define an explicit zero-pending branch: record zero without sending for this exercise.','La muestra produce un resumen con T1; sin pendientes queda un registro de cero y ningún aviso.','The sample produces one summary containing T1; no pending tasks produces a zero record and no alert.'],
 ['Archive closed sheet records after 30 days',{id:'R1',status:'closed',closed_at:'2026-01-01T10:00:00Z'},'Schedule Trigger semanal sobre una copia del documento. Lee id, status y closed_at, conservando la referencia de fila.','Weekly Schedule Trigger against a document copy. Read id, status and closed_at while retaining the row reference.','Filtra status closed y fecha anterior al límite calculado. Busca id en histórico; si existe, compara contenido. Escribe primero los que faltan y vuelve a leerlos para verificar id y campos.','Filter status closed and a date before the calculated threshold. Look up id in the archive; compare existing content. Write missing records first, then read them back to verify ids and fields.','En la práctica marca archived_at en el origen después de verificar la copia. El borrado es una ampliación independiente: selecciona por id verificado y revisa las posiciones actuales antes de eliminar.','For the exercise mark archived_at on the source after verifying the copy. Deletion is a separate extension: select verified ids and recheck current row positions before removal.','Solo los registros cerrados antiguos aparecen en histórico y quedan marcados; los recientes permanecen sin cambios.','Only old closed records appear in the archive and are marked; recent records remain unchanged.'],
 ['Classify a new sheet request with reviewed output',{id:'SOL-002',text:'Necesito ayuda para entrar',category:''},'Google Sheets Trigger sobre filas nuevas de la pestaña de solicitudes. Conserva id y text antes de llamar al modelo.','Google Sheets Trigger on new request rows. Preserve id and text before calling the model.','Solicita JSON con category entre acceso, facturacion y otros, y reason basada en el texto. Valida que el JSON se pueda leer y que category pertenezca a la lista; otros o salida inválida van a revisión.','Request JSON with category in acceso, facturacion or otros and a reason grounded in the text. Parse and validate the category; otros and invalid output go to review.','Update Row por id guarda category, reason y review. No sobrescribas text ni uses el número de fila de una ejecución antigua sin comprobarlo.','Update Row by id stores category, reason and review. Do not overwrite text or reuse an old row position without checking it.','El resultado conserva el texto original y una categoría permitida; el modelo no asigna valores fuera de la lista.','The result preserves original text and a permitted category; unsupported model values are rejected.'],
 ['Notify when a sheet threshold is crossed',{id:'STOCK-A',value:3,threshold:5,previous_below:false},'Schedule Trigger y lectura de la fila de control con id estable. value y threshold deben ser números.','Schedule Trigger and a control-row lookup by stable id. value and threshold must be numbers.','IF calcula value < threshold y compara previous_below. Avisa solo al pasar de false a true; conserva el estado observado en un registro persistente.','IF checks value < threshold against previous_below. Notify only on a false-to-true transition and persist the observed state.','Envía id, valor y umbral al canal de ensayo. Registra el resultado del aviso y una recuperación cuando el valor vuelva a ser suficiente.','Send id, value and threshold to the test channel. Record the notification result and recovery when the value becomes sufficient again.','La muestra propone un aviso; repetir el mismo estado no propone otro.','The sample proposes one alert; repeating the same state does not propose another.'],
 ['Web form → validated spreadsheet record',{id:'SOL-003',name:'Marta',email:'marta@example.com',course:'inicial'},'Webhook POST recibe un JSON de ensayo. Activa Listen for test event y copia Test URL en tu cliente HTTP.','A POST Webhook receives a practice JSON body. Enable Listen for test event and use Test URL in the HTTP client.','Edit Fields mapea body.id, body.name, body.email y body.course. Comprueba nombre no vacío, correo con formato válido y course inicial o avanzado; rechaza antes de escribir.','Edit Fields maps body.id, body.name, body.email and body.course. Validate nonempty name, email syntax and course inicial or avanzado; reject before writing.','Append Row escribe los campos en la pestaña de ensayo. Respond to Webhook devuelve el id y estado solo después de la respuesta del destino.','Append Row writes the fields to the practice sheet. Respond to Webhook returns id and state only after the destination responds.','Una petición válida aparece en la hoja; una inválida recibe error y no añade fila.','A valid request appears in the sheet; an invalid one receives an error and adds no row.'],
])
add('github',[
 null,
 ['New issue → classify, label and assign',{id:12,title:'Login fails',body:'Cannot sign in',repository:'practice'},'GitHub Trigger para issue abierta en un repositorio de ensayo. Conserva número, URL, título y cuerpo.','GitHub Trigger for an opened issue in a practice repository. Retain number, URL, title and body.','Aplica una lista permitida bug, enhancement y question. Empieza con reglas visibles o valida la categoría de un modelo. Define un responsable permitido por categoría; una posible duplicada solo se marca para revisión.','Use permitted labels bug, enhancement and question. Start with visible rules or validate model output. Define an allowed assignee per category; a possible duplicate is marked for review only.','GitHub Update Issue sobre el número original añade la etiqueta y asignación. Comprueba que ambas existen en el repositorio y registra cualquier rechazo de permisos.','GitHub Update Issue on the original number adds label and assignee. Confirm both exist in the repository and record permission errors.','La issue original recibe una etiqueta permitida; no se crea otra issue ni se cierra una supuesta duplicada.','The original issue receives a permitted label; no duplicate issue is created and suspected duplicates are not automatically closed.'],
 ['Pull request → plain-language change summary',{id:8,base:'main',head:'example-change'},'GitHub Trigger sobre apertura o actualización de pull request. Conserva número, base y SHA de la versión recibida.','GitHub Trigger on pull-request opening or update. Retain number, base and received head SHA.','Obtén el diff y resume comportamiento anterior, posterior y comprobaciones observadas. No afirmes que hay pruebas correctas si solo lees código. Relaciona cada afirmación con archivos cambiados.','Retrieve the diff and summarize previous behavior, new behavior and observed checks. Do not claim passing tests from code alone. Relate each claim to changed files.','Guarda el resumen como borrador o comentario de ensayo identificado por número y SHA. Al actualizar, edita el comentario conocido para no publicar uno nuevo por cada evento.','Save a draft or test comment identified by PR number and SHA. On update, edit the known comment instead of posting a new one for every event.','El resumen describe ese diff y señala las pruebas pendientes; conserva el enlace al PR.','The summary describes that diff, identifies pending tests and preserves the PR link.'],
 ['Dependency alert → tracked investigation',{id:'ALERT-1',package:'example-package',severity:'high'},'Usa una alerta de dependencia de ensayo o un evento disponible en el repositorio con sus permisos. Conserva id, paquete y enlace de aviso.','Use a practice dependency alert or an available repository event with appropriate permissions. Retain alert id, package and source URL.','Filtra la severidad según una regla explícita y consulta si ya existe una tarea para ese id. No deduzcas que actualizar siempre será compatible.','Filter severity by an explicit rule and check for an existing task with that alert id. Do not assume every upgrade is compatible.','Crea una tarea de investigación con paquete, versión afectada, referencia y responsable. La actualización se realiza en otra rama con pruebas; el aviso no modifica dependencias.','Create an investigation task with package, affected version, reference and owner. Upgrade in a separate tested branch; the notification itself changes no dependencies.','Un id de alerta produce una tarea identificable; los reenvíos actualizan la misma investigación.','One alert id produces a traceable task; repeated events update the same investigation.'],
 ['Published release → reviewed changelog and notice',{id:'v1.2.0',previous:'v1.1.0',draft:false},'GitHub Trigger para release publicada, excluyendo borradores según la regla del proyecto. Conserva etiqueta y URL.','GitHub Trigger for a published release, excluding drafts according to project rules. Retain tag and URL.','Consulta cambios desde la versión anterior y separa novedades, correcciones y cambios incompatibles. Revisa enlaces y evita inferir ventajas no demostradas por los cambios.','Retrieve changes since the previous release and separate features, fixes and breaking changes. Check links and avoid unsupported benefit claims.','Guarda un borrador de comunicación con la etiqueta como clave. Revisa destinatarios y contenido antes de activar un envío a clientes.','Save a communication draft keyed by release tag. Review recipients and content before enabling customer delivery.','El borrador corresponde a la etiqueta publicada y no mezcla cambios posteriores.','The draft matches the published tag and excludes later changes.'],
])
add('gmail',[
 ['Invoice email → extracted data for review',{id:'MAIL-1',attachment_id:'ATT-1',currency:'EUR'},'Gmail Trigger con una etiqueta de ensayo para facturas. Conserva message_id, thread_id y attachment_id.','Gmail Trigger with a practice invoice label. Retain message_id, thread_id and attachment_id.','Descarga el adjunto, extrae texto con un servicio configurado y valida proveedor, fecha, moneda e importes. Si el texto falta o los totales difieren, envía a revisión en lugar de completar datos.','Download the attachment, extract text with a configured service and validate supplier, date, currency and amounts. Missing text or mismatched totals require review instead of invented values.','Guarda una ficha en una tabla de ensayo con enlace al archivo y estado review. La práctica no contabiliza ni paga automáticamente.','Store a practice-table record with a source-file link and review status. The exercise does not automatically book or pay invoices.','Cada ficha puede rastrearse hasta un adjunto y los campos dudosos quedan identificados.','Every record traces back to an attachment and uncertain fields are identified.'],
 ['Incoming email → allowed labels',{id:'MAIL-2',subject:'Invoice question',text:'Please explain this charge'},'Gmail Trigger sobre mensajes nuevos de un buzón de ensayo. Conserva el identificador de mensaje.','Gmail Trigger on new messages in a practice mailbox. Preserve message identity.','Clasifica usando etiquetas permitidas y valida el resultado. Una categoría desconocida se dirige a revisión. No uses el asunto como identificador único.','Classify into permitted labels and validate the result. Unknown categories go to review. Do not use the subject as a unique identifier.','Gmail añade la etiqueta al mismo mensaje. No archiva ni elimina durante el primer ensayo; comprueba el mensaje en el buzón.','Gmail adds the label to the same message. Do not archive or delete during the first trial; inspect it in the mailbox.','El mensaje original recibe la etiqueta correcta y sigue accesible.','The original message has the correct label and remains accessible.'],
 ['Unanswered thread → 48-hour follow-up',{id:'THREAD-3',last_outbound:'2026-09-01T10:00:00Z',replied:false},'Schedule Trigger consulta una lista de conversaciones pendientes de ensayo. Guarda el último envío saliente y thread_id.','Schedule Trigger queries a practice pending-thread list. Retain last outbound time and thread_id.','Calcula 48 horas desde el último envío y consulta mensajes posteriores del mismo hilo. Una respuesta cancela el recordatorio; compara instantes y no texto de fecha local.','Calculate 48 hours from the last outbound message and inspect later messages in the same thread. A reply cancels the reminder; compare instants rather than formatted date text.','Crea una tarea o borrador de seguimiento, con clave de hilo y envío original. Marca el recordatorio para no crearlo en cada consulta.','Create a follow-up task or draft keyed by thread and original send. Persist the reminder marker so each poll does not create another.','Un hilo vencido sin respuesta produce una tarea; uno respondido no.','An overdue unanswered thread produces a task; a replied thread does not.'],
 ['Important attachment → verified Drive archive',{id:'MAIL-4',attachment_id:'ATT-4',filename:'request.pdf'},'Gmail Trigger filtra una etiqueta de ensayo y enumera los adjuntos del mensaje recibido.','Gmail Trigger filters a practice label and enumerates attachments on the received message.','Conserva attachment_id y descarga su contenido binario. Define tipos y tamaños admitidos; rechaza lo que no corresponda al proceso.','Retain attachment_id and download binary content. Define accepted types and sizes; reject files outside the process contract.','Google Drive Upload usa una carpeta de ensayo por id y un nombre saneado. Guarda file_id y abre el archivo; no borres el origen tras una mera respuesta HTTP.','Google Drive Upload uses a test folder id and sanitized filename. Store file_id and open the file; do not delete the source based only on an HTTP response.','El archivo archivado tiene contenido comprobable y referencia al adjunto original.','The archived file has inspected content and a reference to the original attachment.'],
])
export const connectedCases=cases
export function connectedManual(tool,automation,index,en=false){
 const row=cases[tool.id+':'+index];if(!row)return null
 const [englishTitle,sample,triggerEs,triggerEn,processEs,processEn,destinationEs,destinationEn,resultEs,resultEn]=row
 const t=(es,english)=>en?english:es,trigger=t(triggerEs,triggerEn),process=t(processEs,processEn),destination=t(destinationEs,destinationEn),result=t(resultEs,resultEn),title=en?englishTitle:automation.name
 const input=JSON.stringify(sample,null,2)
 return {language:en?'en':'es',title,outcome:result,context:[t('Este ejemplo se construye primero en recursos de ensayo. El disparador inicia el proceso, la transformación comprueba la información y el destino conserva una salida que puedes abrir. Después sustituirás los datos por los de tu proyecto, manteniendo las comprobaciones.','Build this example in practice resources first. The trigger starts the process, the transformation checks information and the destination retains an output you can open. Then replace the sample with your project data while preserving the checks.'),t('Los nombres de campo de la muestra forman un contrato de trabajo: describen qué información debe existir antes del cálculo. Tu aplicación puede entregar otros nombres; el paso de mapeo los transforma en este contrato. No escribas los valores de Ana o SOL-001 como constantes para todos los eventos.','Sample field names form a working contract: they describe what must exist before calculation. Your application may return different names; mapping converts them into this contract. Do not type Ana or SOL-001 as constants for every event.')],prerequisites:[{name:t('Espacio de ensayo','Practice workspace'),instruction:trigger,check:t('Puedes abrir el recurso elegido y localizar una muestra sin tocar datos de clientes.','You can open the selected resource and locate a sample without changing customer data.')},{name:t('Conexiones y permisos','Connections and permissions'),instruction:t('Crea en n8n las credenciales del origen y del destino. Selecciona un recurso concreto de ensayo y prueba una lectura. Si una acción requiere permiso de escritura, compruébalo con un registro ficticio y conserva su identificador.','Create source and destination credentials in n8n. Select a specific practice resource and test a read. If an action needs write permission, verify it with a fictional record and retain its id.'),check:t('La credencial funciona sobre el recurso correcto; no basta con que la conexión figure como creada.','The credential works on the intended resource; a saved connection name alone is insufficient.')}],inputs:Object.entries(sample).map(([field,value])=>({field,example:JSON.stringify(value),rule:t('Conserva el tipo mostrado y mapea el valor desde el evento real.','Preserve the displayed type and map the value from the actual event.')})),steps:[{title:t('Crear el disparador y capturar una muestra','Create the trigger and capture a sample'),instruction:trigger+' '+t('Ejecuta el paso de ensayo y abre su salida. Identifica el evento concreto por id y fecha; no continúes si la muestra pertenece a otro documento, buzón o repositorio.','Run the test step and open its output. Identify the event by id and time; do not continue if the sample belongs to another document, inbox or repository.'),expected:t('Una salida real del origen con los datos que usarás en los pasos siguientes.','An actual source output containing the data needed next.'),why:t('Probar el origen primero evita construir expresiones sobre campos que no llegan.','Testing the source first avoids expressions referencing absent fields.')},{title:t('Mapear y validar los campos','Map and validate fields'),instruction:t('Añade Edit Fields entre el origen y la operación. Crea las claves de la tabla de entrada y selecciona cada valor desde la salida capturada. Revisa el valor resuelto y su tipo. Una fecha debe conservar su zona y un número no debe convertirse en un texto decorado. Si falta un campo necesario, dirige el evento a revisión antes de escribir.','Add Edit Fields between the source and operation. Create the input-table keys and select each value from the captured output. Inspect the resolved value and type. Preserve a timestamp’s timezone and keep numbers numeric. Route a missing required value to review before writing.'),configuration:input,expected:t('El objeto normalizado coincide con el contrato, usando valores del evento y no una copia fija de la muestra.','The normalized object matches the contract using event values, not a fixed sample copy.'),why:t('El contrato permite comparar eventos de fuentes distintas con la misma regla.','A contract allows different source events to be checked with the same rule.')},{title:t('Aplicar la decisión del proceso','Apply the process decision'),instruction:process,expected:result,why:t('Esta es la regla que da utilidad al flujo; comprueba también el caso que no debe continuar.','This rule makes the flow useful; also test the case that must not proceed.')},{title:t('Configurar el destino y comprobar la salida','Configure the destination and inspect its output'),instruction:destination+' '+t('Abre el registro, mensaje o archivo en su aplicación. Compara id y campos con el origen. Guarda la respuesta del servicio para relacionar ambas partes sin depender de una captura aislada.','Open the record, message or file in its application. Compare id and fields with the source. Retain the service response to connect both ends without relying on an isolated screenshot.'),expected:result,why:t('Un nodo correcto no demuestra por sí solo que el recurso final tenga el contenido correcto.','A successful node alone does not prove the final resource contains the intended content.')},{title:t('Probar ausencia, repetición y fallo','Test absence, repetition and failure'),instruction:t('Conserva el caso original. En una copia retira un campo requerido y verifica que no se realiza la acción final. Repite el mismo id y observa si el destino reconoce la entidad existente. Si una petición queda sin respuesta después de una posible escritura, consulta el destino antes de repetir. Una búsqueda seguida de creación necesita protección adicional si puede haber ejecuciones simultáneas.','Keep the original case. Remove a required field in a copy and verify no final action occurs. Repeat the id and inspect whether the destination recognizes the existing entity. After a lost response following a possible write, inspect the destination before retrying. A lookup followed by creation needs additional protection under concurrent execution.'),expected:t('Cada prueba conserva entrada, esperado y observado; los estados inciertos quedan identificados.','Each test retains input, expectation and observation; uncertain outcomes are identified.'),why:t('El camino correcto es solo una parte del comportamiento que necesitas para reutilizar el proceso.','The successful path is only part of the behavior needed for reuse.')},{title:t('Adaptarlo y entregar el proyecto','Adapt and hand off the project'),instruction:t('Sustituye documento, canal o repositorio por un recurso de tu proyecto y repite primero con una sola muestra permitida. Registra las claves de campos, credenciales por nombre, reglas, destino y versión del flujo. Exporta el workflow sin secretos y explica cómo reconfigurar las conexiones al importarlo. Define quién revisa errores y cómo detener nuevas ejecuciones mientras se investiga.','Replace the document, channel or repository with a project resource and repeat using one permitted sample. Record field names, credential names, rules, destination and workflow version. Export without secrets and explain how to reconnect accounts after import. Name an owner for errors and explain how to pause new executions during investigation.'),expected:t('Otra persona puede repetir el ejemplo y reconocer qué debe cambiar para su propio caso.','Another person can repeat the example and identify what changes for their own case.'),why:t('Una entrega útil contiene el procedimiento y la evidencia, no solo el archivo del flujo.','A useful handoff includes procedure and evidence, not only a workflow file.')}],prompts:[{title:t('Adaptar el caso con instrucciones concretas','Adapt the case with concrete instructions'),where:t('En un asistente de texto; aplica después los parámetros en n8n.','In a text assistant; apply the returned parameters in n8n.'),text:t(`Ayúdame a construir «${title}» en n8n desde cero. Fuente de ensayo: ${input}. Disparador: ${trigger}. Regla: ${process}. Destino: ${destination}. Resultado esperado: ${result}. Mi versión es [VERSION] y mis recursos son [RECURSOS SIN SECRETOS]. Explica cada nodo, parámetro y expresión necesario. Usa la muestra y distingue valores fijos de campos del evento. No inventes permisos ni ejecuciones. Incluye un caso válido, uno incompleto y uno repetido, con la respuesta esperada y dónde comprobarla. Entrega un esquema de nodos y un registro de pruebas que pueda completar con resultados observados.`,`Help me build “${title}” in n8n from the beginning. Sample: ${input}. Trigger: ${trigger}. Rule: ${process}. Destination: ${destination}. Expected: ${result}. My version is [VERSION] and resources are [RESOURCES WITHOUT SECRETS]. Explain each required node, parameter and expression. Use the sample and distinguish constants from event fields. Do not invent permissions or executions. Include valid, incomplete and repeated cases with expected outcomes and inspection locations. Deliver a node plan and test record I can complete with actual observations.`),replace:t('Sustituye versión y recursos; conserva los secretos fuera de la conversación.','Replace version and resource descriptions; keep secrets out of the conversation.'),expected:result,followUp:t('Muestra la configuración del primer nodo cuyo resultado no coincide y explica cómo corregirlo con esta muestra.','Show the first mismatching node configuration and explain its correction using this sample.')}],files:[{name:'sample.json',purpose:t('Datos ficticios del contrato normalizado.','Fictional normalized-contract data.'),content:input},{name:'evidence.md',purpose:t('Registro que completa el alumno después de ejecutar.','Record completed by the learner after execution.'),content:en?'# Verification\nInput:\nExpected:\nObserved:\nDestination id:\nVariation:\nFailure and recovery:\n':'# Comprobación\nEntrada:\nEsperado:\nObservado:\nId del destino:\nVariación:\nFallo y recuperación:\n'}],tests:[{name:t('Caso original','Original case'),input,expected:result,inspect:t('Origen, salida de cada paso y recurso final.','Source, each step output and final resource.')},{name:t('Campo ausente','Missing field'),input:t('Retira un campo necesario en una copia.','Remove a required field in a copy.'),expected:t('El evento queda rechazado o en revisión antes de escribir.','The event is rejected or reviewed before writing.'),inspect:t('Rama de validación y ausencia de un nuevo resultado externo.','Validation branch and absence of a new external result.')},{name:t('Repetición','Repetition'),input:t('Repite el id original conservando la misma información.','Repeat the original id with identical information.'),expected:t('Se reconoce el resultado anterior o se documenta el mecanismo persistente que falta antes de activar el proceso.','The previous result is recognized, or missing persistent protection is documented before enabling the process.'),inspect:t('Identificadores y estado del destino.','Destination identifiers and state.')}],troubleshooting:[{symptom:t('El campo aparece vacío','A field is empty'),cause:t('El mapeo no corresponde a la salida real o perdió el dato después de otro nodo.','Mapping differs from the actual output or a later node replaced the data.'),fix:t('Vuelve al primer evento y compara los campos; conserva la entrada normalizada antes de llamadas externas.','Return to the first event and compare fields; preserve normalized input before external calls.')},{symptom:t('La conexión existe, pero la acción falla','The connection exists but the action fails'),cause:t('Puede faltar permiso sobre ese recurso, un dato requerido o una operación compatible.','The resource may lack permission, a required value or a compatible operation.'),fix:t('Lee el error exacto y prueba la operación mínima sobre el recurso de ensayo.','Read the exact error and test the smallest operation on the practice resource.')}],production:[t('Activa el proceso solo después de revisar las pruebas con la fuente y destino elegidos.','Enable the process after reviewing tests against the selected source and destination.'),t('Documenta límites, frecuencia, responsable y recuperación; un aviso no equivale a una acción externa comprobada.','Document limits, frequency, owner and recovery; a notification is not proof of a checked external action.')],sources:[{title:'n8n integrations',url:'https://docs.n8n.io/integrations/'}]}
}
