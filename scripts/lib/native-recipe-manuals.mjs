import {nativeCases} from './native-recipe-cases.mjs'
const setup={
 whatsapp:['Prepara el número de prueba de WhatsApp Business Cloud en tu cuenta de Meta. Comprueba que puedes recibir un mensaje de tu propio número de prueba y encuentra su id en el evento. La API es la conexión que una aplicación utiliza para hablar con WhatsApp; una cuenta personal de WhatsApp no sustituye esta preparación.','Prepare the WhatsApp Business Cloud test number in your Meta account. Check that you can receive a message from your own test number and find its id in the event. The API is the connection an application uses to talk to WhatsApp; a personal WhatsApp account does not replace this setup.'],
 slack:['Crea o utiliza una aplicación de Slack de prueba. En sus permisos elige las acciones necesarias y autoriza la aplicación en el espacio de prueba. Invítala al canal elegido. Un canal es una conversación de equipo; su id permite reconocerlo aunque cambie de nombre. Guarda la conexión en la herramienta, nunca en el texto de un mensaje.','Create or use a test Slack application. Select required actions in its permissions and authorise it in the practice workspace. Invite it to the chosen channel. A channel is a team conversation; its id identifies it even if renamed. Save the connection in the tool, never in a message.'],
 telegram:['Prepara un bot de prueba con BotFather. Un bot es una cuenta que recibe instrucciones de tu programa. Guarda su token, la clave de acceso, en la conexión de Telegram. Usa tu propia cuenta para la primera conversación. Anota chat_id y user_id: el primero identifica la conversación y el segundo a la persona que escribe.','Prepare a test bot through BotFather. A bot is an account receiving instructions from your program. Save its token, the access key, in the Telegram connection. Use your own account for the first conversation. Record chat_id and user_id: one identifies the conversation and the other the sender.'],
 supabase:['Abre un proyecto de Supabase destinado a practicar. Una tabla guarda una lista de filas y cada fila corresponde a un caso. Crea una tabla propia para la práctica y añade datos inventados. Comprueba que tu cuenta puede leerla. Las claves con permisos de administración se utilizan en el servidor o en conexiones privadas, no en la página que recibe el alumno.','Open a Supabase project used for practice. A table stores rows, each representing one case. Create your own practice table with fictional details and check that your account can read it. Administrative keys belong on the server or in private connections, not in the page sent to a learner.'],
 notion:['Crea una base de prueba en Notion y compártela con la conexión que usarás. Una propiedad es una columna, como nombre o estado. Añade una tarjeta inventada y comprueba que la conexión puede leerla. Si aparece «no encontrado», revisa qué base has compartido antes de cambiar el resto del proceso.','Create a test Notion database and share it with your connection. A property is a column, such as name or status. Add a fictional card and check that the connection can read it. If it says “not found”, check the shared database before changing the rest of the process.'],
 airtable:['Crea una base y una tabla de prueba en Airtable. Una base reúne tablas; un registro es una fila. Añade los campos del ejemplo y una fila inventada. Comprueba el id que devuelve Airtable para esa fila. Si vas a conectar n8n, concede a la conexión acceso solo a la base de práctica y a las acciones que necesita.','Create a test Airtable base and table. A base holds tables; a record is a row. Add the example fields and a fictional row. Check the id Airtable returns for it. If connecting n8n, grant access only to the practice base and needed actions.'],
 postgres:['Abre una base de datos de prueba con una cuenta permitida. Postgres guarda información en tablas. SQL es el lenguaje de las instrucciones que leen o cambian esas tablas. Empieza con una consulta de lectura sobre tres filas inventadas y comprueba sus números. No ejecutes consultas de mantenimiento sobre la base real durante la práctica.','Open a test database with an authorised account. Postgres stores information in tables. SQL is the language for reading or changing those tables. Start with a read query over three fictional rows and check their ids. Do not run maintenance queries on the real database during practice.'],
 vercel:['Utiliza un proyecto de Vercel de prueba y localiza su despliegue actual. Un despliegue es una versión publicada de tu aplicación. Una variable del servidor guarda una configuración o clave que el servidor necesita; no debe aparecer en el código que descarga el navegador. Comprueba qué proyecto vas a cambiar antes de publicar una prueba.','Use a test Vercel project and find its current deployment, a published version of your application. A server variable holds a setting or key needed by the server; it must not appear in browser-downloaded code. Check which project you will change before publishing a test.'],
}
const docs={whatsapp:'https://developers.facebook.com/docs/whatsapp/cloud-api/',slack:'https://docs.slack.dev/',telegram:'https://core.telegram.org/bots/api',supabase:'https://supabase.com/docs/guides/database/webhooks',notion:'https://developers.notion.com/',airtable:'https://support.airtable.com/',postgres:'https://www.postgresql.org/docs/current/',vercel:'https://vercel.com/docs/cron-jobs'}
export function nativeRecipeManual(tool,a,index,en=false){
 const row=nativeCases[tool.id]?.[index];if(!row)return null
 const t=(es,english)=>en?english:es
 const [titleEN,sample,actionES,actionEN,resultES,resultEN]=row
 const title=en?titleEN:a.name, action=en?actionEN:actionES, result=en?resultEN:resultES,prepare=setup[tool.id][en?1:0]
 const data=JSON.stringify(sample,null,2)
 const native=(tool.id==='airtable'&&index===1)||(tool.id==='vercel'&&index===0)||(tool.id==='supabase'&&index===1)||(tool.id==='postgres'&&index===2)
 const place=native?tool.label:'n8n + '+tool.label
 const prompt=t(`Estoy aprendiendo a hacer «${title}» desde cero. Trabajo con ${place}.\n\nMI EJEMPLO\n${data}\n\nLO QUE DEBERÍA OCURRIR\n${result}\n\nLOS PASOS ESPECÍFICOS\n${action}\n\nMi cuenta o versión es [VERSIÓN] y ahora veo [PANTALLA]. Ayúdame con un solo paso cada vez. Antes de decirme qué configurar, explícame qué vamos a conseguir usando los datos de este ejemplo. Si aparece una palabra técnica, explica su significado con una frase corriente. No me pidas pegar un mensaje de tutoría en un campo de código.\n\nPrimero muestra el ejemplo resuelto. Después dime qué aplicación abrir, qué objeto elegir, qué valor escribir y qué respuesta debería ver. Cuando haya que elegir un dato del paso anterior, nombra ese dato y muestra su valor en la muestra. Si el evento real tiene otra forma, pídeme un ejemplo sin datos personales y ayúdame a encontrar el campo; no inventes que la API devuelve exactamente esta tabla didáctica.\n\nSi hace falta una consulta SQL, una petición HTTP o un archivo de configuración, entrega la pieza completa para ese paso y explica cada parte. Deja las contraseñas como conexiones guardadas, no dentro del texto. Indica qué paso solo lee y cuál envía o cambia información. Antes de la primera escritura, confirma que estamos en el recurso de prueba que he indicado.\n\nDespués prepara tres comprobaciones: el caso original, una repetición del mismo número y un caso al que le falte información o permiso. Para cada una escribe lo que debería ocurrir y dónde mirarlo. No digas que el mensaje llegó porque la petición fue aceptada: explica qué confirmación real puedo consultar. Si no se sabe si una acción terminó, enséñame a revisar el destino antes de repetir.\n\nPor último, ayúdame a llevarlo a [MI PROYECTO]. Pregunta quién revisa las decisiones, qué número identifica cada caso y dónde se guarda lo ocurrido. Termina con un documento breve que otra persona pueda seguir: herramientas, datos, pasos, resultado comprobado, errores y siguiente cambio. No marques como hechas acciones que todavía no he realizado.`,
 `I am learning “${title}” from zero using ${place}.\n\nMY EXAMPLE\n${data}\n\nWHAT SHOULD HAPPEN\n${result}\n\nSPECIFIC STEPS\n${action}\n\nMy account or version is [VERSION] and I currently see [SCREEN]. Help with one step at a time. Before telling me to configure something, explain what we will achieve using this example. Explain technical words in everyday language. Do not ask me to paste a tutoring message into a code field.\nFirst show the solved example. Then say which application to open, which object to choose, which value to enter and which response to expect. When selecting a previous step’s detail, name it and show its sample value. If the real event has a different shape, ask for an example without personal information and help locate the field; do not invent that the API returns exactly this teaching table.\nIf SQL, an HTTP request or a configuration file is needed, provide the complete relevant piece and explain its parts. Keep passwords in saved connections rather than text. Identify reading steps and those sending or changing information. Before the first write, check we are in the practice resource I named.\nPrepare three checks: the original case, a repeated number and a case missing information or permission. State what should happen and where to inspect it. Do not say a message arrived because a request was accepted; explain which real confirmation to inspect. If completion is uncertain, show how to check the destination before repeating.\nFinally help adapt this to [MY PROJECT]. Ask who reviews decisions, which number identifies each case and where results are saved. Finish with a document another person can follow: tools, details, steps, checked result, errors and next change. Do not mark actions done before I perform them.`)
 return {language:en?'en':'es',title,outcome:result,context:[t(`Esta guía enseña «${title}» con un caso inventado. Primero lo entenderás a mano y después lo probarás en ${place}. Un disparador es lo que inicia los pasos, como recibir un mensaje o llegar una hora. El destino es la aplicación donde quieres ver el resultado. No necesitas adivinar si funcionó: al final abrirás ese lugar y comprobarás el ejemplo.`,`This guide teaches “${title}” through a fictional case. First understand it manually and then try it in ${place}. A trigger starts the steps, such as a received message or a time of day. The destination is the application where you want the result. At the end, open that place and check the example instead of guessing whether it worked.`),t('El ejemplo de abajo reúne los datos importantes para entender la tarea. No es un evento completo para importar sin cambios. La aplicación puede enviar los mismos datos con otros nombres o dentro de otros apartados; en la práctica tendrás que localizar esos valores en su respuesta.','The example below collects the details important to understanding the task. It is not a complete event to import unchanged. The application may send those details under different names or nested sections; locate those values in its actual response.')],prerequisites:[{name:tool.label,instruction:prepare,check:t('Puedes abrir el recurso de prueba y leer un ejemplo.','You can open the practice resource and read one example.')},{name:t('Tu cuaderno de práctica','Your practice notebook'),instruction:t('Abre un documento vacío. Escribe Ejemplo, Qué debería pasar y Qué pasó al probarlo. Copia la muestra y deja el último apartado para después de ejecutar.','Open a blank document. Write Example, What should happen and What happened when I tried it. Copy the sample and leave the last section until after running.'),check:t('Sabes dónde guardar el resultado y encontrarlo mañana.','You know where to save the result and find it tomorrow.')}],inputs:Object.entries(sample).map(([field,value])=>({field,example:JSON.stringify(value),rule:t('Dato inventado del ejemplo. Localiza su equivalente en el evento real antes de usarlo.','Fictional example detail. Locate its equivalent in the real event before using it.')})),steps:[
 {title:t('Lee el caso y su respuesta','Read the case and answer'),instruction:t(`Copia el ejemplo y lee la respuesta resuelta: ${result} Señala qué dato determina esa respuesta. Los nombres TEST y YOUR indican lugares que sustituirás por los de tu cuenta de prueba; no son cuentas que ya estén conectadas.`,`Copy the example and read the worked answer: ${result} Identify which detail determines that answer. TEST and YOUR names indicate places to replace with your test account details; they are not already-connected accounts.`),configuration:data,expected:result,why:t('Primero necesitas saber reconocer el resultado correcto.','First learn to recognise the right result.')},
 {title:t('Abre las herramientas de prueba','Open the practice tools'),instruction:prepare,expected:t(`Puedes localizar el recurso que utilizará ${place} y comprobar un dato sin modificar información real.`,`You can find the resource ${place} will use and check a detail without changing real information.`),why:t('Una conexión guardada no demuestra que tenga acceso a la tabla o conversación elegida.','A saved connection does not prove access to the chosen table or conversation.')},
 {title:t('Sigue la regla de este ejemplo','Follow this example’s rule'),instruction:action,configuration:a.code||undefined,expected:result,why:t('Haz cada acción por orden. Después de una lectura, mira los datos; después de una escritura, abre el lugar donde deberían haberse guardado.','Do actions in order. After reading, inspect the details; after writing, open the place where they should have been saved.')},
 {title:t('Comprueba lo que ocurrió de verdad','Check what actually happened'),instruction:t(`Abre el registro, mensaje, tabla o despliegue que acabas de preparar. Compara con: ${result} Guarda su número o enlace en tu cuaderno y escribe qué has visto. Si el servicio solo dice que aceptó una petición, no lo conviertas en una entrega confirmada. Comprueba el estado disponible en el destino.`,`Open the record, message, table or deployment you just prepared. Compare with: ${result} Save its number or link in your notebook and describe what you saw. If a service only accepted a request, do not treat that as confirmed delivery. Inspect the destination’s available status.`),expected:t('El cuaderno muestra un resultado que puedes volver a abrir y revisar.','The notebook shows a result you can reopen and inspect.'),why:t('Esto distingue una propuesta de una acción realizada.','This distinguishes a proposal from an action performed.')},
 {title:t('Prueba una repetición y un dato que falta','Try a repeat and a missing detail'),instruction:t('Repite el mismo ejemplo en el recurso de prueba. Mira si aparece una segunda acción y revisa la marca que reconoce repeticiones. Después utiliza una copia sin un dato necesario o con una cuenta sin permiso. El proceso debe parar o pedir revisión antes de mostrar información o cambiar el destino. Anota el resultado de cada intento; no ocultes un error cambiando los datos hasta que parezca funcionar.','Repeat the example in the practice resource. Look for a second action and inspect the marker recognising repeats. Then use a copy missing a needed detail or an account without permission. The process must stop or ask for review before exposing information or changing the destination. Record each attempt; do not hide an error by changing details until it appears to work.'),expected:t('Tienes tres intentos guardados y sabes explicar cómo se evita repetir una acción o usar un dato que falta.','You have three recorded attempts and can explain how repeated actions or missing details are handled.'),why:t('Los casos reales no siempre llegan completos ni una sola vez.','Real cases do not always arrive complete or only once.')},
 {title:t('Adapta una copia a tu centro','Adapt a copy to your centre'),instruction:t('Elige una tarea pequeña parecida al ejemplo. Escribe quién la hace, qué cuenta usará, dónde guarda el resultado y quién revisa los casos dudosos. Cambia las conexiones solo en una copia. Prueba primero un caso y después tres. Deja escritas las instrucciones para detener el proceso y volver a trabajar a mano.','Choose one small task similar to the example. Write who handles it, which account it uses, where the result is saved and who checks uncertain cases. Change connections only in a copy. Try one case and then three. Write how to stop the process and continue manually.'),expected:t('Otra persona puede seguir tus notas y sabe qué revisar antes de activar el proceso.','Another person can follow your notes and knows what to check before enabling the process.'),why:t('El objetivo es que puedas repetirlo en tu proyecto, no solo leer una demostración.','The aim is to repeat it in your project, not only read a demonstration.')},
 ],prompts:[{title:t('Mensaje para que te ayuden paso a paso','Message for step-by-step help'),where:t('En un chat de texto. El código que resulte va después en el lugar que indique el paso.','In a text chat. Any resulting code goes in the place named by the step afterwards.'),text:prompt,replace:t('Completa VERSIÓN, PANTALLA y MI PROYECTO. Usa ejemplos sin información personal.','Fill VERSION, SCREEN and MY PROJECT. Use examples without personal information.'),expected:result,followUp:t('No entiendo [PASO]. Usa los datos de la muestra, explica una sola acción y dime dónde veré su resultado.','I do not understand [STEP]. Use the sample details, explain one action and say where I will see its result.')}],files:[{name:'example.json',purpose:t('Ejemplo explicado para leer y adaptar; no es un flujo importable.','Explained example to read and adapt; it is not an importable workflow.'),content:data},{name:'practice-notes.txt',purpose:t('Tu cuaderno de resultados.','Your result notebook.'),content:`${title}\n\n${t('Ejemplo','Example')}:\n${data}\n\n${t('Qué debería pasar','What should happen')}:\n${result}\n\n${t('Qué pasó al probarlo','What happened when I tried it')}:\n\n${t('Número o enlace del resultado','Result number or link')}:\n`}],tests:[{name:t('Ejemplo original','Original example'),input:data,expected:result,inspect:t('La aplicación donde se recibe el resultado y su historial.','The receiving application and its history.')},{name:t('Repetición','Repeat'),input:t('El mismo número o evento recibido otra vez.','The same number or event received again.'),expected:t('Se reconoce el caso ya tratado o se informa de la repetición antes de realizar otra acción.','Recognise the handled case or report the repeat before another action.'),inspect:t('El registro de casos y los objetos reales del destino.','The case log and actual destination objects.')},{name:t('Falta información o permiso','Missing information or permission'),input:t('Una copia sin un dato necesario o desde una cuenta no permitida.','A copy without a needed detail or from an unpermitted account.'),expected:t('El caso se detiene o pide revisión sin una acción final no autorizada.','The case stops or requests review without an unauthorised final action.'),inspect:t('El aviso o error y la ausencia de cambios en el destino.','The warning or error and absence of destination changes.')}],troubleshooting:[{symptom:t('No encuentro un campo','I cannot find a field'),cause:t('La aplicación puede agrupar los datos de otra manera.','The application may group details differently.'),fix:t('Abre el ejemplo recibido, encuentra el valor conocido y anota el camino hasta él. Usa ese campo al preparar el paso siguiente.','Open the received example, find the known value and record its path. Use that field when preparing the next step.')},{symptom:t('El proceso parece terminado pero no veo el resultado','The process looks finished but I cannot see the result'),cause:t('Puede haber cambiado de cuenta, fallado el guardado o quedado el envío pendiente.','It may use another account, have failed saving or have a pending send.'),fix:t('Comprueba cuenta, objeto de destino y respuesta del paso final. Busca el número antes de volver a ejecutar.','Check account, destination object and final-step response. Look up the number before running again.')}],production:[action,t('Antes de usar datos reales, acuerda quién puede verlos y cuánto tiempo se conservarán. Mantén las claves en las conexiones privadas de la herramienta y utiliza los permisos necesarios para esta tarea.','Before using real details, agree who may see them and how long to retain them. Keep keys in private tool connections and use permissions needed for this task.'),t('La muestra resuelve una situación concreta. Revisa límites de tu plan, datos obligatorios, repeticiones y errores antes de activar muchas ejecuciones. Una marca guardada después del envío no impide por sí sola dos envíos simultáneos: para ese caso necesitas un registro que reserve la acción de forma única y permita revisar intentos inciertos.','The sample solves one situation. Check plan limits, required details, repeats and errors before many runs. A marker saved after sending cannot alone prevent simultaneous sends; use a log that uniquely reserves the action and supports reviewing uncertain attempts.')],sources:[{title:tool.label+' · '+t('documentación oficial','official documentation'),url:docs[tool.id]}]}
}
