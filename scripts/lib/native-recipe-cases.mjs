// Fictional examples, with a different decision and visible result for every recipe.
export const nativeCases={
 whatsapp:[
 ['Incoming message → request record and receipt',{id:'MSG-01',from:'TEST_NUMBER',text:'I need information about the course'},'Recibe un mensaje de texto del número de prueba y conserva messages[0].id, from y text.body. Comprueba la firma con el secreto de la aplicación antes de aceptar una llamada externa. Guarda el mensaje por su id antes de confirmar recepción; el teléfono identifica la conversación, no cada mensaje. Busca la conversación por teléfono y añade el mensaje a ella.','Receive a text message from the test number and retain messages[0].id, from and text.body. Verify the signature using the app secret before accepting an external call. Save the message by its id before acknowledging receipt; the phone identifies the conversation, not each message. Find the conversation by phone and add its message.','Una conversación con un mensaje guardado. Repetir MSG-01 no crea otro mensaje ni otro acuse.','One conversation with one saved message. Repeating MSG-01 creates neither a second message nor another receipt.'],
 ['Send an approved template for a recorded event',{id:'NOTICE-01',to:'TEST_NUMBER',template:'APPROVED_TEMPLATE',consent:true},'Lee el permiso de contacto guardado para ese número. Si no está confirmado, detén el envío. En la acción WhatsApp Business Cloud selecciona una plantilla aprobada disponible en tu cuenta y su idioma; rellena sus variables en el orden que pide esa plantilla. Usa solo el destinatario de prueba. Guarda el id del mensaje devuelto y relaciona después sus avisos de estado con ese id.','Read the saved contact permission for that number. Stop when it is unconfirmed. In the WhatsApp Business Cloud action choose an approved template available in your account and its language; fill variables in the order that template requests. Use only the test recipient. Save the returned message id and match later status notices to it.','Se conserva el id del mensaje y su estado real. Una petición sin permiso queda pendiente y no se envía.','The message id and actual status are retained. A request without permission remains pending and is not sent.'],
 ['Send one appointment reminder',{id:'APPT-01',starts:'2026-09-07T10:00:00Z',now:'2026-09-06T10:00:00Z',reminded:false},'Configura Schedule Trigger para consultar las citas. Calcula la diferencia de horas usando las fechas completas y una zona horaria acordada. El ejemplo está a 24 horas y aún no tiene recordatorio. Busca una marca de envío por cita y tipo de recordatorio antes de preparar la plantilla. Después de un envío confirmado conserva id del mensaje y fecha. Si se pierde la respuesta, revisa el estado antes de repetir.','Use Schedule Trigger to query appointments. Calculate the hour difference from full timestamps in an agreed timezone. The example is 24 hours away with no reminder. Look for a send marker by appointment and reminder type before preparing the template. After a confirmed send retain message id and date. If the response is lost, check status before repeating.','APPT-01 genera un recordatorio. La siguiente consulta lo reconoce y no envía otro.','APPT-01 produces one reminder. The next query recognises it and does not send another.'],
 ['Voice message → checked transcript and summary',{id:'AUDIO-01',media_id:'TEST_MEDIA',expectedText:'Please call me on Tuesday'},'Filtra los mensajes cuyo tipo sea audio. Usa su media_id para pedir la dirección de descarga a la API y descarga el archivo con la conexión de la cuenta. Envía el archivo al servicio de transcripción elegido y guarda el texto completo. Pide resumir únicamente qué se solicita y qué fecha se menciona. Escucha el audio al comparar: un resumen correcto de una transcripción equivocada sigue siendo incorrecto.','Filter messages whose type is audio. Use media_id to request the download address from the API and download through the account connection. Send the file to your chosen transcription service and retain the full text. Ask for a summary containing only the request and stated date. Listen while comparing: a correct summary of an incorrect transcript is still wrong.','La nota dice que pide una llamada el martes. No añade hora ni convierte el martes en una fecha que no conocemos.','The note says a Tuesday call was requested. It adds no time and does not turn Tuesday into an unknown calendar date.'],
 ['Hand a conversation to a person',{id:'CHAT-01',mode:'bot',text:'I want to make a complaint'},'Antes de preparar una respuesta automática consulta el estado de la conversación. Si ya dice human, guarda el mensaje y avisa al responsable sin responder como bot. En el ejemplo, una reclamación cambia el estado a human. Guarda ese cambio antes de avisar. Solo una persona autorizada puede devolver el estado a bot al cerrar la revisión.','Before preparing an automatic answer check the conversation state. If it already says human, save the message and notify the responsible person without a bot reply. In this example a complaint changes state to human. Save that change before notifying. Only an authorised person may switch back to bot after review.','CHAT-01 queda en human. El siguiente mensaje se guarda pero no provoca una respuesta automática.','CHAT-01 stays in human mode. The next message is saved without triggering an automatic reply.'],
 ],
 slack:[
 ['Send a clear notice to the right channel',{id:'JOB-01',status:'failed',channel:'TEST_CHANNEL',detail:'Email field missing'},'Añade Slack > Send a message y selecciona el canal de prueba por su id. La aplicación debe estar invitada al canal y tener permiso para escribir. Prepara una primera línea con «JOB-01: falta el correo». Escribe debajo qué paso falló y el enlace al registro. Conserva channel y ts de la respuesta; ts identifica el mensaje para continuar en un hilo sin llenar el canal de avisos repetidos.','Add Slack > Send a message and select the test channel by id. Invite the app to that channel and grant message-writing permission. Prepare a first line saying “JOB-01: email missing”. Below it state the failing step and link to the record. Retain channel and ts from the response; ts identifies the message for follow-up in a thread.','El canal de prueba muestra un aviso con el número y el dato que falta; su id queda guardado.','The test channel shows a notice with the job number and missing detail; its id is saved.'],
 ['Use an authorised reaction to update a case',{id:'CASE-01',reaction:'white_check_mark',user:'APPROVER_ID',channel:'TEST_CHANNEL'},'Recibe reaction_added con una conexión que verifique que el evento viene de Slack. Filtra por canal, reacción y lista de ids de personas que pueden aprobar. Busca el caso a partir del mensaje original guardado en tu tabla, no de una frase que cualquiera pueda editar. Actualiza solo si sigue pendiente y registra quién aprobó. Conserva el id del evento para reconocer repeticiones.','Receive reaction_added through a connection verifying Slack’s event. Filter by channel, reaction and permitted approver ids. Find the case using the original message stored in your table, not text anyone could edit. Update only if it is pending and record the approver. Keep the event id to recognise repeats.','Solo APPROVER_ID puede aprobar CASE-01. Otra persona o una reacción repetida no vuelve a ejecutar la acción.','Only APPROVER_ID can approve CASE-01. Another person or a repeated reaction does not run the action again.'],
 ['Look up a case with a slash command',{id:'CASE-01',command:'/consulta',text:'CASE-01',user:'TEST_USER'},'Configura /consulta en tu aplicación de Slack y apunta su Request URL al receptor. Verifica la firma del cuerpo original, el momento de envío y el usuario permitido. Confirma recepción dentro de tres segundos y realiza la búsqueda después. Busca CASE-01 por igualdad exacta en una tabla de prueba. Responde por response_url sin publicar información en un canal abierto.','Configure /consulta in your Slack app and point Request URL to the receiver. Verify the original-body signature, timestamp and permitted user. Acknowledge within three seconds and do the lookup afterwards. Find CASE-01 by exact match in a test table. Reply through response_url without publishing information in an open channel.','CASE-01 devuelve la ficha correcta. Un número inexistente dice «no encontrado» y una persona sin permiso no recibe datos.','CASE-01 returns the correct record. An absent number says “not found” and an unauthorised person receives no data.'],
 ['Summarise support messages with links',{id:'DAY-01',messages:[{id:'M1',text:'How do I sign in?',replies:0},{id:'M2',text:'Thank you',replies:1}]},'Programa una consulta del canal de prueba y fija inicio y fin del día con su zona horaria. Lee todas las páginas de mensajes, excluye bots y consulta los hilos que necesites para comprobar respuestas. Da al modelo mensajes con su id y enlace; pide temas y preguntas sin contestar usando solo esos datos. No deduzcas que un hilo resuelto sigue pendiente por no leer sus respuestas.','Schedule a query of the test channel and define day boundaries and timezone. Read all message pages, exclude bots and inspect threads when checking replies. Give the model messages with ids and links; request topics and unanswered questions using only those details. Do not label a resolved thread pending because its replies were not read.','El resumen señala M1 como pregunta sin respuesta y conserva su enlace. M2 no se presenta como una pregunta pendiente.','The summary identifies M1 as unanswered and keeps its link. M2 is not shown as a pending question.'],
 ['Prepare a new colleague’s welcome checklist',{id:'STAFF-01',name:'Test colleague',email:'your-test-address@example.com',team:'Support'},'Empieza con una fila de prueba en la tabla de incorporaciones. Busca STAFF-01 antes de crear una carpeta en Drive. Guarda folder_id. Selecciona los canales permitidos para Support y prepara la bienvenida con el enlace a la carpeta. Separa las tareas que requieren una persona, como entregar material, de los accesos que la conexión puede conceder. No supongas que enviar una invitación equivale a que fue aceptada.','Start with a test row in the joiners table. Look up STAFF-01 before creating a Drive folder. Save folder_id. Select permitted Support channels and prepare the welcome message with its folder link. Separate tasks needing a person, such as equipment handover, from access the connection can grant. Sending an invitation does not mean it was accepted.','Hay una carpeta, una lista de canales y una lista de tareas por revisar. Repetir STAFF-01 conserva los mismos enlaces.','There is one folder, a channel list and a checklist awaiting review. Repeating STAFF-01 retains the same links.'],
 ],
 telegram:[
 ['Send yourself a useful notice',{id:'NOTICE-01',chat_id:'YOUR_TEST_CHAT',text:'The practice finished'},'Crea un bot con BotFather y guarda el token en la conexión de Telegram. Abre una conversación con él y escribe /start. Usa Telegram Trigger para leer el chat.id de tu propio mensaje. En Telegram > Send Message selecciona ese chat_id y el texto del ejemplo. No escribas el token en el mensaje ni lo guardes en el cuaderno.','Create a bot with BotFather and save the token in the Telegram connection. Open a conversation and send /start. Use Telegram Trigger to read chat.id from your own message. In Telegram > Send Message select that chat_id and the example text. Keep the token out of messages and the notebook.','Tu chat de prueba recibe el texto y el proceso guarda message_id. Se comprueba la llegada; no se promete un tiempo fijo.','Your test chat receives the text and the process saves message_id. Check arrival without promising a fixed delivery time.'],
 ['Look up your own records through a private bot',{id:'ORDER-01',chat_id:'ALLOWED_CHAT',query:'ORDER-01'},'Recibe el mensaje con Telegram Trigger y comprueba chat.id y from.id contra tu lista permitida antes de consultar datos. Busca ORDER-01 en una tabla de prueba usando una consulta con valores separados del código. Devuelve solo número y estado. Para un número inexistente prepara un aviso claro; para una cuenta no autorizada no devuelvas información de la tabla.','Receive the message with Telegram Trigger and check chat.id and from.id against your allowed list before reading data. Look up ORDER-01 in a test table using values separate from query code. Return only number and status. Give a clear notice for an absent number; return no table details to an unauthorised account.','La cuenta permitida ve ORDER-01; la segunda cuenta de prueba no puede consultar la tabla.','The permitted account sees ORDER-01; the second test account cannot query the table.'],
 ['Approve a case with two mobile buttons',{id:'APPROVAL-01',approver:'TEST_USER_ID',state:'pending'},'Envía un mensaje con dos botones inline y callback_data con una referencia al caso. Guarda caso, usuario aprobador, chat_id y message_id. Recibe la pulsación como callback_query con Telegram Trigger. Comprueba from.id, caso pendiente y fecha límite antes de actualizar el estado. Usa answerCallbackQuery para confirmar la pulsación y edita el mensaje original. El botón no llama directamente al webhook de un nodo Wait; necesita este receptor de pulsaciones.','Send a message with two inline buttons and callback_data referencing the case. Save case, approver user id, chat_id and message_id. Receive presses as callback_query with Telegram Trigger. Check from.id, pending state and deadline before updating. Use answerCallbackQuery to acknowledge and edit the original message. The button does not directly call a Wait node webhook; it needs this callback receiver.','Una pulsación válida cambia pending una sola vez. Otra persona, un botón vencido o una repetición no aprueban el caso.','A valid press changes pending once. Another person, an expired button or a repeat cannot approve the case.'],
 ['Publish reviewed news to a test channel',{id:'NEWS-01',title:'Workshop opens',url:'https://example.com/workshop',approved:true},'Añade el bot como administrador de un canal de prueba con permiso para publicar. Lee una novedad aprobada y busca su id en el registro de publicaciones. Si es nueva, prepara título, dos líneas y enlace; revisa su aspecto antes del primer envío. Usa Send Message con el nombre o id del canal y guarda message_id. No marques como publicado un intento cuya respuesta no llegó.','Add the bot as an administrator of a test channel with posting permission. Read an approved update and look up its id in the publication log. For a new one prepare title, two lines and link; review the format before the first send. Use Send Message with channel name or id and retain message_id. Do not mark an attempt published when its response was lost.','NEWS-01 aparece una vez en el canal y está relacionado con el message_id guardado.','NEWS-01 appears once in the channel and is linked to its saved message_id.'],
 ],
 supabase:[
 ['New database row → checked follow-up',{id:'ROW-01',type:'INSERT',record:{id:'ROW-01',email:'ana@example.com'}},'Crea una tabla pública de prueba y un Database Webhook para INSERT. Usa la dirección publicada de un flujo de ensayo y una cabecera secreta. En el receptor comprueba la cabecera antes de leer body.record: record es la fila insertada. Busca record.id antes de realizar la acción. Guarda processed_at solo después de comprobar el resultado; escuchar solo INSERT evita que esa actualización vuelva a activar el mismo proceso.','Create a public test table and an INSERT Database Webhook. Use a published practice workflow address with a secret header. Check that header in the receiver before reading body.record, the inserted row. Look up record.id before taking action. Save processed_at only after checking the result; listening only for INSERT prevents that update restarting this process.','ROW-01 se procesa una vez y tiene fecha de procesamiento. Una cabecera incorrecta se rechaza antes de trabajar.','ROW-01 is processed once with a processing date. An incorrect header is rejected before work.'],
 ['Run a small scheduled database check',{id:'CHECK-01',task:'Count practice requests'},'Activa pg_cron en una base de prueba. Primero ejecuta la consulta de recuento a mano y guarda hora y cantidad en una tabla de control. Programa esa misma operación con cron.schedule usando un nombre único. Consulta cron.job_run_details para ver si funcionó. Durante la práctica cuenta filas; no empieces borrando datos. Desprograma el trabajo de prueba antes de crear el horario definitivo.','Enable pg_cron in a practice database. First run the count query manually and save time and count in a log table. Schedule that operation with cron.schedule using a unique name. Read cron.job_run_details to check success. Count rows during practice instead of deleting data. Unschedule the test job before creating the final schedule.','Dos ejecuciones dejan dos entradas de control con cantidades que coinciden con la tabla.','Two runs leave two log entries with counts matching the table.'],
 ['Prepare a welcome after confirmed registration',{id:'USER-01',profileReady:true,emailConfirmed:false},'Usa un evento de INSERT en tu tabla de perfiles de prueba, sin modificar directamente las tablas internas de Auth. Después de la espera acordada comprueba que el perfil sigue activo y consulta desde un servidor autorizado si el correo está confirmado. El ejemplo tiene emailConfirmed en false: debe esperar. Guarda una marca de bienvenida por usuario antes de permitir repeticiones.','Use an INSERT event on your test profiles table without directly modifying internal Auth tables. After the agreed wait check the profile is active and ask an authorised server whether email is confirmed. The example has emailConfirmed false and must wait. Keep a per-user welcome marker before allowing repeats.','USER-01 no recibe bienvenida mientras el correo siga sin confirmar. Al confirmarlo, se prepara una sola bienvenida.','USER-01 gets no welcome while email remains unconfirmed. Once confirmed, one welcome is prepared.'],
 ['Process an uploaded file and keep its reference',{id:'FILE-01',bucket:'practice',path:'sample.txt',expectedText:'Welcome to the course'},'Después de una subida correcta, tu aplicación añade una fila a una tabla propia llamada uploads con bucket y path. Escucha INSERT en esa tabla para iniciar el flujo. Así no necesitas editar metadatos internos de Storage. Descarga el archivo desde un bucket privado usando acceso autorizado o una dirección firmada temporal. Extrae el texto según el tipo real del archivo y guarda el resultado con upload_id.','After a successful upload your application adds a row to its own uploads table with bucket and path. Listen for INSERT there to start the flow without editing internal Storage metadata. Download the file from a private bucket using authorised access or a temporary signed address. Extract text according to the actual file type and save it with upload_id.','FILE-01 tiene texto extraído y referencia al archivo original. Volver a recibir el mismo upload_id no crea otra ficha.','FILE-01 has extracted text and a reference to the original file. Receiving the same upload_id does not create another record.'],
 ['Make a copy and practise restoring it',{id:'BACKUP-01',table:'practice_requests',rows:3},'Elige una tabla de prueba con tres filas y anota sus números. Exporta datos y estructura con la herramienta de copia compatible con tu cuenta. Guarda la copia en una ubicación privada distinta. Restaura en una base vacía de prueba y consulta los mismos números. Una exportación CSV de una tabla no incluye por sí sola usuarios, archivos de Storage ni todos los permisos de un proyecto.','Choose a three-row test table and record its ids. Export data and structure using a backup tool compatible with your account. Keep the copy in a different private location. Restore into an empty test database and query the same ids. A table CSV export does not by itself include users, Storage files or every project permission.','La base restaurada contiene las tres filas y puedes leerlas. El cuaderno enumera qué incluye y qué no incluye la copia.','The restored database has all three readable rows. The notebook lists what the backup includes and excludes.'],
 ],
 notion:[
 ['New Notion item → tracked follow-up',{id:'PAGE-01',name:'Course enquiry',processed:false},'Comparte la base o fuente de datos de prueba con la conexión de Notion. Configura Notion Trigger para detectar elementos nuevos con la frecuencia disponible. En la salida encuentra el id de página y las propiedades de título y procesado. Selecciona cada valor real al preparar la ficha de destino. Guarda el id de destino en la página solo después de comprobar que se creó.','Share the test database or data source with the Notion connection. Configure Notion Trigger to detect new items at the available frequency. Find page id, title and processed properties in its output. Select actual values when preparing the destination record. Save the destination id on the page only after checking creation.','PAGE-01 tiene una ficha relacionada y una marca de procesamiento; repetir su evento no crea otra ficha.','PAGE-01 has one related record and a processing marker; repeating its event does not create another.'],
 ['Weekly summary including old unfinished items',{id:'WEEK-01',recentEdited:['P1'],staleOpen:['P2']},'Haz dos consultas distintas: páginas editadas durante los últimos siete días y páginas abiertas sin editar durante más de catorce días. Recoge todas las páginas de ambas consultas. No busques los atascados solo dentro de los editados esta semana: los dejarías fuera. Cuenta por estado y conserva id y enlace de cada elemento citado.','Run two separate queries: pages edited in the last seven days and open pages untouched for over fourteen days. Read every page of both queries. Do not search for stale items only within this week’s edits or you will exclude them. Count by state and retain each cited item’s id and link.','El resumen incluye P1 como movimiento reciente y P2 como pendiente antiguo, con enlaces distintos.','The summary includes P1 as recent activity and P2 as an old pending item, with separate links.'],
 ['Labelled email → Notion task',{id:'MAIL-01',label:'a-notion',text:'Please arrange a call on Tuesday'},'En Gmail Trigger selecciona el buzón y la etiqueta de prueba. Conserva message_id y thread_id. Prepara título, petición y fecha mencionada sin inventar una fecha exacta para «martes». Crea una página en la base compartida con la conexión de Notion y guarda su id. Quita la etiqueta del correo solo cuando la página exista y se pueda abrir.','In Gmail Trigger select the mailbox and test label. Retain message_id and thread_id. Prepare title, request and stated date without inventing an exact date for “Tuesday”. Create a page in the database shared with the Notion connection and save its id. Remove the email label only after the page exists and can be opened.','La tarjeta pide organizar una llamada y conserva el enlace al correo. La etiqueta desaparece después de comprobar la tarjeta.','The card requests a call and retains its email link. The label is removed after checking the card.'],
 ],
 airtable:[
 ['New Airtable row → result linked back',{id:'REC-01',name:'Test request',processed:false},'Añade un campo Created time a la tabla de prueba y selecciónalo en Airtable Trigger. Lee el record id real que devuelve Airtable; no lo confundas con el nombre de la solicitud. Prepara una ficha en la hoja de destino y guarda su enlace. En Airtable Update Record usa el record id original para escribir procesado y el enlace, conservando el resto de campos.','Add Created time to the test table and select it in Airtable Trigger. Read Airtable’s returned record id rather than using the request name. Prepare a destination-sheet record and save its link. In Airtable Update Record use the original record id to write processed and the link while preserving other fields.','El registro original tiene el enlace correcto al resultado y no se crea una segunda fila de origen.','The original record links to the correct result without creating a second source row.'],
 ['Send a native Airtable notice for an urgent record',{id:'REC-02',status:'urgent',notified:false},'En la pestaña Automations de Airtable crea When record matches conditions. Exige status igual a urgent y notified sin marcar. Añade una acción de aviso a tu destinatario de prueba y relaciona número y título con el registro disparador. Después actualiza notified en ese mismo record id. Prueba las acciones antes de activar. Si el aviso llega pero la marca falla, revisa el historial antes de repetir.','In Airtable Automations create When record matches conditions. Require status urgent and notified unchecked. Add a notice to your test recipient and map number and title from the triggering record. Then update notified on the same record id. Test actions before enabling. If the notice arrives but marking fails, inspect history before repeating.','REC-02 genera un aviso y queda marcado. Un registro marcado no cumple las condiciones de un nuevo aviso.','REC-02 produces a notice and is marked. A marked record does not meet a new notice’s conditions.'],
 ['Synchronise changed fields without overwriting a conflict',{id:'REC-03',sourceVersion:2,destinationVersion:3,lastSyncedVersion:1},'Añade Last modified time para seleccionar cambios y conserva el último momento procesado. Compara cada registro por su id y por la versión que guardaste al sincronizar. Si origen y destino cambiaron desde esa versión, como ocurre en el ejemplo, marca conflicto y no sobrescribas. Guarda la nueva marca de lectura después de procesar todas las páginas; usa un pequeño solapamiento y reconoce ids para no perder cambios durante la consulta.','Add Last modified time to select changes and retain the last processed time. Compare records by id and by the version saved during the previous sync. If source and destination both changed since that version, as here, flag a conflict without overwriting. Save the new read marker after every page is processed; use a small overlap and recognise ids to avoid losing changes during the query.','REC-03 queda en revisión y ambas versiones se conservan. No se decide en silencio que la última fecha gana.','REC-03 awaits review with both versions preserved. The system does not silently choose the latest timestamp.'],
 ],
 postgres:[
 ['Send a checked daily count',{id:'REPORT-01',today:3,lastWeek:2},'Usa una cuenta que pueda leer la tabla de prueba. Define el día con una zona horaria y consulta entre inicio incluido y fin excluido. Calcula el mismo periodo de la semana anterior por separado. Con tres casos frente a dos, la diferencia es más uno. Prepara ese texto y el rango de fechas para el canal de prueba. Si la consulta falla, informa del error en lugar de enviar cero.','Use an account allowed to read the test table. Define the day and timezone and query from inclusive start to exclusive end. Calculate the same period one week earlier separately. Three cases versus two means plus one. Prepare that text and date range for the test channel. If the query fails, report an error instead of sending zero.','El informe dice 3 frente a 2, diferencia +1, y muestra qué fechas ha contado.','The report says 3 versus 2, difference +1, and shows the dates counted.'],
 ['Notice a queue that needs attention',{id:'RULE-01',pendingHours:50,thresholdHours:48,lastNotice:null},'Escribe una consulta de lectura que seleccione pendientes anteriores al momento actual menos 48 horas. Devuelve el número total y los tres más antiguos. El ejemplo supera el límite por dos horas. Antes de avisar consulta una tabla de avisos por regla y día; guarda la marca con una restricción única para que dos ejecuciones no creen dos avisos pendientes. Distingue reservar el aviso de confirmar su envío.','Write a read query selecting pending items older than now minus 48 hours. Return the count and three oldest items. The example exceeds the limit by two hours. Before notifying query a rule-and-day log; keep its marker under a unique constraint so two runs cannot create two pending notices. Distinguish reserving a notice from confirming its send.','RULE-01 prepara un aviso con casos concretos; repetir la revisión ese día no prepara otro aviso igual.','RULE-01 prepares a notice with specific cases; repeating the check that day does not prepare another identical notice.'],
 ['Schedule maintenance and check that it ran',{id:'MAINT-01',expectedRun:'2026-09-06',observedRun:null},'Empieza con una función de prueba que solo cuente filas y escriba en una tabla de control. Prográmala con pg_cron en la hora acordada y consulta su historial. Prepara un segundo proceso independiente que busque la entrada de control del día. En el ejemplo no existe esa entrada: debe avisar de revisión, no inventar que el mantenimiento funcionó. Añade operaciones que cambien datos únicamente después de probarlas en una copia.','Start with a test function only counting rows and writing to a log table. Schedule it with pg_cron at the agreed time and inspect its history. Prepare a second independent process looking for today’s log entry. None exists in the example: request review rather than inventing successful maintenance. Add data-changing operations only after testing them on a copy.','El vigilante detecta que falta la entrada del día y muestra el nombre del trabajo que hay que revisar.','The watcher detects the missing daily entry and names the job needing review.'],
 ],
 vercel:[
 ['Run a protected Vercel scheduled task',{id:'CRON-01',path:'/api/tarea',schedule:'0 7 * * *'},'Crea la ruta /api/tarea en el servidor del proyecto. Lee CRON_SECRET de las variables del servidor y rechaza la petición con 401 si Authorization no coincide con Bearer seguido del secreto. No devuelvas el secreto en errores. La primera tarea solo cuenta registros de prueba y guarda fecha y cantidad. Declara la ruta y el horario en crons de vercel.json; comprueba zona horaria y límites del plan antes de publicar.','Create the server route /api/tarea. Read CRON_SECRET from server variables and reject with 401 when Authorization does not match Bearer followed by the secret. Never return the secret in errors. The first task only counts practice records and saves date and count. Declare path and schedule in vercel.json crons; check timezone and plan limits before publishing.','La petición sin secreto recibe 401. Una ejecución autorizada deja un registro comprobable; la ruta no expone la clave.','A request without the secret receives 401. An authorised run leaves an inspectable record without exposing the key.'],
 ['Rebuild after a group of content changes',{id:'CONTENT-01',changes:5,lastChange:'2026-09-06T10:00:00Z'},'Crea un Deploy Hook en Settings > Git del proyecto de prueba y guarda su dirección como secreto. Cada cambio de contenido actualiza una fila compartida con project_id y last_change. Un proceso programado busca proyectos sin cambios durante cinco minutos y reclama una única tarea de publicación. Una espera separada por cada evento no agrupa cinco cambios: produciría cinco llamadas. Guarda el trabajo devuelto por el hook y comprueba el despliegue correspondiente.','Create a Deploy Hook in Settings > Git for the test project and keep its address secret. Each content change updates a shared project_id/last_change row. A scheduled process finds projects unchanged for five minutes and claims one publication job. A separate wait per event does not group five changes; it produces five calls. Save the hook job and check its corresponding deployment.','Cinco cambios del mismo proyecto dejan una petición de reconstrucción después del periodo de calma.','Five changes to one project leave one rebuild request after the quiet period.'],
 ['Explain a failed deployment with its original log',{id:'DEPLOY-01',status:'failed',log:'Missing environment variable EXAMPLE_KEY'},'Recibe un evento de despliegue con la verificación de firma indicada por Vercel. Filtra por el proyecto de prueba y estado de fallo. Consulta el registro del despliegue con una cuenta autorizada y oculta claves o datos personales antes de usar IA. Pide explicar el error en una frase y citar la línea original. Guarda deployment id para reconocer eventos repetidos; envía el enlace al despliegue para que una persona compruebe la causa.','Receive a deployment event with Vercel’s documented signature verification. Filter for the test project and failure status. Read its log using an authorised account and remove keys or personal information before using AI. Ask for a one-sentence explanation citing the original line. Keep deployment id to recognise repeated events and link to the deployment so a person can check the cause.','El aviso dice que falta EXAMPLE_KEY y enlaza al despliegue; no asegura que crear cualquier valor arreglará el proyecto.','The notice says EXAMPLE_KEY is missing and links to the deployment; it does not promise that any arbitrary value fixes the project.'],
 ],
}
