/**
 * Las automatizaciones reales, en inglés.
 *
 * Mismo orden y mismo número de recetas que `automations-reales.mjs`. Se
 * traduce el texto; los disparadores, nodos, códigos de error y nombres de
 * producto se dejan como están, porque son lo que el alumno va a ver
 * literalmente en pantalla dentro de n8n, Meta o Google.
 *
 * `npm run validate` comprueba que las dos listas coinciden en longitud.
 */

export const REAL_AUTOMATIONS_EN = {
  sheets: [
    {
      name: 'New row in the sheet → record created and the owner notified',
      goal: 'A new entry in the sheet (from a form or typed by hand) starts the process without anyone watching.',
      difficulty: 'basica',
      platform: 'n8n · Google Sheets Trigger',
      trigger: 'The n8n Sheets Trigger checks the sheet every minute and hands you only the rows added since the last pass.',
      steps: [
        'Sheets Trigger pointed at the log tab, with row 1 as the field names (lower case, no accents).',
        'Filter: drop test rows (name contains "test") and rows with no email.',
        'Check whether the email already exists in the master sheet before creating anything: that is what stops duplicates.',
        'Create the record wherever it belongs (another sheet, Notion, a CRM) with the fields mapped.',
        'Write the processed marker back on the original row: date and time in a column of its own.',
        'Notify the owner on Slack or by email with the name and a link, only when the entry is real.',
      ],
      test: 'Add a real row, one with no email and one that repeats. Only the first should create a record and a notification; the other two should stop and be marked.',
      failure: 'If the sheet returns 404, the credential cannot see it: share it again. If duplicate rows arrive, someone inserted rows in the middle: the trigger works by position, so always append at the end.',
      credentials: 'A Google OAuth credential with access to that specific sheet. The processed marker is your safety net if the flow gets rerun.',
    },
    {
      name: 'Daily summary of the sheet at 9:00',
      goal: 'An email or message every morning with what is outstanding, without opening the sheet.',
      difficulty: 'basica',
      platform: 'n8n · Schedule + Google Sheets',
      trigger: 'Schedule Trigger every working day at 9:00, with the Europe/Madrid timezone checked.',
      steps: [
        'Read every row of the log tab.',
        'Filter by pending status, comparing in lower case and with "contains", not "exactly equals".',
        'Aggregate the pending items into a single list (Aggregate node): one message, not one per row.',
        'If there are zero pending items, finish without sending: silence is correct information too.',
        'Send the summary with the total and the three oldest highlighted.',
      ],
      test: 'Run it with pending items and with none. With zero, no message should arrive. Check the actual firing time the next day.',
      failure: 'If the summary arrives at 7:00, the timezone is set to UTC. If one message arrives per row, the Aggregate node is missing.',
      credentials: 'The same Google credential as the previous flow; it gets reused.',
    },
    {
      name: 'Weekly clean-up: move closed items to the archive',
      goal: 'Keep the working sheet from piling up thousands of rows and slowing down.',
      difficulty: 'intermedia',
      platform: 'n8n · Schedule + Google Sheets',
      trigger: 'Schedule Trigger on Sundays at 22:00.',
      steps: [
        'Read the rows with a closed status and a closing date more than 30 days ago.',
        'Append them to the "archive" tab of the same document.',
        'Check that the number of rows written matches the number read before deleting anything.',
        'Delete from the log only the rows you verified, from the bottom up so positions do not shift.',
        'Note in a control cell how many were moved and when.',
      ],
      test: 'Try it with 3 old closed rows and 2 recent ones: only the 3 should move. Check the archive has them BEFORE you check whether the log lost them.',
      failure: 'If the flow dies between writing and deleting, you will have rows duplicated across tabs: duplicated beats lost, which is why it writes first and deletes after.',
      credentials: 'Google OAuth. This flow DELETES data: test it against a copy of the document before you give it the real sheet.',
    },
    {
      name: 'Enrich every new entry with AI and classify it',
      goal: 'Every new row arrives already classified (client type, urgency) with no manual work.',
      difficulty: 'intermedia',
      platform: 'n8n · Sheets Trigger + a language model',
      trigger: 'A new row in the entries sheet.',
      steps: [
        'Take the free text from the row (subject, the message from the form).',
        'Call the model with structured output: a category from a closed list, urgency 1-3, a one-sentence summary.',
        'Validate the answer against the closed list; if it does not fit, mark it "review" instead of inventing one.',
        'Write the three new columns onto the same row.',
        'If urgency is 3, alert immediately; everything else goes into the morning summary.',
      ],
      test: 'Feed it a clear case, an ambiguous one and a blank one. The ambiguous one should come out as "review", not as a confidently invented category.',
      failure: 'If the model returns free text instead of the category, tighten the prompt (only those values) or use the node\'s structured output. Never write anything into the sheet that did not pass validation.',
      credentials: 'Google OAuth plus an AI provider key with a spending cap set. Cost per row: cents; put a counter on it.',
    },
    {
      name: 'Watch a threshold cell and alert when it is crossed',
      goal: 'Find out the day stock, balance or a metric crosses the red line.',
      difficulty: 'basica',
      platform: 'n8n · Schedule + Google Sheets',
      trigger: 'Schedule every hour during working hours.',
      steps: [
        'Read the specific range (a cell, or a column calculated in the sheet).',
        'Compare it against the threshold defined in the flow itself, not hidden inside a formula.',
        'Alert only if it is crossed AND you have not already alerted today: store the last alert date in a control cell.',
        'Include the current value, the threshold and a direct link to the sheet in the alert.',
      ],
      test: 'Force the value below the threshold and run it twice in a row: it should alert the first time and stay quiet the second.',
      failure: 'Without the control cell you get one alert an hour until someone fixes the data: the flow becomes noise and people mute it.',
      credentials: 'Google OAuth and the alert channel (Slack, email or Telegram).',
    },
    {
      name: 'Web form → sheet, with validation in between',
      goal: 'Collect entries from your website into the sheet without a paid service in the middle.',
      difficulty: 'intermedia',
      platform: 'n8n · Webhook + Google Sheets',
      trigger: 'An n8n Webhook that receives the form submission (a POST with the fields).',
      steps: [
        'Webhook with an immediate "received" response so the website does not wait.',
        'Validate the fields: an email with an @, a phone number of a sensible length, an empty honeypot (anti-spam).',
        'Normalise: lower-case the email, trim spaces, put the date in YYYY-MM-DD.',
        'Append the row with source "web" and a timestamp.',
        'Send the confirmation email to the sender only if everything validated.',
      ],
      test: 'Submit the legitimate form, one with the honeypot filled in and one with no email. Only the first should reach the sheet.',
      failure: 'If duplicate entries arrive, the user clicked twice: disable the button on submit in the website, and check for an existing email+date in the flow.',
      credentials: 'The webhook\'s Production URL (not the test one) and Google OAuth. The honeypot stops 90% of spam without a captcha.',
    },
  ],

  whatsapp: [
    {
      name: 'Incoming message → record and an acknowledgement reply',
      goal: 'No client message gets lost on somebody\'s phone.',
      difficulty: 'intermedia',
      platform: 'n8n · WhatsApp Business webhook (Cloud API)',
      trigger: 'The Cloud API sends a webhook to your n8n for every message the company number receives.',
      steps: [
        'Verify the webhook signature and return 200 immediately: if you are slow, Meta retries and duplicates arrive.',
        'Pull the sender, the text and the type (text, audio, image) out of the payload.',
        'Check whether the sender already has an open conversation (by phone number) before creating a new record.',
        'Save the message in your log (a sheet or a database) with a timestamp.',
        'Send the acknowledgement inside the 24-hour window: outside it, only approved templates are allowed.',
        'Alert the team if the message contains urgency words you have defined.',
      ],
      test: 'Write in from a test phone: a normal message, an audio note and two messages in a row. Check the second does not create a second record.',
      failure: 'If webhooks stop arriving, the token expired or Meta disabled the URL after errors: check the app panel at developers.facebook.com. Meta\'s retries are the number one cause of duplicates.',
      credentials: 'A WhatsApp Business app in Meta for Developers, a permanent token, a verified number and the webhook\'s Production URL. The 24-hour window is Meta\'s rule, not yours.',
    },
    {
      name: 'Outbound alert with a template when something happens in your system',
      goal: 'Confirmations and alerts (order ready, appointment tomorrow) to the client\'s WhatsApp.',
      difficulty: 'intermedia',
      platform: 'n8n · WhatsApp node / HTTP to the Cloud API',
      trigger: 'Whatever happens in your system: a new row, a status change, a scheduled time.',
      steps: [
        'Check there is recorded consent to write to that number: without it, nothing gets sent.',
        'Pick the approved template that fits (free-form messages only work inside the 24-hour window).',
        'Fill the template variables with this case\'s data.',
        'Send it and save the message identifier the API returns.',
        'Listen to the status webhook (sent, delivered, read, failed) and record the outcome.',
        'If it fails because the number is invalid, flag the record for review instead of retrying blindly.',
      ],
      test: 'Send a real template to your own number. Check the status reaches "delivered" in your log.',
      failure: 'Error 131047: outside the 24-hour window with no template. Error 100 on variables: the template expects a different number of parameters. Templates are approved in Meta\'s panel and take hours.',
      credentials: 'The same Meta app credentials, plus the approved templates. Consent is a legal obligation (GDPR), not a courtesy.',
    },
    {
      name: 'Appointment reminder 24 hours before',
      goal: 'Cut no-shows: an automatic reminder with the option to confirm or reschedule.',
      difficulty: 'intermedia',
      platform: 'n8n · Schedule + Cloud API + your calendar',
      trigger: 'Schedule every hour, looking for tomorrow\'s appointments that have not had a reminder yet.',
      steps: [
        'Read the appointments between 23 and 25 hours away from your calendar (a sheet, Calendar, a database).',
        'Drop the ones that already have a reminder sent (control column).',
        'Send the template with the date, the time and the confirm or reschedule buttons.',
        'Mark the send in the control column with the date and time.',
        'Listen for the button replies and update the appointment: confirmed, or to be rescheduled.',
        'Tell the team about the ones that asked to reschedule.',
      ],
      test: 'Create a test appointment for tomorrow with your own number. Exactly one reminder should arrive, even though the flow runs every hour.',
      failure: 'Duplicate reminders = the control column is missing, or it is read before it is written. Lost replies = the incoming webhook does not tell button replies apart from normal messages: look at the payload\'s type field.',
      credentials: 'A template with buttons approved by Meta. Quick-reply buttons arrive as a different payload from text: test it before assuming.',
    },
    {
      name: 'WhatsApp audio → text → summary in your log',
      goal: 'Turn clients\' three-minute voice notes into text you can act on.',
      difficulty: 'avanzada',
      platform: 'n8n · Cloud API + transcription + a language model',
      trigger: 'The incoming-message webhook with type audio.',
      steps: [
        'Download the audio using the temporary URL the API gives you (it expires: download it right away).',
        'Transcribe it with a speech-to-text service (Whisper or another).',
        'Summarise with the model: what they are asking for, by when, and whether it is urgent, as structured output.',
        'Save the transcript and the summary on the client\'s record.',
        'Send the acknowledgement and alert the team with the summary, not with the audio.',
      ],
      test: 'Send yourself a real voice note with a request inside it. The summary should capture what is being asked and by when; if it does not, adjust the prompt.',
      failure: 'If the download returns 404, the URL expired: it has to be downloaded in the same flow, not a later one. Cost: transcribing plus summarising costs cents per audio; put a monthly counter on it.',
      credentials: 'Meta plus a key for the transcription service and the model, both with spending caps. Voice notes are personal data: delete the file after transcribing if you do not need it.',
    },
    {
      name: 'Escalate to a human when the bot cannot cope',
      goal: 'The automation knows when to step back: complex cases or an angry client go to a person.',
      difficulty: 'avanzada',
      platform: 'n8n · Cloud API + a classifier',
      trigger: 'Every incoming message, after the record flow.',
      steps: [
        'Classify the message: simple question, admin request, complaint, urgency.',
        'The flow answers the simple ones with replies you have defined (opening hours, address, prices).',
        'Complaint or urgency: mark the conversation as "human", alert the owner with the recent history, and have the bot STOP replying in that conversation.',
        'Store the human/bot state per conversation so the flow respects it.',
        'Go back to bot mode only when the human closes the conversation.',
      ],
      test: 'Write "I want to make a complaint": it should alert the owner, and the bot should go quiet on the following messages.',
      failure: 'The serious failure is a bot replying to an angry client in a loop. The per-conversation state is what prevents it: do not switch this flow on without it.',
      credentials: 'The Meta credentials plus the per-conversation state store. House rule: when in doubt, escalate; one human too many is cheap, a lost client is not.',
    },
  ],

  slack: [
    {
      name: 'Alerts from your systems in the right channel',
      goal: 'What happens in your flows (new entries, errors, thresholds) reaches Slack in order and without noise.',
      difficulty: 'basica',
      platform: 'n8n · Slack node',
      trigger: 'Whatever your other flows decide: a new entry, an error, a threshold crossed.',
      steps: [
        'Create the Slack app with the chat:write scope and invite it to the channels with /invite.',
        'Separate the channels: #business-alerts for normal things, #failure-alerts for broken ones.',
        'Put the key fact on the first line of the message: it is the only part visible on a phone.',
        'Detail goes in the thread, not the channel: one alert, one message.',
        'Successes get grouped into a daily summary; failures go out immediately.',
      ],
      test: 'Send a test alert and look at it on a locked phone: if the notification does not say what happened, rewrite the first line.',
      failure: 'not_in_channel: the /invite is missing. channel_not_found: use the channel ID (C0...), not the name. A channel that overdoes alerts ends up muted, and then it does not exist.',
      credentials: 'A Slack app with a bot token (xoxb-...). Incoming webhooks are fine to start with; the app gives you more control.',
    },
    {
      name: 'Emoji reaction → a real action',
      goal: 'Turn Slack into a control panel: a ✅ from the owner approves; a 📌 creates a task.',
      difficulty: 'intermedia',
      platform: 'n8n · Slack Trigger (reaction_added)',
      trigger: 'The reaction_added event in the channels the app is in.',
      steps: [
        'Listen for the event and filter by the specific emoji and by channel.',
        'Check WHO reacted: approvals only count from the list of approvers.',
        'Fetch the original message to work out which case it refers to.',
        'Run the action: approve the record, create the task, move the status.',
        'Reply in the thread confirming what was done and who approved it.',
        'Silently ignore reactions from anyone not authorised.',
      ],
      test: 'React with ✅ from an authorised account and from one that is not: only the first should run, and the thread should say who approved.',
      failure: 'If the action runs twice, someone removed the emoji and put it back: store the approved state and make the action idempotent (a second approval does nothing).',
      credentials: 'An app with reactions:read, channels:history and chat:write, and an event subscription pointing at your n8n webhook.',
    },
    {
      name: 'A /lookup command to query your data from Slack',
      goal: 'The team can check an order or a client without leaving Slack or touching the database.',
      difficulty: 'avanzada',
      platform: 'n8n · Slash command + your database',
      trigger: 'The /lookup [term] command defined in the Slack app, which calls your webhook.',
      steps: [
        'Reply to Slack in under 3 seconds with "searching…" (mandatory: otherwise you get a timeout error).',
        'Search for the term in your database or sheet: by name, email or order number.',
        'Format the record you found with the four useful facts and a link to the system.',
        'Send the result using the response_url Slack included in the call.',
        'If there are no results, say so with a suggestion ("try the email"), not in silence.',
      ],
      test: 'Run /lookup with a real client, a non-existent one and an ambiguous term with several matches: all three should answer something useful.',
      failure: 'The 3-second timeout: the immediate reply is MANDATORY and the search comes afterwards, via response_url. It is the mistake everyone makes the first time.',
      credentials: 'An app with the slash command pointed at your Production URL, plus read access to your data. Be careful which personal data you show in a channel: a direct message is better.',
    },
    {
      name: 'Daily AI summary of the support channel',
      goal: 'A paragraph each afternoon: what was asked today, what went unanswered, what keeps coming up.',
      difficulty: 'intermedia',
      platform: 'n8n · Schedule + Slack + a language model',
      trigger: 'Schedule at 18:00, Monday to Friday.',
      steps: [
        'Read the day\'s channel messages with channels:history.',
        'Filter out bots and automated alerts: people\'s messages only.',
        'Ask the model for the summary: the topics, the unanswered questions (no replies in the thread), and what has repeated this week.',
        'Post the summary in the team channel with links to the unanswered messages.',
      ],
      test: 'Run it on a genuinely busy day: the "unanswered" ones should be real messages with no thread — check by hand for the first week.',
      failure: 'If the summary invents topics, lower the temperature and confine the model to the messages given ("only from these messages, add nothing"). If messages are missing, the app is not in the channel or the history scope is missing.',
      credentials: 'An app with channels:history and chat:write, plus a model key with a cap. Team messages are internal data: use a provider your company has approved.',
    },
    {
      name: 'New hire → folder, access and a welcome',
      goal: 'Someone\'s first day does not depend on anybody remembering anything.',
      difficulty: 'intermedia',
      platform: 'n8n · triggered from the new-hire entry + Slack',
      trigger: 'A new row in the staff onboarding sheet (or the event from your HR system).',
      steps: [
        'Create the employee\'s Drive folder with the standard structure.',
        'Invite them to the Slack channels that match their team.',
        'Send them the welcome message with the links for their first few days.',
        'Create the owner\'s task list (equipment, the access that really does need a human).',
        'Announce in the team channel that they are joining, with the date.',
      ],
      test: 'Onboard a fictional person using your own email: check the folder, the channels and the message. Delete it all afterwards.',
      failure: 'Real permissions (company email, internal systems) are NOT automated from here without IT signing off: the flow creates the request, not the access.',
      credentials: 'Slack with a limited users:write, Google Drive, and an explicit agreement with IT about what this flow may touch.',
    },
  ],

  github: [
    {
      name: 'Push to main → deploy and report the result',
      goal: 'Publishing follows from pushing code, with a notification saying whether it worked.',
      difficulty: 'basica',
      platform: 'GitHub Actions (native) + a Slack/Telegram alert',
      trigger: 'on: push to the repository\'s main branch.',
      steps: [
        'An Actions workflow: install dependencies, run the tests, build.',
        'If the tests fail, stop there: nothing broken gets deployed.',
        'Deploy (Vercel, your own server) only when the tests are green.',
        'Notify the channel with the commit, the author and a link, on success and on failure alike.',
        'On failure, include the last lines of the log: the alert should be enough to start fixing.',
      ],
      test: 'Push a commit that breaks a test on purpose: it should stop, not deploy, and report why.',
      failure: 'If the deploy runs even when tests fail, the needs: between jobs is missing. If Actions does not start, the YAML has an indentation error: GitHub\'s own validator points at it.',
      credentials: 'The deployment secrets in the repo\'s Settings → Secrets, never in the YAML. The alert uses a channel webhook.',
    },
    {
      name: 'New issue → classify, label and assign',
      goal: 'Issues do not pile up with no owner and no label.',
      difficulty: 'intermedia',
      platform: 'n8n · GitHub Trigger (issues) + a language model',
      trigger: 'A GitHub webhook when an issue is opened in the repo.',
      steps: [
        'Receive the webhook with the issue title and body.',
        'Classify with the model: bug, improvement, question or duplicate, against the list of open issues.',
        'Apply the label and assign by area (file paths mentioned, keywords).',
        'If it looks like a duplicate, comment with a link to the original and label it "possible duplicate" — closing is a human decision.',
        'If it is from a paying client (author on your list), alert the channel with priority.',
      ],
      test: 'Open a clearly written test issue and an ambiguous one: the ambiguous one should end up "unclassified" for review, not confidently mislabelled.',
      failure: 'Closing duplicates automatically is the line you do not cross: the model gets it wrong, and closing someone\'s valid issue is expensive. Labelling and suggesting, yes; closing, human.',
      credentials: 'A repo webhook (Settings → Webhooks) pointed at your Production URL, a GitHub token with issue permissions, and a model key.',
    },
    {
      name: 'Pull request → a summary of the change for non-technical people',
      goal: 'The business side knows what is being changed without reading code.',
      difficulty: 'intermedia',
      platform: 'n8n · GitHub Trigger (pull_request) + a model',
      trigger: 'A PR opened, or marked ready for review.',
      steps: [
        'Collect the title, the description and the list of changed files (the full diff only if it is small).',
        'Ask the model for a summary in business language: what changes for the user, what the risk is, what should be tested.',
        'Post the summary as a comment on the PR.',
        'Send the one-line version to the product channel.',
      ],
      test: 'Try it on a real, small PR: someone who does not code should understand the summary. If it talks about files and functions, adjust the prompt.',
      failure: 'With huge PRs the diff does not fit in the context: summarise from the file list and the title, and say so in the comment ("partial summary: large change").',
      credentials: 'A PR webhook, a token with comment permission, and a model key. The code may be confidential: use a provider that does not train on your data.',
    },
    {
      name: 'Alert for a dependency with a known vulnerability',
      goal: 'Hear about security advisories for your dependencies without living in GitHub.',
      difficulty: 'basica',
      platform: 'GitHub (native Dependabot) + n8n for the alert',
      trigger: 'A Dependabot alert in any of your repos.',
      steps: [
        'Turn on Dependabot alerts in the repos (Settings → Security).',
        'Point an organisation or repo webhook at n8n, filtering for security events.',
        'Classify by severity: critical and high alert immediately; the rest go into the weekly summary.',
        'The alert carries the package, the fixed version and a link: what you need in order to decide.',
      ],
      test: 'Repos with old dependencies generate real alerts straight away; check that severity separates the noise from the urgent properly.',
      failure: 'The classic mistake is the opposite one: alerting on EVERYTHING and muting it a week later. Critical goes to the channel, the rest goes to Friday.',
      credentials: 'Repo admin permissions for security webhooks. Dependabot is free on public and private repos.',
    },
    {
      name: 'Release published → changelog and a note to clients',
      goal: 'Publishing a version triggers the communication, rather than another manual task.',
      difficulty: 'intermedia',
      platform: 'n8n · GitHub Trigger (release)',
      trigger: 'The repository\'s release published event.',
      steps: [
        'Collect the version name and the release notes.',
        'Rewrite the notes with the model in client language: what is new and what was fixed, without jargon.',
        'Post it in the internal channel and on the what\'s-new page.',
        'Prepare (do not send) the draft email to clients: a mass send gets approved by a human.',
      ],
      test: 'Publish a test release in a test repo: the client-facing text should contain no function or file names.',
      failure: 'A draft that sends itself with no approval is the recipe for a mass email with a mistake inside it. The human at the last step is not optional.',
      credentials: 'A release webhook, a model, and access to the email tool in draft mode.',
    },
  ],

  telegram: [
    {
      name: 'Personal alerts bot: your system talks to you on Telegram',
      goal: 'The cheapest, most reliable alert channel there is: free, instant, and no corporate app needed.',
      difficulty: 'basica',
      platform: 'n8n · Telegram node',
      trigger: 'Any of your flows that wants to tell you something.',
      steps: [
        'Create the bot with @BotFather (/newbot) and save the token.',
        'Send /start to the bot from your account and capture your chat_id with the Telegram Trigger.',
        'Save the chat_id: it is your "address" for every future flow.',
        'A Telegram node in any flow: sendMessage with the alert.',
        'Format with bold text and functional emoji: 🔴 failure, 🟡 warning, 🟢 summary.',
      ],
      test: 'Send yourself an alert from a test flow: it should arrive in under a second.',
      failure: 'chat not found: you have not sent /start to the bot (bots cannot start a conversation — that is Telegram\'s rule). 401: the token was copied wrong.',
      credentials: 'Just the BotFather token. It is the best usefulness-to-effort integration in the whole course.',
    },
    {
      name: 'Lookup bot: query your data from your phone',
      goal: 'Write "order 2043" to the bot and get the record back, from anywhere.',
      difficulty: 'intermedia',
      platform: 'n8n · Telegram Trigger + your data',
      trigger: 'An incoming message to the bot.',
      steps: [
        'Telegram Trigger listening for messages.',
        'Filter by authorised chat_id: the bot is public by design and anyone can find it — the allowlist is the security.',
        'Interpret the query: a simple pattern (an order number) or a model for free-form language.',
        'Search your sheet or database and reply with the formatted record.',
        'For anyone not authorised, silence or "private bot", and log the attempt.',
      ],
      test: 'Ask from your account and from another, unauthorised one: the second must not receive data.',
      failure: 'Forgetting the allowlist is serious: a bot with no filter shows your data to whoever finds it. It is the first line of the flow, not an extra.',
      credentials: 'The bot token plus read access to your data. The authorised chat_id lives in the flow, not in the code.',
    },
    {
      name: 'Approve from your phone with two buttons',
      goal: 'Approvals (a quote, a publication, a discount) without opening the laptop.',
      difficulty: 'intermedia',
      platform: 'n8n · Telegram with inline buttons + Wait',
      trigger: 'The flow that needs approval reaches its human step.',
      steps: [
        'Send the case to the approver with two inline buttons: Approve / Reject.',
        'The flow waits (a Wait node with a webhook) for the press.',
        'When it is pressed, check that whoever pressed it is the approver.',
        'Carry on down the right branch and edit the original message with the result ("Approved by Ana, 14:02").',
        'If nobody answers within the deadline, remind once and then escalate.',
      ],
      test: 'Send a test approval and press it from another account: it should not count. Leave another one unanswered: it should escalate.',
      failure: 'If the button does nothing, the callback is not reaching the Wait: check that the resume webhook URL is the production one. Editing the message with the result prevents double approvals.',
      credentials: 'The bot token and the list of approvers by chat_id. Log who approved what and when: that is your audit trail.',
    },
    {
      name: 'A Telegram channel as an automatic noticeboard',
      goal: 'Publish updates (offers, notices, content) to your channel without doing it by hand.',
      difficulty: 'basica',
      platform: 'n8n · Schedule or a trigger + Telegram',
      trigger: 'Whatever produces the update: a new row, an RSS item, a fixed time.',
      steps: [
        'Add the bot as an administrator of the channel, with permission to post.',
        'Format the post with a fixed template: title, two lines, link.',
        'Post with sendMessage to @channel_name.',
        'Log what was posted so content does not repeat.',
      ],
      test: 'Post a test in a test channel first. Check the formatting on a phone.',
      failure: 'CHAT_WRITE_FORBIDDEN: the bot is not an administrator of the channel. Posting the same thing twice: the posted-items log is missing.',
      credentials: 'The bot as a channel admin. A channel is public: no client data there.',
    },
  ],

  supabase: [
    {
      name: 'Row inserted → webhook to n8n → the rest of the process',
      goal: 'A new entry in your database (from your app) starts the whole process without touching the app.',
      difficulty: 'intermedia',
      platform: 'Supabase Database Webhooks → n8n',
      trigger: 'Supabase\'s native webhook on INSERT against the table (Database → Webhooks).',
      steps: [
        'Create the webhook in Supabase: the table, the INSERT event, and your n8n Production URL.',
        'In n8n, validate the shared secret in the header: nobody should be able to invent entries.',
        'Read the record from the payload (it arrives whole, with the new row).',
        'Do whatever comes next: a welcome email, a record in the CRM, an alert to the team.',
        'Write the outcome back onto the row itself (processed_at) via the Supabase API.',
      ],
      test: 'Insert a row from the Table Editor: the flow should fire within a second. Insert another with incomplete data and check the error path.',
      failure: 'If nothing arrives, the webhook points at the test URL (which dies when you close the editor) or the flow is not active. Supabase retries if you return an error: make the flow idempotent with processed_at.',
      credentials: 'A Supabase project with the webhook, the shared secret, and the service_role key ONLY in the n8n credential, never in the browser.',
    },
    {
      name: 'Scheduled job inside Supabase with pg_cron',
      goal: 'Daily clean-ups and closings that run INSIDE the database, depending on nothing external.',
      difficulty: 'avanzada',
      platform: 'Supabase native · the pg_cron extension',
      trigger: 'The database\'s own scheduler: every night at 3:00.',
      steps: [
        'Enable the pg_cron extension from Database → Extensions.',
        'Write the operation as a SQL function: delete expired sessions, archive closed items, recalculate counters.',
        "Schedule it: select cron.schedule('cleanup', '0 3 * * *', 'select nightly_cleanup()');",
        'Log every run in an audit table (when, how many rows).',
        'A weekly n8n flow checks the audit table has recent entries: the watcher\'s watcher.',
      ],
      test: 'Schedule it every minute while testing, confirm two runs in the audit table, then change it to the real time.',
      failure: 'pg_cron runs whether you are looking or not: without the audit table and its watcher, it can sit broken for weeks with nobody knowing.',
      credentials: 'SQL access to the project. pg_cron uses the server\'s UTC time: 3:00 in Madrid is 1:00 or 2:00 UTC depending on the time of year.',
    },
    {
      name: 'New user signs up → welcome and a record in your CRM',
      goal: 'Every sign-up in your app starts the commercial process.',
      difficulty: 'intermedia',
      platform: 'Supabase Auth Hook / webhook on auth.users → n8n',
      trigger: 'A new row in the Auth users table (an INSERT webhook or an Auth Hook).',
      steps: [
        'Set the trigger on the new user (a database webhook on the profiles table is the simplest option).',
        'Wait 10 minutes (Wait node): sign-ups that get deleted a minute later do not deserve the process.',
        'Check the user still exists and confirmed their email.',
        'Create the record in your CRM or sales sheet with source "app".',
        'Send the welcome (a template a human reviewed once, not on every send).',
      ],
      test: 'Sign up with a test email: the record and the welcome should arrive after the wait, and only if you confirmed the email.',
      failure: 'Welcomes to unconfirmed addresses = spam to the wrong people. The check after the Wait is not paranoia: it is data hygiene.',
      credentials: 'The webhook plus service_role in n8n. The auth.users table is sensitive: better to mirror it in public.profiles and trigger on that.',
    },
    {
      name: 'File uploaded to Storage → process it',
      goal: 'Uploading a PDF or an image to your app starts its processing (extract, resize, classify).',
      difficulty: 'avanzada',
      platform: 'Supabase Storage webhook → n8n',
      trigger: 'An object created in the Storage bucket.',
      steps: [
        'A webhook on the storage.objects table on INSERT, filtered by bucket.',
        'Download the file in n8n with a temporary signed URL.',
        'Process it by type: extract text from the PDF, a thumbnail from the image, a transcript from the audio.',
        'Save the result in the right table, linked to the file.',
        'Mark the object as processed in its metadata so it does not repeat.',
      ],
      test: 'Upload a real PDF from the app: the extracted text should appear in its table in under a minute.',
      failure: 'Huge files break the flow on memory or time: put a size limit on the upload (the bucket policy) and split heavy processing into chunks.',
      credentials: 'service_role for the signed URLs. User files are personal data: process them, and do not move them to services they should not reach.',
    },
    {
      name: 'Weekly external backup of the critical tables',
      goal: 'Your data also exists outside Supabase, restorable by you.',
      difficulty: 'intermedia',
      platform: 'n8n · Schedule + the Supabase API',
      trigger: 'Schedule on Sundays at 23:00.',
      steps: [
        'Export the critical tables via the API or pg_dump (depending on your access) to JSON or CSV.',
        'Compress and upload to storage outside Supabase (Drive, S3, a disk of your own).',
        'Verify the backup: open it and count the rows against the database.',
        'Log the date, size and count; alert only on failure.',
        'Once a quarter: restore the backup into an empty project. An untested backup is not a backup.',
      ],
      test: 'Run it and restore the backup into a test project: if you cannot restore it today, you will not manage it in a crisis.',
      failure: 'Supabase has its own backups, but they are THEIRS and tied to their plan: an external one protects you from account closures, your own mistakes and plan changes. The silent failure is avoided by alerting on "it did not run".',
      credentials: 'service_role or a read-only connection string. The backup carries personal data: encrypt it and control access to it exactly as you do the database.',
    },
  ],

  notion: [
    {
      name: 'New item in a Notion database → the whole process',
      goal: 'Adding a card in Notion (a lead, a task, an order) starts the process.',
      difficulty: 'intermedia',
      platform: 'n8n · Notion Trigger (polling)',
      trigger: 'The Notion Trigger asks every minute for new items in the chosen database.',
      steps: [
        'Share the database with the Notion integration: without that step the API cannot see it (the classic 404).',
        'Notion Trigger on the database, ordered by created time.',
        'Read the properties: in the API each type (title, select, date) arrives with a different shape; map them carefully once.',
        'Run the process: an alert, an external record, an email.',
        'Mark the item with a "processed" property so a retry does not duplicate it.',
      ],
      test: 'Create a test card: the flow should see it within a minute or two (it is polling, not a webhook: the delay is normal).',
      failure: 'object_not_found: the database is not shared with the integration. Empty properties: the property name changed in Notion and the mapping is stale.',
      credentials: 'A Notion internal integration with its token, and the database SHARED with it. The polling delay is the price: if you need seconds, Notion is not your trigger.',
    },
    {
      name: 'Weekly summary of what moved in Notion',
      goal: 'On Mondays, a message with what was created, closed and stuck last week.',
      difficulty: 'basica',
      platform: 'n8n · Schedule + Notion',
      trigger: 'Schedule on Mondays at 8:30.',
      steps: [
        'Query the database filtering by "last edited in the past 7 days".',
        'Group by status: new, closed, and untouched for more than 14 days (the stuck ones).',
        'Format the summary with the counts and the three oldest stuck items, with links.',
        'Send it to the team channel.',
      ],
      test: 'Run it by hand on a Monday: the stuck items should genuinely be the ones nobody is touching — check against the database.',
      failure: 'If the query brings back too much, the date filter has the timezone wrong: Notion API dates come in ISO with a zone; compare in UTC.',
      credentials: 'The same Notion integration, read-only here.',
    },
    {
      name: 'Important email → a Notion card with a summary',
      goal: 'The emails that matter end up as cards you can act on, instead of lost in the inbox.',
      difficulty: 'intermedia',
      platform: 'n8n · Gmail Trigger + a model + Notion',
      trigger: 'An incoming email with a specific label (set by you or a Gmail filter).',
      steps: [
        'Gmail Trigger filtered by the "to-notion" label.',
        'Summarise the email with the model: what is being asked, by whom, by when.',
        'Create the card in the Notion database with the summary, a link to the thread, and the deadline if there is one.',
        'Remove the label from the email: the inbox stays clean and the flow does not reprocess it.',
      ],
      test: 'Label a real email: the card should appear with the right summary, and the label should disappear from the email.',
      failure: 'If duplicate cards appear, the label is not being removed (the Gmail modify scope is missing) and the polling sees it again.',
      credentials: 'Gmail OAuth with permission to modify labels, the Notion integration, and a model with a cap.',
    },
  ],

  airtable: [
    {
      name: 'New record → external process with a marker written back',
      goal: 'Airtable as the control panel: what goes in there starts what happens outside.',
      difficulty: 'basica',
      platform: 'n8n · Airtable Trigger',
      trigger: 'A record created in the table, detected by polling the Created time field.',
      steps: [
        'Add a Created time field to the table (required for the trigger to order things correctly).',
        'Airtable Trigger on the base and table.',
        'The external process: an email, a record in another system, a generated document.',
        'Write the outcome back onto the record: status "processed" and a link to what was created.',
      ],
      test: 'Create a test record and check the trip out and the marker coming back.',
      failure: 'NOT_AUTHORIZED: the token has no scope on that base (Airtable tokens are per base, not global). Without Created time the trigger repeats or skips records.',
      credentials: 'An Airtable Personal Access Token with that base selected and read/write scopes.',
    },
    {
      name: 'Airtable native automation: an alert without leaving the tool',
      goal: 'For simple alerts, Airtable can do it alone: no n8n in the middle.',
      difficulty: 'basica',
      platform: 'Airtable Automations (native)',
      trigger: 'When record matches conditions: the status changes to "urgent".',
      steps: [
        'In the Automations tab, create the automation with the condition trigger.',
        'Action: send an email or a Slack message with fields from the record.',
        'Test it with the test button against a real record.',
        'Turn it on, and note in your automation inventory that this one lives INSIDE Airtable.',
      ],
      test: 'Change a record to "urgent": the alert should arrive. Change it twice: it arrives twice (that is how it works; if it bothers you, add a date condition).',
      failure: 'The free plan caps runs per month: for high volumes, use n8n. And native automations scattered across ten bases with no inventory are the ones nobody remembers when something breaks.',
      credentials: 'Nothing external: it is all inside Airtable. That is both its appeal and its limit.',
    },
    {
      name: 'Sync Airtable with your master sheet or database',
      goal: 'Airtable (where the team works) and your master system do not drift apart.',
      difficulty: 'avanzada',
      platform: 'n8n · Schedule + Airtable + the destination',
      trigger: 'Schedule every 15 minutes, with a Last modified time field on the table.',
      steps: [
        'Read the records modified since the last pass (filter on Last modified).',
        'Compare them against the destination by unique identifier.',
        'Update only the fields that changed; create the ones that do not exist.',
        'Save this pass\'s timestamp for the next one.',
        'Conflicts (changed on both sides) are not overwritten: they get flagged for a human to review.',
      ],
      test: 'Change a record on each side and let one pass go by: the conflict should be flagged, not silently resolved.',
      failure: 'Fully automatic two-way sync is a trap: without the conflict rule, one side overwrites the other and nobody knows which version was right.',
      credentials: 'An Airtable token plus the destination credential. The shared unique identifier is the piece holding it all up.',
    },
  ],

  postgres: [
    {
      name: 'Daily report straight from SQL to the channel',
      goal: 'The morning business figure comes out of the database, not a manual export.',
      difficulty: 'basica',
      platform: 'n8n · Schedule + Postgres node',
      trigger: 'Schedule at 8:00.',
      steps: [
        'A read-only SQL query with the day\'s three numbers (sales, sign-ups, outstanding).',
        'Compare them against the same day last week.',
        'Format the message with the numbers and trend arrows.',
        'Send it to the channel. If the query fails, report the failure: a report that silently does not arrive is worse than an error.',
      ],
      test: 'Reconcile the report\'s numbers against a manual query for the first week.',
      failure: 'Heavy queries at 8:00 compete with the database\'s real work: if it is slow, materialise it overnight with pg_cron and read the materialised table.',
      credentials: 'A READ-ONLY user for n8n: a reporting flow never needs to write, and if it goes wrong one day, it should not be able to.',
    },
    {
      name: 'Anomaly watcher: odd rows, growing queues, gaps',
      goal: 'Spot what smells wrong before it becomes an incident: orders unpaid for days, queues that never drain.',
      difficulty: 'intermedia',
      platform: 'n8n · Schedule + Postgres',
      trigger: 'Schedule every hour.',
      steps: [
        'A battery of control queries, each with its own threshold: outstanding for more than 48h, a queue table above N, gaps in a sequence.',
        'Only the ones that cross a threshold alert, with the count and the three oldest cases.',
        'Every rule has its own last-alert cell: at most one alert per rule per day.',
        'Log every pass in a control table.',
      ],
      test: 'Force an anomaly in test data and check you get a single alert and a log entry.',
      failure: 'The temptation is to alert on everything: a week later the channel is muted. A few rules with well-set thresholds are worth more than twenty with nervous ones.',
      credentials: 'The same read-only user. Document the control queries: in a year nobody will remember what each one watches.',
    },
    {
      name: 'Nightly maintenance with pg_cron and an external check',
      goal: 'Archiving, clean-up and statistics every night, inside the database, with a witness outside it.',
      difficulty: 'avanzada',
      platform: 'pg_cron (native) + n8n as the watcher',
      trigger: 'cron.schedule at 3:00 UTC.',
      steps: [
        'A SQL function with the maintenance: move to archive, delete temporaries, refresh materialised views.',
        'Every operation writes its count into an audit table.',
        'An n8n flow at 8:00 that checks the audit table has tonight\'s entry.',
        'If it does not, alert: the maintenance did not run and somebody needs to look.',
      ],
      test: 'Schedule it every 5 minutes while testing, check the audit table, move to the real time, and test the watcher path too (delete the entry and wait for the alert).',
      failure: 'Maintenance that fails silently for weeks is the classic: the database grows, everything slows down and nobody knows why. That is what the external watcher is for.',
      credentials: 'Admin access to create the function and the cron job; the watcher uses the read-only user.',
    },
  ],

  vercel: [
    {
      name: 'Vercel Cron Job: your scheduled task with no server',
      goal: 'Periodic tasks (refresh data, send summaries, clean up) without maintaining any server.',
      difficulty: 'intermedia',
      platform: 'Vercel Cron Jobs (native)',
      trigger: 'The schedule declared in vercel.json: {"crons":[{"path":"/api/task","schedule":"0 7 * * *"}]}.',
      steps: [
        'Create the /api/task route in your project with the work inside it.',
        'Protect it: check the authorisation header against CRON_SECRET, because the route is public.',
        'Declare the cron in vercel.json and deploy.',
        'Check in the Vercel panel (Crons tab) that it appears and when it ran.',
        'The task writes its result into a log of yours: the panel says it ran, your log says what it did.',
      ],
      test: 'Call the route by hand with and without the secret: without it, it should return 401. Wait for the first real run and check it in the panel.',
      failure: 'An unprotected route is a task anyone can fire. And watch your plan\'s function duration limit: a long task gets cut off halfway; break it into chunks.',
      credentials: 'CRON_SECRET as an environment variable in Vercel. The schedule is in UTC.',
    },
    {
      name: 'Deploy Hook: republish the site when the content changes',
      goal: 'Your static site rebuilds itself when you edit the content in Notion, a sheet or the CMS.',
      difficulty: 'basica',
      platform: 'Vercel Deploy Hooks + n8n',
      trigger: 'The content change (detected by n8n) calls the Deploy Hook URL.',
      steps: [
        'Create the Deploy Hook in Vercel (Settings → Git → Deploy Hooks) and store its URL as a secret.',
        'In n8n, detect the content change (Notion Trigger, Sheets Trigger).',
        'Group the changes: wait 5 quiet minutes before calling, so you do not rebuild twenty times in a row.',
        'Call the hook and wait for Vercel\'s deployment succeeded/failed webhook.',
        'Alert only on failure.',
      ],
      test: 'Change the content and check the new site appears within a few minutes. Make five changes in a row: there should be ONE rebuild.',
      failure: 'Without the quiet period, every keystroke in the editor fires a build: you burn through your build quota and deploys trip over each other.',
      credentials: 'The hook URL is a secret: whoever has it can deploy for you. Store it as a credential, not in plain sight in the flow.',
    },
    {
      name: 'Failed deployment → an alert with the reason chewed over',
      goal: 'Hear about a broken build within a minute, with the error summarised, rather than finding out from a client.',
      difficulty: 'intermedia',
      platform: 'Vercel webhooks → n8n + a model',
      trigger: 'A Vercel webhook on deployment.failed (or deployment.succeeded for green silence).',
      steps: [
        'Point the project webhook at n8n.',
        'On failure: fetch the build log through the Vercel API.',
        'Summarise the last lines with the model: what failed, in one sentence of plain language.',
        'Alert with the commit, the author, that sentence and a link to the full log.',
      ],
      test: 'Break the build on purpose in a branch: the alert should arrive with an understandable cause ("variable X is missing", "type error in Y").',
      failure: 'Half of broken builds on Vercel are missing environment variables: if the summary says that, the fix is Settings → Environment Variables and a redeploy, not touching code.',
      credentials: 'The project webhook, a Vercel API token to read logs, and a model with a cap.',
    },
  ],

  gmail: [
    {
      name: 'Email with an invoice → data extracted → accounting',
      goal: 'Invoices that arrive by email end up recorded without anyone copying them by hand.',
      difficulty: 'intermedia',
      platform: 'n8n · Gmail Trigger + a language model',
      trigger: 'An incoming email with a PDF attachment and a supplier subject or sender.',
      steps: [
        'Gmail Trigger filtered: has:attachment and the "invoices" label (applied by a Gmail filter on the sender).',
        'Download the attachment and extract the text from the PDF.',
        'Extract with the model, as structured output: supplier, number, date, net, VAT and total.',
        'Validate: the total has to match net plus VAT; if it does not, send it to human review, not to the ledger.',
        'Record the row in your sheet or accounting system, with a link to the original email.',
        'Label the email "invoices/recorded" so it does not get reprocessed.',
      ],
      test: 'Forward yourself three real invoices from different suppliers: the totals should reconcile, and the ambiguous one should land in review.',
      failure: 'A tax figure extracted wrong and recorded silently is a genuine problem: the arithmetic check is the safety net, and anything that does not reconcile NEVER goes through on its own.',
      credentials: 'Gmail OAuth with read and label-modify permission, plus a model with a cap. Invoices carry tax data: use an AI provider that does not train on your data.',
    },
    {
      name: 'A self-sorting inbox: labels by type as mail arrives',
      goal: 'Mail arrives pre-sorted (client, supplier, urgent, noise) and the inbox becomes a worklist.',
      difficulty: 'intermedia',
      platform: 'n8n · Gmail Trigger + a model',
      trigger: 'Every incoming email that the native filters have not already labelled.',
      steps: [
        'The cheap thing first: native Gmail filters on sender and subject for the obvious cases (newsletters, automated notices).',
        'The ambiguous ones go to the model: classify into YOUR four or five categories, from a closed list.',
        'Apply the label, and for "urgent" add a star and a Telegram alert.',
        'A daily log of counts per category, so you can see whether the classification is holding up.',
      ],
      test: 'Run a whole day in parallel: the flow labels, but you review. If it gets fewer than 9 out of 10 right, adjust the categories before you trust it.',
      failure: 'Email classification is paid per message: the free native filters should eat 70% of it before the model touches anything. Without that, your AI bill grows with your spam.',
      credentials: 'Gmail OAuth plus a model with a spending cap. Your email is sensitive data: decide consciously which provider gets to read it.',
    },
    {
      name: 'Awaiting a reply → a reminder if nobody answered in 48h',
      goal: 'No important email you sent goes unanswered and unchased.',
      difficulty: 'intermedia',
      platform: 'n8n · Schedule + Gmail search',
      trigger: 'Schedule daily at 9:30.',
      steps: [
        'Search using Gmail syntax: sent messages with the "follow-up" label from more than 48h ago.',
        'Check whether the thread has a reply after your last message.',
        'If it does not, create the reminder as a draft (do not send it: you review it and send it yourself).',
        'Notify you with the list of outstanding items and their drafts, ready.',
        'Remove the label once the thread has a reply.',
      ],
      test: 'Label a message you sent three days ago with no reply: tomorrow the draft and the notification should appear.',
      failure: 'A reminder that sends itself ends up pressuring someone who already replied through another channel: hence the draft and the human. The automation prepares; you decide.',
      credentials: 'Gmail OAuth with draft permission. You apply the "follow-up" label when you send: that is how you tell the flow what to watch.',
    },
    {
      name: 'Important attachments → filed in Drive by folder',
      goal: 'Client attachments end up in the client\'s folder, not buried in email.',
      difficulty: 'basica',
      platform: 'n8n · Gmail Trigger + Google Drive',
      trigger: 'An email with an attachment from a sender on your client list.',
      steps: [
        'Gmail Trigger with has:attachment.',
        'Match the sender against your client list; if they are not on it, ignore.',
        'Save the attachment in Drive: the client\'s folder, named with the date (YYYY-MM-DD_subject.pdf).',
        'If the file already exists (same name and size), do not duplicate it.',
        'Add a note to your client log: a new file, with a link.',
      ],
      test: 'Send yourself an attachment from a sender on the list and another from outside it: only the first should be filed.',
      failure: 'Without the existence check, every forward of the same email duplicates the file: the folder becomes the junk drawer you were trying to avoid.',
      credentials: 'Gmail OAuth plus Drive OAuth (the same Google account works for both).',
    },
  ],
}
