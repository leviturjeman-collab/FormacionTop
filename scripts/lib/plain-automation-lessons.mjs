const titles={
 classify:['Ordena las peticiones según lo que necesitan','Sort requests by what they need'],
 'priority-alert':['Separa los avisos que necesitan atención primero','Separate notices needing attention first'],
 'daily-summary':['Prepara un resumen de lo ocurrido hoy','Prepare a summary of today’s activity'],
 deduplicate:['Reconoce una petición que llega dos veces','Recognise a request arriving twice'],
 approval:['Deja un trabajo pendiente hasta que alguien lo apruebe','Keep work pending until someone approves it'],
 retry:['Vuelve a intentar un paso sin repetir todo','Retry one step without repeating everything'],
 'document-record':['Guarda los datos de un documento','Save the details from a document'],
 'follow-up':['Comprueba qué peticiones siguen sin respuesta','Check which requests still need an answer'],
 sync:['Mantén el mismo dato en dos lugares','Keep the same detail in two places'],
 audit:['Guarda una lista de lo que hizo el proceso','Keep a list of what the process did'],
 validation:['Comprueba que no falta información','Check for missing information'],
 usage:['Cuenta cuántas veces se ha usado el proceso','Count how often the process was used'],
 draft:['Prepara una respuesta para revisarla antes de enviar','Prepare an answer for review before sending'],
 attachments:['Comprueba los archivos que llegan con una petición','Check files arriving with a request'],
 invoice:['Revisa los datos de una factura de ejemplo','Check an example invoice’s details'],
 crm:['Añade una consulta a la lista de seguimiento','Add an enquiry to the follow-up list'],
 ticket:['Crea una tarea a partir de un aviso','Create a task from a notice'],
 escalate:['Pasa un caso difícil a una persona','Pass a difficult case to a person'],
 publish:['Comprueba un contenido antes de publicarlo','Check content before publishing'],
 variants:['Prepara varias versiones de un mismo texto','Prepare several versions of a text'],
 monitor:['Comprueba si algo ha cambiado','Check whether something changed'],
 normalize:['Escribe los datos siempre de la misma forma','Write details in a consistent way'],
 onboarding:['Prepara los pasos de bienvenida','Prepare welcome steps'],
 meeting:['Convierte notas de reunión en tareas','Turn meeting notes into tasks'],
 'day-close':['Revisa lo que queda al terminar el día','Review what remains at the end of the day'],
}
export function plainAutomationManual(m,s,en=false){
 const t=(es,english)=>en?english:es
 const glossary=t('En n8n verás cajas unidas por flechas. Cada caja, llamada nodo, hace un paso. El flujo o workflow es la lista completa. Ejecutar significa poner esos pasos en marcha. Entrada es la información que recibe una caja y salida la que entrega a la siguiente. JSON es la forma de escribir nombres y valores para que los lea la aplicación. Por ejemplo, id es el número que nos permite reconocer una petición; true significa sí y false significa no.','In n8n you will see boxes joined by arrows. Each box, called a node, performs one step. The workflow is the complete list. Execute means run those steps. Input is the information a box receives; output is what it passes on. JSON writes names and values in a format the application can read. For example, id is the number identifying a request; true means yes and false means no.')
 const steps=m.steps.map((step,index)=>{
 const title=(en?['Open the practice in n8n','Read the example and the answer','Change one detail and try again','Receive details from another application','Keep a copy of what arrived','Save the result without repeating the number','Check the action in the receiving application','Reply to the application that sent the details']:['Abre la práctica en n8n','Lee el ejemplo y su respuesta','Cambia un dato y vuelve a probar','Recibe datos de otra aplicación','Conserva una copia de lo que ha llegado','Guarda el resultado sin repetir el número','Comprueba la acción en la aplicación que la recibe','Responde a la aplicación que envió los datos'])[index] || step.title
 let instruction=step.instruction
 if(index===0)instruction=t('Descarga el archivo JSON del apartado Archivos. Es un archivo de texto que contiene las cajas y sus conexiones. Abre n8n, crea un flujo vacío y utiliza la opción de importar desde archivo. Elige el archivo descargado. Verás las cajas del ejemplo. Pulsa Execute workflow para ejecutarlo. Después abre la última caja y mira su salida. Esta primera práctica utiliza datos inventados y no necesita conectar tus cuentas.','Download the JSON file in Files. It is a text file containing the boxes and their connections. Open n8n, create an empty workflow and use the import-from-file option. Choose the downloaded file. You will see the example boxes. Click Execute workflow to run it. Then open the final box and inspect its output. This first practice uses fictional details and needs no connected accounts.')
 if(index===1)instruction=t('Abre la caja llamada Muestra del proyecto. Mira los nombres que aparecen a la izquierda de cada valor y compáralos con la tabla de arriba. Después abre Resolver. Contiene instrucciones escritas en JavaScript, un lenguaje que n8n puede ejecutar. Deja seleccionado Run Once for All Items: significa hacer el paso una vez con los datos recibidos. No necesitas aprender todo ese lenguaje hoy; sigue qué dato lee cada línea y qué respuesta produce. La caja Comprobar compara la respuesta con el ejemplo resuelto y muestra un error si no coincide.','Open the box named Muestra del proyecto. Look at the names to the left of each value and compare them with the table above. Then open Resolver. It contains JavaScript instructions, a language n8n can run. Keep Run Once for All Items selected: it means doing the step once using the received details. You do not need to learn the whole language today; follow which detail each line reads and the answer it produces. Comprobar compares the answer with the worked example and reports an error if they differ. The box names remain in Spanish to match the downloadable file.')
 if(index===4)instruction=t('Antes de cambiar los datos, conserva lo que ha llegado. Añade una caja Edit Fields y llámala Entrada normalizada, igual que aparece en las instrucciones de abajo. Edit Fields significa elegir y organizar campos. Un campo es un dato, como el número o el correo. Copia los nombres y valores de la configuración. Ejecuta solo hasta aquí y comprueba que el número de la petición sigue siendo el mismo y que puedes leer el mensaje original.','Before changing details, keep what arrived. Add an Edit Fields box and name it Entrada normalizada, matching the instructions below. Edit Fields chooses and organises fields. A field is one detail, such as the number or email. Copy the setting names and values. Run up to this point and check that the request number is unchanged and the original message is still readable.')
 if(index===5)instruction=t('Ahora vamos a guardar el resultado fuera de n8n. Postgres es una base de datos: un lugar que conserva información en tablas. Primero sigue la preparación de la base de datos indicada en esta guía. Después añade Postgres y elige Execute Query, que significa ejecutar una instrucción sobre la tabla. Conecta una cuenta de prueba. Copia la consulta y coloca por separado los valores de Query Parameters, que son los datos que esa consulta necesita. No pegues contraseñas en la consulta. La regla de número único permite reconocer una petición repetida; la respuesta indica si es nueva, repetida o tiene datos diferentes. Comprueba las tres situaciones con las pruebas de abajo.','Now save the result outside n8n. Postgres is a database: a place storing information in tables. First follow this guide’s database preparation. Then add Postgres and choose Execute Query, meaning run an instruction on the table. Connect a practice account. Copy the query and supply Query Parameters separately: those are the values the query needs. Do not paste passwords into the query. The unique-number rule recognises repeated requests; its response says whether one is new, repeated or has different details. Check all three situations using the tests below.')
 if(index===7)instruction=t('Un Webhook es una dirección donde tu flujo recibe datos de otra aplicación. Esa aplicación necesita una respuesta para saber qué ocurrió. En Webhook elige Respond: Using Respond to Webhook Node y añade una caja Respond to Webhook. Prepara una respuesta para datos correctos y otra para los que faltan o no coinciden. Usa primero Test URL, la dirección de prueba, mientras n8n espera un ejemplo. Mira la respuesta desde la aplicación que envía los datos. La dirección Production URL se utiliza después de publicar el flujo y comprobarlo; no son direcciones intercambiables.','A Webhook is an address where your workflow receives details from another application. That application needs a response to know what happened. In Webhook choose Respond: Using Respond to Webhook Node and add a Respond to Webhook box. Prepare a response for correct details and another for missing or conflicting ones. First use Test URL while n8n is waiting for an example. Inspect the response in the sending application. Production URL is used after publishing and checking the workflow; the two addresses are not interchangeable.')
 return {...step,title,instruction}
 })
 return {...m,title:titles[s.id]?.[en?1:0]||m.title,context:[t('Primero harás una práctica que puedes abrir y repetir en n8n. Después aprenderás a recibir datos reales y guardarlos. No tienes que conectar todos los servicios de una vez: termina el ejemplo y comprueba su respuesta antes de continuar.','First run a practice you can open and repeat in n8n. Then learn to receive real details and save them. You do not need to connect every service at once: finish the example and check its answer before continuing.'),glossary,...m.context.slice(1)],steps}
}
