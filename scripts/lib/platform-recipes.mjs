import { englishScenarios } from './automation-projects.en.mjs'
const docs={make:'https://apps.make.com/code',zapier:'https://help.zapier.com/hc/en-us/articles/8496310939021-Use-JavaScript-code-in-Zap-workflows',pipedream:'https://pipedream.com/docs/workflows/building-workflows/code/nodejs'}
export function platformCode(s,platform,live=false){
 const source=live ? platform==='make'?'JSON.parse(input.payload)':platform==='zapier'?'JSON.parse(inputData.payload)':'steps.trigger.event.body' : JSON.stringify(s.sample,null,2)
 const body=`const x = ${source};\nfor (const key of ${JSON.stringify(s.required)}) {\n  if (x[key] === undefined || x[key] === null || x[key] === '') throw new Error('Missing '+key);\n}\nfunction resolve(x) {\n${s.body}\n}\nconst result = resolve(x);\nreturn { source_json: JSON.stringify(x), result_json: JSON.stringify(result), practice_only: true };`
 return platform==='pipedream'?`export default defineComponent({\n async run({ steps, $ }) {\n${body}\n }\n});`:body
}
export function platformRecipe(tool,s,old,en=false){
 const t=(es,english)=>en?english:es,e=englishScenarios.get(s.id)
 const title=(en?e.name:s.name)+' · '+tool.label
 const sample=JSON.stringify(s.sample,null,2),answer=JSON.stringify(s.expected,null,2),code=platformCode(s,tool.id),live=platformCode(s,tool.id,true)
 const setup=tool.id==='make'?t('Abre Make y crea un escenario nuevo. Un escenario es una lista de pasos unidos. Busca Make Code y añade Run code. Elige JavaScript y Code editor. Make Code está disponible en planes de pago; comprueba que aparece en tu cuenta antes de contratar o seguir este camino. Puedes leer el ejemplo sin contratar nada. Deja la programación desactivada mientras practicas.','Open Make and create a new scenario, a joined list of steps. Find Make Code and add Run code. Choose JavaScript and Code editor. Make Code is available on paid plans; check your account before purchasing or following this path. You can read the example without purchasing anything. Leave scheduling off while practising.'):tool.id==='zapier'?t('Crea un Zap, que es el nombre de una lista de pasos en Zapier. Para la práctica usa Schedule by Zapier como primer paso y prueba su ejemplo sin activar el Zap. Añade una acción Code by Zapier y elige Run Javascript. Deja Input Data vacío para el primer intento: el código ya contiene los datos inventados. Comprueba la disponibilidad de estos pasos en tu plan.','Create a Zap, Zapier’s name for a list of steps. For practice use Schedule by Zapier as the first step and test its example without turning the Zap on. Add a Code by Zapier action and choose Run Javascript. Leave Input Data empty for the first attempt because the code already includes fictional details. Check these steps are available on your plan.'):t('Crea un workflow en Pipedream. Es la lista de pasos del proceso. Añade un disparador HTTP/Webhook y crea un evento de prueba con los datos del ejemplo. Añade un paso Node.js y abre su editor de código. Deja sin publicar el flujo mientras preparas la práctica. El primer código usa la muestra incluida, para que puedas comparar la respuesta sin depender todavía del evento.','Create a workflow in Pipedream, the process’s list of steps. Add an HTTP/Webhook trigger and create a test event using the example details. Add a Node.js step and open its code editor. Leave the workflow unpublished while preparing the practice. The first code uses the included sample so you can compare the answer before depending on the event.')
 const mapping=tool.id==='make'?t('En Input crea una variable llamada payload. Coloca ahí un texto JSON con los datos recibidos del paso anterior. Puedes usar JSON > Create JSON para reunir los campos del formulario o del mensaje. Cambia el primer código por el del archivo live.js. input.payload es el nombre que Make utiliza para leer esa variable.','In Input create a variable named payload. Put JSON text containing the previous step’s received details there. JSON > Create JSON can collect fields from a form or message. Replace the first code with live.js. input.payload is the name Make uses to read that variable.'):tool.id==='zapier'?t('En Input Data crea la clave payload y asigna un texto JSON con los datos del paso anterior. Cambia el primer código por live.js. inputData.payload es el nombre con el que Code by Zapier lee esa clave. No escribas Ana como valor fijo si quieres procesar diferentes personas: selecciona el dato del disparador que corresponda.','In Input Data create the payload key and map JSON text containing the previous step’s details. Replace the first code with live.js. inputData.payload is how Code by Zapier reads that key. Do not enter Ana as a fixed value when processing different people: select the appropriate trigger detail.'):t('Envía los datos del ejemplo como cuerpo JSON al disparador HTTP de prueba. Abre el evento recibido y comprueba sus nombres y valores. Cambia el código por live.js, que lee steps.trigger.event.body. Esa expresión significa «el cuerpo de los datos que recibió el primer paso». Si tu disparador es de otro tipo, mira dónde coloca el evento y adapta solo esa lectura.','Send the example details as a JSON body to the test HTTP trigger. Open the received event and check its names and values. Replace the code with live.js, which reads steps.trigger.event.body. That expression means the details received in the first step’s body. If you use a different trigger type, inspect where it places the event and adapt only that reading.')
 const sheet=tool.id==='make'?'Google Sheets > Add a Row':tool.id==='zapier'?'Google Sheets > Create Spreadsheet Row':'Google Sheets > Add Single Row'
 const steps=[
 {title:t('Lee qué llega y qué debe salir','Read what arrives and what should come out'),instruction:t(`Esta práctica sirve para: ${s.purpose} Los datos de abajo son inventados. JSON es un texto con nombres y valores; los nombres a la izquierda de los dos puntos indican qué significa cada dato. Copia la muestra en tu cuaderno y compara la respuesta resuelta. true significa sí, false significa no y null significa que no hay un valor en ese lugar.`,`This practice is about: ${e.name}. The details below are fictional. JSON is text with names and values; names to the left of each colon identify a detail. Copy the sample into your notebook and compare the worked answer. true means yes, false means no and null means no value is present there.`),configuration:sample,expected:answer,why:t('Primero entiendes la regla; después dejas que la herramienta la repita.','First understand the rule; then let the tool repeat it.')},
 {title:t(`Prepara el espacio en ${tool.label}`,`Prepare the space in ${tool.label}`),instruction:setup,expected:t('Ves un editor donde puedes pegar JavaScript, el lenguaje del ejemplo. El proceso todavía no funciona de forma automática.','You can see an editor for JavaScript, the example’s language. The process is not running automatically yet.'),why:t('Usamos el editor propio de esta herramienta. No necesitas instalar n8n para esta receta.','This recipe uses the tool’s own editor. You do not need to install n8n.')},
 {title:t('Copia el código y ejecuta una prueba','Copy the code and run a test'),instruction:t('Pega el código completo de abajo en el editor. Usa el botón de prueba del paso; en Make es Run once. Busca result_json en la respuesta. Ábrelo o copia su texto para leer el resultado. source_json conserva el ejemplo original. practice_only: true recuerda que hemos calculado una respuesta, sin guardarla en otra aplicación ni enviar nada.','Paste the complete code below into the editor. Use the step’s test button; in Make use Run once. Find result_json in the reply. Open it or copy its text to read the result. source_json keeps the original example. practice_only: true reminds you that this calculated an answer without saving to another application or sending anything.'),configuration:code,expected:answer,why:t('El código lee los datos, comprueba que estén los obligatorios y aplica la regla escrita dentro de resolve. return devuelve el resultado al siguiente paso.','The code reads details, checks required ones and applies the rule written inside resolve. return passes the result to the next step.')},
 {title:t('Cambia un dato y comprueba la diferencia','Change a detail and check the difference'),instruction:(en?e.variation:s.variation)+' '+t('Hazlo en una copia del primer código. Escribe antes qué debería cambiar y compara después. Conserva el código original. Después borra uno de los datos obligatorios y repite: debe aparecer Missing seguido del nombre que falta.','Use a copy of the first code. Write what should change before trying, then compare. Keep the original code. Next remove a required detail and repeat: Missing should appear followed by its name.'),expected:t('Tienes el primer resultado, otro con un cambio y un aviso por falta de información.','You have the original result, a changed one and a missing-information warning.'),why:t('Estas pruebas te permiten comprobar que entiendes la decisión y que el proceso no continúa con datos incompletos.','These checks show whether you understand the decision and whether the process stops for incomplete details.')},
 {title:t('Usa los datos que llegan del paso anterior','Use details arriving from the previous step'),instruction:mapping+' '+t('Primero manda el mismo ejemplo. Después cambia el número o el texto y comprueba que cambia source_json. Así sabes que ya no está usando una muestra fija escondida en el código.','First send the same example. Then change its number or text and check that source_json changes. This shows it is no longer using a fixed sample hidden in the code.'),configuration:live,expected:t('La respuesta conserva exactamente los datos del último ejemplo enviado.','The response preserves the latest submitted example’s details.'),why:t('Relacionar un campo significa elegir de qué paso anterior se toma su valor. Es diferente a escribir siempre el mismo nombre.','Mapping a field means choosing which previous step supplies its value. That differs from always typing the same name.')},
 {title:t('Guarda una primera prueba en una hoja','Save one first test to a sheet'),instruction:t(`Crea una hoja de cálculo de prueba con las columnas case_id, source_json, result_json y reviewed. Añade ${sheet} después del código. Conecta tu cuenta usando el apartado de conexión, selecciona el documento y la pestaña. Para case_id escribe TEST-001 solo en este primer ensayo. Relaciona source_json y result_json con las salidas del código y deja reviewed en false. Ejecuta una vez y abre la hoja: debe aparecer una fila con esos valores. Este paso añade filas y repetirá la fila si vuelves a ejecutarlo; antes de automatizarlo sigue la explicación de números repetidos del apartado de proyecto real.`,`Create a test spreadsheet with case_id, source_json, result_json and reviewed columns. Add ${sheet} after the code. Connect your account using the connection area, then choose the document and sheet. Use TEST-001 for case_id only in this first trial. Map source_json and result_json from the code outputs and leave reviewed false. Run once and open the sheet: one row should contain those values. This action appends rows and repeats a row if run again; follow the repeated-number instructions in the real-project section before enabling automation.`),expected:t('Ves una fila real en la hoja y puedes compararla con el resultado del código.','You can see a real row in the sheet and compare it with the code result.'),why:t('Una respuesta en el editor no demuestra que esté guardada. Abrir la hoja sí permite comprobarlo.','An editor reply does not prove saving. Opening the sheet lets you check it.')},
 ]
 const prompt=t(`Ayúdame a realizar «${title}» desde cero en ${tool.label}.\nMuestra original:\n${sample}\nRespuesta que tengo que comprobar:\n${answer}\n\nLa práctica empieza con un código que calcula una respuesta. Después lee datos del paso anterior y guarda una fila en una hoja de prueba. Mi versión o plan es [PLAN]. La pantalla en la que estoy es [PANTALLA].\nExplícame un paso cada vez, con palabras corrientes. Dime qué abrir, qué valor copiar, dónde colocarlo y qué debería ver. Si necesitas un término técnico, explícalo con el ejemplo antes de continuar. No mezcles los campos de Make, Zapier y Pipedream: estoy utilizando ${tool.label}.\n\nPrimero resuelve la muestra y muestra qué datos llevan a esa respuesta. Después explica el código: qué lee x, qué comprueba la lista de campos obligatorios, qué hace resolve y qué devuelve result_json. No me pidas sustituir el resultado por un valor fijo para que la prueba parezca funcionar.\nPrepara esta segunda prueba: ${en?e.variation:s.variation} Conserva la original. También explica qué ocurre al quitar un dato obligatorio. Si una acción escribe fuera de la herramienta, dime dónde comprobar el registro y cómo evitar repetirlo antes de activar el proceso. No afirmes que un código sin almacenamiento evita duplicados entre ejecuciones.\nAl terminar, ayúdame a adaptar una copia a [MI PROYECTO]. Pide el nombre del dato que identifica cada caso y quién revisa las respuestas. Déjame una tabla con paso, dato recibido, resultado esperado, resultado visto y parte pendiente. Si no conoces un control de mi versión, pídeme sus nombres; no inventes una opción.`,
 `Help me complete “${title}” from zero in ${tool.label}.\nOriginal sample:\n${sample}\nAnswer to check:\n${answer}\n\nThe practice starts with code calculating a reply. It then reads the previous step’s details and saves a row in a test sheet. My version or plan is [PLAN]. My current screen is [SCREEN]. Explain one step at a time using everyday words. Say what to open, which value to copy, where to place it and what I should see. Explain technical words through the example. Do not mix Make, Zapier and Pipedream fields: I use ${tool.label}.\nFirst solve the sample and show which details lead to its answer. Then explain what x reads, what the required-field list checks, what resolve does and what result_json returns. Do not replace the result with a fixed value to make a test appear to work.\nPrepare this second test: ${e.variation} Keep the original. Explain what happens when a required detail is removed. If a step writes to another application, tell me where to inspect the record and how to avoid repeating it before enabling the process. Do not claim stateless code prevents duplicates between runs.\nFinally help me adapt a copy to [MY PROJECT]. Ask which detail identifies each case and who checks replies. Leave a table with step, received detail, expected result, observed result and remaining work. Ask for control names if you do not know my version; do not invent an option.`)
 const m={language:en?'en':'es',title,outcome:t('Resuelve el ejemplo, cambia sus datos y guarda una prueba que puedas revisar.','Solve the example, change its details and save a test you can review.'),context:[t(`Vas a practicar una tarea concreta con ${tool.label}. Primero verás el ejemplo resuelto, luego lo repetirás y al final prepararás una copia para tu propio proyecto. El archivo de código no envía avisos ni modifica cuentas: esas acciones se añaden después de comprobar la respuesta.`,`Practise one concrete task with ${tool.label}. First see the worked example, then repeat it and prepare a copy for your project. The code file does not send notices or change accounts; those actions are added after checking the answer.`),setup],prerequisites:[{name:tool.label,instruction:setup,check:t('El editor de código está disponible en tu cuenta.','Your account has the code editor available.')},{name:t('Hoja de práctica','Practice sheet'),instruction:t('Prepara una hoja vacía y escribe las cuatro columnas que aparecen en el último paso.','Prepare an empty sheet and write the four columns shown in the last step.'),check:t('Puedes abrirla y añadir una fila a mano.','You can open it and add one row manually.')}],inputs:Object.entries(s.sample).map(([field,value])=>({field,example:JSON.stringify(value),rule:s.required.includes(field)?t('Este dato es obligatorio. Conserva su tipo: texto, número, sí/no o lista.','This detail is required. Keep its type: text, number, yes/no or list.'):t('Conserva el valor del ejemplo durante la primera prueba.','Keep the example value for the first test.')})),steps,prompts:[{title:t('Pide ayuda con esta receta','Ask for help with this recipe'),where:t('En tu chat de IA, no en el editor de código.','In your AI chat, not the code editor.'),text:prompt,replace:t('Completa PLAN, PANTALLA y MI PROYECTO.','Fill in PLAN, SCREEN and MY PROJECT.'),expected:t('Una explicación con los campos reales de tu herramienta y la muestra resuelta.','An explanation using your tool’s actual fields and the solved sample.'),followUp:t('No entiendo el paso [NÚMERO]. Explícalo usando un solo dato del ejemplo y dime qué debería aparecer después.','I do not understand step [NUMBER]. Explain it using one example detail and say what should appear afterwards.')}],files:[{name:'practice.js',purpose:t('Código completo con la muestra incluida.','Complete code with the sample included.'),content:code},{name:'live.js',purpose:t('Código que lee datos del paso anterior. Configura la lectura antes de ejecutarlo.','Code reading the previous step’s details. Configure that reading before running it.'),content:live},{name:'example.json',purpose:t('Datos inventados para la primera prueba.','Fictional details for the first test.'),content:sample}],tests:[{name:t('Ejemplo original','Original example'),input:sample,expected:answer,inspect:'result_json'},{name:t('Cambia un dato','Change a detail'),input:en?e.variation:s.variation,expected:en?e.variation:s.variation,inspect:'result_json'},{name:t('Falta un dato','Missing detail'),input:t(`Borra ${s.required[0]} de la muestra.`,`Remove ${s.required[0]} from the sample.`),expected:'Missing '+s.required[0],inspect:t('El historial del paso de código; la hoja no debe recibir una fila.','The code step history; the sheet should not receive a row.')}],troubleshooting:[{symptom:t('Siempre aparece el mismo ejemplo','The same example always appears'),cause:t('El código sigue usando la muestra fija.','The code still uses the fixed sample.'),fix:mapping},{symptom:t('La hoja tiene dos filas iguales','The sheet contains identical rows'),cause:t('El paso de añadir fila se ha ejecutado dos veces.','The append-row step ran twice.'),fix:t('Detén la programación y comprueba los números antes de repetir. Prepara la búsqueda o la restricción de número único indicada abajo.','Stop scheduling and inspect the numbers before repeating. Prepare the lookup or unique-number rule described below.')}],production:[t('Sustituye TEST-001 por el número real del caso recibido. Añade una búsqueda de case_id antes de crear la fila. Si existe, compara el contenido y actualiza únicamente lo permitido. Para varias ejecuciones simultáneas usa una base de datos con número único: una búsqueda seguida de escritura en una hoja no garantiza evitar duplicados.','Replace TEST-001 with the real incoming case number. Look up case_id before creating a row. If it exists, compare contents and update only permitted details. For simultaneous runs use a database enforcing unique numbers; a sheet lookup followed by writing does not guarantee avoiding duplicates.'),t('El resultado calculado puede preparar un aviso, una clasificación o un borrador; no demuestra que se haya enviado o aprobado. Añade la acción del servicio que utilice tu centro después de una revisión. Elige cuenta y destinatario de prueba, relaciona sus campos y conserva la respuesta del servicio.','The calculated result may prepare a notice, category or draft; it does not prove sending or approval. Add your centre’s service action after review. Choose a test account and recipient, map its fields and retain the service response.'),t('Prueba primero tres casos pequeños con la persona responsable. Comprueba la regla del ejemplo, datos que faltan y repeticiones. Si una escritura devuelve error después de empezar, mira el destino antes de repetirla. Anota cómo detener el proceso y quién revisará los errores.','Try three small cases with the responsible person. Check the example rule, missing details and repeats. If a write reports an error after starting, inspect the destination before repeating. Record how to stop the process and who checks errors.')],sources:[{title:tool.label+' · '+t('documentación del paso de código','code-step documentation'),url:docs[tool.id]}]}
 return {...old,name:title,aliases:[old.name],goal:m.outcome,platform:tool.label,credentials:setup,trigger:steps[1].title,steps:steps.map(x=>x.instruction),code,test:m.tests[0].expected,failure:m.troubleshooting[0].fix,project:m}
}
